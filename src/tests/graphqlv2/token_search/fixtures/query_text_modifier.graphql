query {
    conditions(_debug: true, code: { text: "text query" }) {
        entry {
            resource {
                id
                code {
                    coding {
                        system
                        code
                        display
                    }
                    text
                }
            }
        }
    }
}
