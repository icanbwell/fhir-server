query {
  condition(
    _debug: true,
    onset_age: { value: "52", missing:false, notEquals: { value: "2" } }
  ) {
    entry {
      resource {
        id
        meta {
          source
        }
        onsetAge {
          value
        }
      }
    }
  }
}
