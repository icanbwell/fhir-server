query {
    riskAssessment(
        probability: {
            missing: true
        },
        _debug: true
    ) {
        entry {
            resource {
                id
            }
        }
    }
}
