query OnSubject {
  observation(
    _debug: true,
    category: {
      missing: false
    }
  ){
    entry {
      resource {
        id
        category {
          coding {
            code
          }
        }
      }
    }
  }
}
