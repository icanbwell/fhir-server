// This file is auto-generated by generate_classes so do not edit manually

/** @type {import('../complex_types/extension.js')} */
let Extension;
/** @type {import('../complex_types/codeableConcept.js')} */
let CodeableConcept;
/** @type {import('../complex_types/relatedArtifact.js')} */
let RelatedArtifact;
/** @type {import('../complex_types/reference.js')} */
let Reference;
/** @type {import('../complex_types/triggerDefinition.js')} */
let TriggerDefinition;
/** @type {import('../backbone_elements/planDefinitionCondition.js')} */
let PlanDefinitionCondition;
/** @type {import('../complex_types/dataRequirement.js')} */
let DataRequirement;
/** @type {import('../backbone_elements/planDefinitionRelatedAction.js')} */
let PlanDefinitionRelatedAction;
/** @type {import('../complex_types/quantity.js')} */
let Quantity;
/** @type {import('../complex_types/period.js')} */
let Period;
/** @type {import('../complex_types/range.js')} */
let Range;
/** @type {import('../backbone_elements/timing.js')} */
let Timing;
/** @type {import('../backbone_elements/planDefinitionParticipant.js')} */
let PlanDefinitionParticipant;
/** @type {import('../backbone_elements/planDefinitionDynamicValue.js')} */
let PlanDefinitionDynamicValue;

/**
 * Lazy loads the required serializers
 * It esnures that require is called only once for each serializer to minimize the call stack for require()
 * @returns {any}
 */
function initializeSerializers(serializerName) {
    initializeResourceSerializer()
    if (serializerName === 'Extension' && !Extension) {
        Extension = require('../complex_types/extension.js');
        return Extension;
    }
    if (serializerName === 'CodeableConcept' && !CodeableConcept) {
        CodeableConcept = require('../complex_types/codeableConcept.js');
        return CodeableConcept;
    }
    if (serializerName === 'RelatedArtifact' && !RelatedArtifact) {
        RelatedArtifact = require('../complex_types/relatedArtifact.js');
        return RelatedArtifact;
    }
    if (serializerName === 'Reference' && !Reference) {
        Reference = require('../complex_types/reference.js');
        return Reference;
    }
    if (serializerName === 'TriggerDefinition' && !TriggerDefinition) {
        TriggerDefinition = require('../complex_types/triggerDefinition.js');
        return TriggerDefinition;
    }
    if (serializerName === 'PlanDefinitionCondition' && !PlanDefinitionCondition) {
        PlanDefinitionCondition = require('../backbone_elements/planDefinitionCondition.js');
        return PlanDefinitionCondition;
    }
    if (serializerName === 'DataRequirement' && !DataRequirement) {
        DataRequirement = require('../complex_types/dataRequirement.js');
        return DataRequirement;
    }
    if (serializerName === 'PlanDefinitionRelatedAction' && !PlanDefinitionRelatedAction) {
        PlanDefinitionRelatedAction = require('../backbone_elements/planDefinitionRelatedAction.js');
        return PlanDefinitionRelatedAction;
    }
    if (serializerName === 'Quantity' && !Quantity) {
        Quantity = require('../complex_types/quantity.js');
        return Quantity;
    }
    if (serializerName === 'Period' && !Period) {
        Period = require('../complex_types/period.js');
        return Period;
    }
    if (serializerName === 'Range' && !Range) {
        Range = require('../complex_types/range.js');
        return Range;
    }
    if (serializerName === 'Timing' && !Timing) {
        Timing = require('../backbone_elements/timing.js');
        return Timing;
    }
    if (serializerName === 'PlanDefinitionParticipant' && !PlanDefinitionParticipant) {
        PlanDefinitionParticipant = require('../backbone_elements/planDefinitionParticipant.js');
        return PlanDefinitionParticipant;
    }
    if (serializerName === 'PlanDefinitionDynamicValue' && !PlanDefinitionDynamicValue) {
        PlanDefinitionDynamicValue = require('../backbone_elements/planDefinitionDynamicValue.js');
        return PlanDefinitionDynamicValue;
    }
}

/** @type {import('../../../fhirResourceSerializer.js').FhirResourceSerializer} */
let FhirResourceSerializer;

function initializeResourceSerializer() {
    if (!FhirResourceSerializer) {
        FhirResourceSerializer = require('../../../fhirResourceSerializer.js').FhirResourceSerializer;
    }

    return FhirResourceSerializer;
}

class PlanDefinitionActionSerializer {
    static propertyToSerializerMap = {
        id: null,
        extension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, Extension);
        },
        modifierExtension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, Extension);
        },
        prefix: null,
        title: null,
        description: null,
        textEquivalent: null,
        priority: null,
        code: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serializeArray(value, CodeableConcept);
        },
        reason: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serializeArray(value, CodeableConcept);
        },
        documentation: (value) => {
            initializeSerializers('RelatedArtifact');
            return FhirResourceSerializer.serializeArray(value, RelatedArtifact);
        },
        goalId: null,
        subjectCodeableConcept: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serialize(value, CodeableConcept);
        },
        subjectReference: (value) => {
            initializeSerializers('Reference');
            return FhirResourceSerializer.serialize(value, Reference);
        },
        subjectCanonical: null,
        trigger: (value) => {
            initializeSerializers('TriggerDefinition');
            return FhirResourceSerializer.serializeArray(value, TriggerDefinition);
        },
        condition: (value) => {
            initializeSerializers('PlanDefinitionCondition');
            return FhirResourceSerializer.serializeArray(value, PlanDefinitionCondition);
        },
        input: (value) => {
            initializeSerializers('DataRequirement');
            return FhirResourceSerializer.serializeArray(value, DataRequirement);
        },
        output: (value) => {
            initializeSerializers('DataRequirement');
            return FhirResourceSerializer.serializeArray(value, DataRequirement);
        },
        relatedAction: (value) => {
            initializeSerializers('PlanDefinitionRelatedAction');
            return FhirResourceSerializer.serializeArray(value, PlanDefinitionRelatedAction);
        },
        timingDateTime: null,
        timingAge: (value) => {
            initializeSerializers('Quantity');
            return FhirResourceSerializer.serialize(value, Quantity);
        },
        timingPeriod: (value) => {
            initializeSerializers('Period');
            return FhirResourceSerializer.serialize(value, Period);
        },
        timingDuration: (value) => {
            initializeSerializers('Quantity');
            return FhirResourceSerializer.serialize(value, Quantity);
        },
        timingRange: (value) => {
            initializeSerializers('Range');
            return FhirResourceSerializer.serialize(value, Range);
        },
        timingTiming: (value) => {
            initializeSerializers('Timing');
            return FhirResourceSerializer.serialize(value, Timing);
        },
        participant: (value) => {
            initializeSerializers('PlanDefinitionParticipant');
            return FhirResourceSerializer.serializeArray(value, PlanDefinitionParticipant);
        },
        type: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serialize(value, CodeableConcept);
        },
        groupingBehavior: null,
        selectionBehavior: null,
        requiredBehavior: null,
        precheckBehavior: null,
        cardinalityBehavior: null,
        definitionCanonical: null,
        definitionUri: null,
        transform: null,
        dynamicValue: (value) => {
            initializeSerializers('PlanDefinitionDynamicValue');
            return FhirResourceSerializer.serializeArray(value, PlanDefinitionDynamicValue);
        },
        action: (value) => {
            initializeSerializers('PlanDefinitionAction');
            return FhirResourceSerializer.serializeArray(value, PlanDefinitionAction);
        }
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @returns {any} Cleaned object
     */
    static serialize(rawJson) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => PlanDefinitionActionSerializer.serialize(item));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in PlanDefinitionActionSerializer.propertyToSerializerMap) {
                if (PlanDefinitionActionSerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = PlanDefinitionActionSerializer.propertyToSerializerMap[propertyName](value);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = PlanDefinitionActionSerializer;
