// This file is auto-generated by generate_classes so do not edit manually

/** @type {import('../complex_types/extension.js')} */
let ExtensionSerializer;
/** @type {import('../backbone_elements/operationDefinitionBinding.js')} */
let OperationDefinitionBindingSerializer;
/** @type {import('../backbone_elements/operationDefinitionReferencedFrom.js')} */
let OperationDefinitionReferencedFromSerializer;

/**
 * Lazy loads the required serializers
 * It esnures that require is called only once for each serializer to minimize the call stack for require()
 * @returns {any}
 */
function initializeSerializers(serializerName) {
    initializeResourceSerializer()
    if (serializerName === 'Extension' && !ExtensionSerializer) {
        ExtensionSerializer = require('../complex_types/extension.js');
        return ExtensionSerializer;
    }
    if (serializerName === 'OperationDefinitionBinding' && !OperationDefinitionBindingSerializer) {
        OperationDefinitionBindingSerializer = require('../backbone_elements/operationDefinitionBinding.js');
        return OperationDefinitionBindingSerializer;
    }
    if (serializerName === 'OperationDefinitionReferencedFrom' && !OperationDefinitionReferencedFromSerializer) {
        OperationDefinitionReferencedFromSerializer = require('../backbone_elements/operationDefinitionReferencedFrom.js');
        return OperationDefinitionReferencedFromSerializer;
    }
}

/** @type {import('../../../fhirResourceSerializer.js').FhirResourceSerializer} */
let FhirResourceSerializer;

function initializeResourceSerializer() {
    if (!FhirResourceSerializer) {
        FhirResourceSerializer = require('../../../fhirResourceSerializer.js').FhirResourceSerializer;
    }

    return FhirResourceSerializer;
}

class OperationDefinitionParameterSerializer {
    static propertyToSerializerMap = {
        id: null,
        extension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer);
        },
        modifierExtension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer);
        },
        name: null,
        use: null,
        min: null,
        max: null,
        documentation: null,
        type: null,
        targetProfile: null,
        searchType: null,
        binding: (value) => {
            initializeSerializers('OperationDefinitionBinding');
            return FhirResourceSerializer.serialize(value, OperationDefinitionBindingSerializer);
        },
        referencedFrom: (value) => {
            initializeSerializers('OperationDefinitionReferencedFrom');
            return FhirResourceSerializer.serializeArray(value, OperationDefinitionReferencedFromSerializer);
        },
        part: (value) => {
            initializeSerializers('OperationDefinitionParameter');
            return FhirResourceSerializer.serializeArray(value, OperationDefinitionParameterSerializer);
        }
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @returns {any} Cleaned object
     */
    static serialize(rawJson) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => OperationDefinitionParameterSerializer.serialize(item));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in OperationDefinitionParameterSerializer.propertyToSerializerMap) {
                if (OperationDefinitionParameterSerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = OperationDefinitionParameterSerializer.propertyToSerializerMap[propertyName](value);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = OperationDefinitionParameterSerializer;
