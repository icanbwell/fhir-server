// This file is auto-generated by generate_classes so do not edit manually

/** @type {import('../complex_types/extension.js')} */
let Extension;
/** @type {import('../complex_types/humanName.js')} */
let HumanName;
/** @type {import('../complex_types/identifier.js')} */
let Identifier;
/** @type {import('../backbone_elements/citationAffiliationInfo.js')} */
let CitationAffiliationInfo;
/** @type {import('../complex_types/address.js')} */
let Address;
/** @type {import('../complex_types/contactPoint.js')} */
let ContactPoint;
/** @type {import('../complex_types/codeableConcept.js')} */
let CodeableConcept;
/** @type {import('../backbone_elements/citationContributionInstance.js')} */
let CitationContributionInstance;

/**
 * Lazy loads the required serializers
 * It esnures that require is called only once for each serializer to minimize the call stack for require()
 * @returns {any}
 */
function initializeSerializers(serializerName) {
    initializeResourceSerializer()
    if (serializerName === 'Extension' && !Extension) {
        Extension = require('../complex_types/extension.js');
        return Extension;
    }
    if (serializerName === 'HumanName' && !HumanName) {
        HumanName = require('../complex_types/humanName.js');
        return HumanName;
    }
    if (serializerName === 'Identifier' && !Identifier) {
        Identifier = require('../complex_types/identifier.js');
        return Identifier;
    }
    if (serializerName === 'CitationAffiliationInfo' && !CitationAffiliationInfo) {
        CitationAffiliationInfo = require('../backbone_elements/citationAffiliationInfo.js');
        return CitationAffiliationInfo;
    }
    if (serializerName === 'Address' && !Address) {
        Address = require('../complex_types/address.js');
        return Address;
    }
    if (serializerName === 'ContactPoint' && !ContactPoint) {
        ContactPoint = require('../complex_types/contactPoint.js');
        return ContactPoint;
    }
    if (serializerName === 'CodeableConcept' && !CodeableConcept) {
        CodeableConcept = require('../complex_types/codeableConcept.js');
        return CodeableConcept;
    }
    if (serializerName === 'CitationContributionInstance' && !CitationContributionInstance) {
        CitationContributionInstance = require('../backbone_elements/citationContributionInstance.js');
        return CitationContributionInstance;
    }
}

/** @type {import('../../../fhirResourceSerializer.js').FhirResourceSerializer} */
let FhirResourceSerializer;

function initializeResourceSerializer() {
    if (!FhirResourceSerializer) {
        FhirResourceSerializer = require('../../../fhirResourceSerializer.js').FhirResourceSerializer;
    }

    return FhirResourceSerializer;
}

class CitationEntrySerializer {
    static propertyToSerializerMap = {
        id: null,
        extension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, Extension);
        },
        modifierExtension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, Extension);
        },
        name: (value) => {
            initializeSerializers('HumanName');
            return FhirResourceSerializer.serialize(value, HumanName);
        },
        initials: null,
        collectiveName: null,
        identifier: (value) => {
            initializeSerializers('Identifier');
            return FhirResourceSerializer.serializeArray(value, Identifier);
        },
        affiliationInfo: (value) => {
            initializeSerializers('CitationAffiliationInfo');
            return FhirResourceSerializer.serializeArray(value, CitationAffiliationInfo);
        },
        address: (value) => {
            initializeSerializers('Address');
            return FhirResourceSerializer.serializeArray(value, Address);
        },
        telecom: (value) => {
            initializeSerializers('ContactPoint');
            return FhirResourceSerializer.serializeArray(value, ContactPoint);
        },
        contributionType: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serializeArray(value, CodeableConcept);
        },
        role: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serialize(value, CodeableConcept);
        },
        contributionInstance: (value) => {
            initializeSerializers('CitationContributionInstance');
            return FhirResourceSerializer.serializeArray(value, CitationContributionInstance);
        },
        correspondingContact: null,
        listOrder: null
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @returns {any} Cleaned object
     */
    static serialize(rawJson) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => CitationEntrySerializer.serialize(item));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in CitationEntrySerializer.propertyToSerializerMap) {
                if (CitationEntrySerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = CitationEntrySerializer.propertyToSerializerMap[propertyName](value);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = CitationEntrySerializer;
