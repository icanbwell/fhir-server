// This file is auto-generated by generate_classes so do not edit manually

/** @type {import('../complex_types/extension.js')} */
let Extension;
/** @type {import('../complex_types/annotation.js')} */
let Annotation;
/** @type {import('../complex_types/codeableConcept.js')} */
let CodeableConcept;
/** @type {import('../complex_types/quantity.js')} */
let Quantity;
/** @type {import('../backbone_elements/evidenceSampleSize.js')} */
let EvidenceSampleSize;
/** @type {import('../backbone_elements/evidenceAttributeEstimate.js')} */
let EvidenceAttributeEstimate;
/** @type {import('../backbone_elements/evidenceModelCharacteristic.js')} */
let EvidenceModelCharacteristic;

/**
 * Lazy loads the required serializers
 * It esnures that require is called only once for each serializer to minimize the call stack for require()
 * @returns {any}
 */
function initializeSerializers(serializerName) {
    initializeResourceSerializer()
    if (serializerName === 'Extension' && !Extension) {
        Extension = require('../complex_types/extension.js');
        return Extension;
    }
    if (serializerName === 'Annotation' && !Annotation) {
        Annotation = require('../complex_types/annotation.js');
        return Annotation;
    }
    if (serializerName === 'CodeableConcept' && !CodeableConcept) {
        CodeableConcept = require('../complex_types/codeableConcept.js');
        return CodeableConcept;
    }
    if (serializerName === 'Quantity' && !Quantity) {
        Quantity = require('../complex_types/quantity.js');
        return Quantity;
    }
    if (serializerName === 'EvidenceSampleSize' && !EvidenceSampleSize) {
        EvidenceSampleSize = require('../backbone_elements/evidenceSampleSize.js');
        return EvidenceSampleSize;
    }
    if (serializerName === 'EvidenceAttributeEstimate' && !EvidenceAttributeEstimate) {
        EvidenceAttributeEstimate = require('../backbone_elements/evidenceAttributeEstimate.js');
        return EvidenceAttributeEstimate;
    }
    if (serializerName === 'EvidenceModelCharacteristic' && !EvidenceModelCharacteristic) {
        EvidenceModelCharacteristic = require('../backbone_elements/evidenceModelCharacteristic.js');
        return EvidenceModelCharacteristic;
    }
}

/** @type {import('../../../fhirResourceSerializer.js').FhirResourceSerializer} */
let FhirResourceSerializer;

function initializeResourceSerializer() {
    if (!FhirResourceSerializer) {
        FhirResourceSerializer = require('../../../fhirResourceSerializer.js').FhirResourceSerializer;
    }

    return FhirResourceSerializer;
}

class EvidenceStatisticSerializer {
    static propertyToSerializerMap = {
        id: null,
        extension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, Extension);
        },
        modifierExtension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, Extension);
        },
        description: null,
        note: (value) => {
            initializeSerializers('Annotation');
            return FhirResourceSerializer.serializeArray(value, Annotation);
        },
        statisticType: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serialize(value, CodeableConcept);
        },
        category: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serialize(value, CodeableConcept);
        },
        quantity: (value) => {
            initializeSerializers('Quantity');
            return FhirResourceSerializer.serialize(value, Quantity);
        },
        numberOfEvents: null,
        numberAffected: null,
        sampleSize: (value) => {
            initializeSerializers('EvidenceSampleSize');
            return FhirResourceSerializer.serialize(value, EvidenceSampleSize);
        },
        attributeEstimate: (value) => {
            initializeSerializers('EvidenceAttributeEstimate');
            return FhirResourceSerializer.serializeArray(value, EvidenceAttributeEstimate);
        },
        modelCharacteristic: (value) => {
            initializeSerializers('EvidenceModelCharacteristic');
            return FhirResourceSerializer.serializeArray(value, EvidenceModelCharacteristic);
        }
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @returns {any} Cleaned object
     */
    static serialize(rawJson) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => EvidenceStatisticSerializer.serialize(item));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in EvidenceStatisticSerializer.propertyToSerializerMap) {
                if (EvidenceStatisticSerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = EvidenceStatisticSerializer.propertyToSerializerMap[propertyName](value);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = EvidenceStatisticSerializer;
