// This file is auto-generated by generate_classes so do not edit manually

/** @type {import('../complex_types/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complex_types/annotation.js')} */
let AnnotationSerializer;
/** @type {import('../complex_types/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../complex_types/quantity.js')} */
let QuantitySerializer;
/** @type {import('../backbone_elements/evidenceSampleSize.js')} */
let EvidenceSampleSizeSerializer;
/** @type {import('../backbone_elements/evidenceAttributeEstimate.js')} */
let EvidenceAttributeEstimateSerializer;
/** @type {import('../backbone_elements/evidenceModelCharacteristic.js')} */
let EvidenceModelCharacteristicSerializer;

/**
 * Lazy loads the required serializers
 * It esnures that require is called only once for each serializer to minimize the call stack for require()
 * @returns {any}
 */
function initializeSerializers(serializerName) {
    initializeResourceSerializer()
    if (serializerName === 'Extension' && !ExtensionSerializer) {
        ExtensionSerializer = require('../complex_types/extension.js');
        return ExtensionSerializer;
    }
    if (serializerName === 'Annotation' && !AnnotationSerializer) {
        AnnotationSerializer = require('../complex_types/annotation.js');
        return AnnotationSerializer;
    }
    if (serializerName === 'CodeableConcept' && !CodeableConceptSerializer) {
        CodeableConceptSerializer = require('../complex_types/codeableConcept.js');
        return CodeableConceptSerializer;
    }
    if (serializerName === 'Quantity' && !QuantitySerializer) {
        QuantitySerializer = require('../complex_types/quantity.js');
        return QuantitySerializer;
    }
    if (serializerName === 'EvidenceSampleSize' && !EvidenceSampleSizeSerializer) {
        EvidenceSampleSizeSerializer = require('../backbone_elements/evidenceSampleSize.js');
        return EvidenceSampleSizeSerializer;
    }
    if (serializerName === 'EvidenceAttributeEstimate' && !EvidenceAttributeEstimateSerializer) {
        EvidenceAttributeEstimateSerializer = require('../backbone_elements/evidenceAttributeEstimate.js');
        return EvidenceAttributeEstimateSerializer;
    }
    if (serializerName === 'EvidenceModelCharacteristic' && !EvidenceModelCharacteristicSerializer) {
        EvidenceModelCharacteristicSerializer = require('../backbone_elements/evidenceModelCharacteristic.js');
        return EvidenceModelCharacteristicSerializer;
    }
}

/** @type {import('../../../fhirResourceSerializer.js').FhirResourceSerializer} */
let FhirResourceSerializer;

function initializeResourceSerializer() {
    if (!FhirResourceSerializer) {
        FhirResourceSerializer = require('../../../fhirResourceSerializer.js').FhirResourceSerializer;
    }

    return FhirResourceSerializer;
}

class EvidenceStatisticSerializer {
    static propertyToSerializerMap = {
        id: null,
        extension: (value, context) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer, context);
        },
        modifierExtension: (value, context) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer, context);
        },
        description: null,
        note: (value, context) => {
            initializeSerializers('Annotation');
            return FhirResourceSerializer.serializeArray(value, AnnotationSerializer, context);
        },
        statisticType: (value, context) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serialize(value, CodeableConceptSerializer, context);
        },
        category: (value, context) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serialize(value, CodeableConceptSerializer, context);
        },
        quantity: (value, context) => {
            initializeSerializers('Quantity');
            return FhirResourceSerializer.serialize(value, QuantitySerializer, context);
        },
        numberOfEvents: null,
        numberAffected: null,
        sampleSize: (value, context) => {
            initializeSerializers('EvidenceSampleSize');
            return FhirResourceSerializer.serialize(value, EvidenceSampleSizeSerializer, context);
        },
        attributeEstimate: (value, context) => {
            initializeSerializers('EvidenceAttributeEstimate');
            return FhirResourceSerializer.serializeArray(value, EvidenceAttributeEstimateSerializer, context);
        },
        modelCharacteristic: (value, context) => {
            initializeSerializers('EvidenceModelCharacteristic');
            return FhirResourceSerializer.serializeArray(value, EvidenceModelCharacteristicSerializer, context);
        }
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @param {Object} context
     * @returns {any} Cleaned object
     */
    static serialize(rawJson, context = {}) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => EvidenceStatisticSerializer.serialize(item, context));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in EvidenceStatisticSerializer.propertyToSerializerMap) {
                if (EvidenceStatisticSerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = EvidenceStatisticSerializer.propertyToSerializerMap[propertyName](value, context);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = EvidenceStatisticSerializer;
