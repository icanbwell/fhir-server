// This file is auto-generated by generate_classes so do not edit manually

/** @type {import('../complex_types/extension.js')} */
let ExtensionSerializer;
/** @type {import('../backbone_elements/structureMapSource.js')} */
let StructureMapSourceSerializer;
/** @type {import('../backbone_elements/structureMapTarget.js')} */
let StructureMapTargetSerializer;
/** @type {import('../backbone_elements/structureMapDependent.js')} */
let StructureMapDependentSerializer;

/**
 * Lazy loads the required serializers
 * It esnures that require is called only once for each serializer to minimize the call stack for require()
 * @returns {any}
 */
function initializeSerializers(serializerName) {
    initializeResourceSerializer()
    if (serializerName === 'Extension' && !ExtensionSerializer) {
        ExtensionSerializer = require('../complex_types/extension.js');
        return ExtensionSerializer;
    }
    if (serializerName === 'StructureMapSource' && !StructureMapSourceSerializer) {
        StructureMapSourceSerializer = require('../backbone_elements/structureMapSource.js');
        return StructureMapSourceSerializer;
    }
    if (serializerName === 'StructureMapTarget' && !StructureMapTargetSerializer) {
        StructureMapTargetSerializer = require('../backbone_elements/structureMapTarget.js');
        return StructureMapTargetSerializer;
    }
    if (serializerName === 'StructureMapDependent' && !StructureMapDependentSerializer) {
        StructureMapDependentSerializer = require('../backbone_elements/structureMapDependent.js');
        return StructureMapDependentSerializer;
    }
}

/** @type {import('../../../fhirResourceSerializer.js').FhirResourceSerializer} */
let FhirResourceSerializer;

function initializeResourceSerializer() {
    if (!FhirResourceSerializer) {
        FhirResourceSerializer = require('../../../fhirResourceSerializer.js').FhirResourceSerializer;
    }

    return FhirResourceSerializer;
}

class StructureMapRuleSerializer {
    static propertyToSerializerMap = {
        id: null,
        extension: (value, context) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer, context);
        },
        modifierExtension: (value, context) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer, context);
        },
        name: null,
        source: (value, context) => {
            initializeSerializers('StructureMapSource');
            return FhirResourceSerializer.serializeArray(value, StructureMapSourceSerializer, context);
        },
        target: (value, context) => {
            initializeSerializers('StructureMapTarget');
            return FhirResourceSerializer.serializeArray(value, StructureMapTargetSerializer, context);
        },
        rule: (value, context) => {
            initializeSerializers('StructureMapRule');
            return FhirResourceSerializer.serializeArray(value, StructureMapRuleSerializer, context);
        },
        dependent: (value, context) => {
            initializeSerializers('StructureMapDependent');
            return FhirResourceSerializer.serializeArray(value, StructureMapDependentSerializer, context);
        },
        documentation: null
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @param {Object} context
     * @returns {any} Cleaned object
     */
    static serialize(rawJson, context = {}) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => StructureMapRuleSerializer.serialize(item, context));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in StructureMapRuleSerializer.propertyToSerializerMap) {
                if (StructureMapRuleSerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = StructureMapRuleSerializer.propertyToSerializerMap[propertyName](value, context);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = StructureMapRuleSerializer;
