// This file is auto-generated by generate_classes so do not edit manually

/** @type {import('../complex_types/extension.js')} */
let ExtensionSerializer;
/** @type {import('../backbone_elements/implementationGuideResource1.js')} */
let ImplementationGuideResource1Serializer;
/** @type {import('../backbone_elements/implementationGuidePage1.js')} */
let ImplementationGuidePage1Serializer;

/**
 * Lazy loads the required serializers
 * It esnures that require is called only once for each serializer to minimize the call stack for require()
 * @returns {any}
 */
function initializeSerializers(serializerName) {
    initializeResourceSerializer()
    if (serializerName === 'Extension' && !ExtensionSerializer) {
        ExtensionSerializer = require('../complex_types/extension.js');
        return ExtensionSerializer;
    }
    if (serializerName === 'ImplementationGuideResource1' && !ImplementationGuideResource1Serializer) {
        ImplementationGuideResource1Serializer = require('../backbone_elements/implementationGuideResource1.js');
        return ImplementationGuideResource1Serializer;
    }
    if (serializerName === 'ImplementationGuidePage1' && !ImplementationGuidePage1Serializer) {
        ImplementationGuidePage1Serializer = require('../backbone_elements/implementationGuidePage1.js');
        return ImplementationGuidePage1Serializer;
    }
}

/** @type {import('../../../fhirResourceSerializer.js').FhirResourceSerializer} */
let FhirResourceSerializer;

function initializeResourceSerializer() {
    if (!FhirResourceSerializer) {
        FhirResourceSerializer = require('../../../fhirResourceSerializer.js').FhirResourceSerializer;
    }

    return FhirResourceSerializer;
}

class ImplementationGuideManifestSerializer {
    static propertyToSerializerMap = {
        id: null,
        extension: (value, context) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer, context);
        },
        modifierExtension: (value, context) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer, context);
        },
        rendering: null,
        resource: (value, context) => {
            initializeSerializers('ImplementationGuideResource1');
            return FhirResourceSerializer.serializeArray(value, ImplementationGuideResource1Serializer, context);
        },
        page: (value, context) => {
            initializeSerializers('ImplementationGuidePage1');
            return FhirResourceSerializer.serializeArray(value, ImplementationGuidePage1Serializer, context);
        },
        image: null,
        other: null
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @param {Object} context
     * @returns {any} Cleaned object
     */
    static serialize(rawJson, context = {}) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => ImplementationGuideManifestSerializer.serialize(item, context));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in ImplementationGuideManifestSerializer.propertyToSerializerMap) {
                if (ImplementationGuideManifestSerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = ImplementationGuideManifestSerializer.propertyToSerializerMap[propertyName](value, context);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = ImplementationGuideManifestSerializer;
