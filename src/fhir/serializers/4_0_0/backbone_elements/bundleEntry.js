// This file is auto-generated by generate_classes so do not edit manually

/** @type {import('../complex_types/extension.js')} */
let Extension;
/** @type {import('../backbone_elements/bundleLink.js')} */
let BundleLink;
/** @type {import('../simple_types/resourceContainer.js')} */
let ResourceContainer;
/** @type {import('../backbone_elements/bundleSearch.js')} */
let BundleSearch;
/** @type {import('../backbone_elements/bundleRequest.js')} */
let BundleRequest;
/** @type {import('../backbone_elements/bundleResponse.js')} */
let BundleResponse;

/**
 * Lazy loads the required serializers
 * It esnures that require is called only once for each serializer to minimize the call stack for require()
 * @returns {any}
 */
function initializeSerializers(serializerName) {
    initializeResourceSerializer()
    if (serializerName === 'Extension' && !Extension) {
        Extension = require('../complex_types/extension.js');
        return Extension;
    }
    if (serializerName === 'BundleLink' && !BundleLink) {
        BundleLink = require('../backbone_elements/bundleLink.js');
        return BundleLink;
    }
    if (serializerName === 'ResourceContainer' && !ResourceContainer) {
        ResourceContainer = require('../simple_types/resourceContainer.js');
        return ResourceContainer;
    }
    if (serializerName === 'BundleSearch' && !BundleSearch) {
        BundleSearch = require('../backbone_elements/bundleSearch.js');
        return BundleSearch;
    }
    if (serializerName === 'BundleRequest' && !BundleRequest) {
        BundleRequest = require('../backbone_elements/bundleRequest.js');
        return BundleRequest;
    }
    if (serializerName === 'BundleResponse' && !BundleResponse) {
        BundleResponse = require('../backbone_elements/bundleResponse.js');
        return BundleResponse;
    }
}

/** @type {import('../../../fhirResourceSerializer.js').FhirResourceSerializer} */
let FhirResourceSerializer;

function initializeResourceSerializer() {
    if (!FhirResourceSerializer) {
        FhirResourceSerializer = require('../../../fhirResourceSerializer.js').FhirResourceSerializer;
    }

    return FhirResourceSerializer;
}

class BundleEntrySerializer {
    static propertyToSerializerMap = {
        id: null,
        extension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, Extension);
        },
        modifierExtension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, Extension);
        },
        link: (value) => {
            initializeSerializers('BundleLink');
            return FhirResourceSerializer.serializeArray(value, BundleLink);
        },
        fullUrl: null,
        resource: (value) => {
            initializeSerializers('ResourceContainer');
            return FhirResourceSerializer.serialize(value);
        },
        search: (value) => {
            initializeSerializers('BundleSearch');
            return FhirResourceSerializer.serialize(value, BundleSearch);
        },
        request: (value) => {
            initializeSerializers('BundleRequest');
            return FhirResourceSerializer.serialize(value, BundleRequest);
        },
        response: (value) => {
            initializeSerializers('BundleResponse');
            return FhirResourceSerializer.serialize(value, BundleResponse);
        }
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @returns {any} Cleaned object
     */
    static serialize(rawJson) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => BundleEntrySerializer.serialize(item));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in BundleEntrySerializer.propertyToSerializerMap) {
                if (BundleEntrySerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = BundleEntrySerializer.propertyToSerializerMap[propertyName](value);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = BundleEntrySerializer;
