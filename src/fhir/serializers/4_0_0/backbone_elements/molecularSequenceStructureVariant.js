// This file is auto-generated by generate_classes so do not edit manually

/** @type {import('../complex_types/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complex_types/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../backbone_elements/molecularSequenceOuter.js')} */
let MolecularSequenceOuterSerializer;
/** @type {import('../backbone_elements/molecularSequenceInner.js')} */
let MolecularSequenceInnerSerializer;

/**
 * Lazy loads the required serializers
 * It esnures that require is called only once for each serializer to minimize the call stack for require()
 * @returns {any}
 */
function initializeSerializers(serializerName) {
    initializeResourceSerializer()
    if (serializerName === 'Extension' && !ExtensionSerializer) {
        ExtensionSerializer = require('../complex_types/extension.js');
        return ExtensionSerializer;
    }
    if (serializerName === 'CodeableConcept' && !CodeableConceptSerializer) {
        CodeableConceptSerializer = require('../complex_types/codeableConcept.js');
        return CodeableConceptSerializer;
    }
    if (serializerName === 'MolecularSequenceOuter' && !MolecularSequenceOuterSerializer) {
        MolecularSequenceOuterSerializer = require('../backbone_elements/molecularSequenceOuter.js');
        return MolecularSequenceOuterSerializer;
    }
    if (serializerName === 'MolecularSequenceInner' && !MolecularSequenceInnerSerializer) {
        MolecularSequenceInnerSerializer = require('../backbone_elements/molecularSequenceInner.js');
        return MolecularSequenceInnerSerializer;
    }
}

/** @type {import('../../../fhirResourceSerializer.js').FhirResourceSerializer} */
let FhirResourceSerializer;

function initializeResourceSerializer() {
    if (!FhirResourceSerializer) {
        FhirResourceSerializer = require('../../../fhirResourceSerializer.js').FhirResourceSerializer;
    }

    return FhirResourceSerializer;
}

class MolecularSequenceStructureVariantSerializer {
    static propertyToSerializerMap = {
        id: null,
        extension: (value, context) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer, context);
        },
        modifierExtension: (value, context) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer, context);
        },
        variantType: (value, context) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serialize(value, CodeableConceptSerializer, context);
        },
        exact: null,
        length: null,
        outer: (value, context) => {
            initializeSerializers('MolecularSequenceOuter');
            return FhirResourceSerializer.serialize(value, MolecularSequenceOuterSerializer, context);
        },
        inner: (value, context) => {
            initializeSerializers('MolecularSequenceInner');
            return FhirResourceSerializer.serialize(value, MolecularSequenceInnerSerializer, context);
        }
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @param {Object} context
     * @returns {any} Cleaned object
     */
    static serialize(rawJson, context = {}) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => MolecularSequenceStructureVariantSerializer.serialize(item, context));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in MolecularSequenceStructureVariantSerializer.propertyToSerializerMap) {
                if (MolecularSequenceStructureVariantSerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = MolecularSequenceStructureVariantSerializer.propertyToSerializerMap[propertyName](value, context);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = MolecularSequenceStructureVariantSerializer;
