// This file is auto-generated by generate_classes so do not edit manually

/** @type {import('../complex_types/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complex_types/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../complex_types/reference.js')} */
let ReferenceSerializer;
/** @type {import('../complex_types/coding.js')} */
let CodingSerializer;
/** @type {import('../backbone_elements/contractContext.js')} */
let ContractContextSerializer;
/** @type {import('../complex_types/period.js')} */
let PeriodSerializer;
/** @type {import('../backbone_elements/contractAnswer.js')} */
let ContractAnswerSerializer;
/** @type {import('../backbone_elements/contractValuedItem.js')} */
let ContractValuedItemSerializer;

/**
 * Lazy loads the required serializers
 * It esnures that require is called only once for each serializer to minimize the call stack for require()
 * @returns {any}
 */
function initializeSerializers(serializerName) {
    initializeResourceSerializer()
    if (serializerName === 'Extension' && !ExtensionSerializer) {
        ExtensionSerializer = require('../complex_types/extension.js');
        return ExtensionSerializer;
    }
    if (serializerName === 'CodeableConcept' && !CodeableConceptSerializer) {
        CodeableConceptSerializer = require('../complex_types/codeableConcept.js');
        return CodeableConceptSerializer;
    }
    if (serializerName === 'Reference' && !ReferenceSerializer) {
        ReferenceSerializer = require('../complex_types/reference.js');
        return ReferenceSerializer;
    }
    if (serializerName === 'Coding' && !CodingSerializer) {
        CodingSerializer = require('../complex_types/coding.js');
        return CodingSerializer;
    }
    if (serializerName === 'ContractContext' && !ContractContextSerializer) {
        ContractContextSerializer = require('../backbone_elements/contractContext.js');
        return ContractContextSerializer;
    }
    if (serializerName === 'Period' && !PeriodSerializer) {
        PeriodSerializer = require('../complex_types/period.js');
        return PeriodSerializer;
    }
    if (serializerName === 'ContractAnswer' && !ContractAnswerSerializer) {
        ContractAnswerSerializer = require('../backbone_elements/contractAnswer.js');
        return ContractAnswerSerializer;
    }
    if (serializerName === 'ContractValuedItem' && !ContractValuedItemSerializer) {
        ContractValuedItemSerializer = require('../backbone_elements/contractValuedItem.js');
        return ContractValuedItemSerializer;
    }
}

/** @type {import('../../../fhirResourceSerializer.js').FhirResourceSerializer} */
let FhirResourceSerializer;

function initializeResourceSerializer() {
    if (!FhirResourceSerializer) {
        FhirResourceSerializer = require('../../../fhirResourceSerializer.js').FhirResourceSerializer;
    }

    return FhirResourceSerializer;
}

class ContractAssetSerializer {
    static propertyToSerializerMap = {
        id: null,
        extension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer);
        },
        modifierExtension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer);
        },
        scope: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serialize(value, CodeableConceptSerializer);
        },
        type: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serializeArray(value, CodeableConceptSerializer);
        },
        typeReference: (value) => {
            initializeSerializers('Reference');
            return FhirResourceSerializer.serializeArray(value, ReferenceSerializer);
        },
        subtype: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serializeArray(value, CodeableConceptSerializer);
        },
        relationship: (value) => {
            initializeSerializers('Coding');
            return FhirResourceSerializer.serialize(value, CodingSerializer);
        },
        context: (value) => {
            initializeSerializers('ContractContext');
            return FhirResourceSerializer.serializeArray(value, ContractContextSerializer);
        },
        condition: null,
        periodType: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serializeArray(value, CodeableConceptSerializer);
        },
        period: (value) => {
            initializeSerializers('Period');
            return FhirResourceSerializer.serializeArray(value, PeriodSerializer);
        },
        usePeriod: (value) => {
            initializeSerializers('Period');
            return FhirResourceSerializer.serializeArray(value, PeriodSerializer);
        },
        text: null,
        linkId: null,
        answer: (value) => {
            initializeSerializers('ContractAnswer');
            return FhirResourceSerializer.serializeArray(value, ContractAnswerSerializer);
        },
        securityLabelNumber: null,
        valuedItem: (value) => {
            initializeSerializers('ContractValuedItem');
            return FhirResourceSerializer.serializeArray(value, ContractValuedItemSerializer);
        }
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @returns {any} Cleaned object
     */
    static serialize(rawJson) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => ContractAssetSerializer.serialize(item));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in ContractAssetSerializer.propertyToSerializerMap) {
                if (ContractAssetSerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = ContractAssetSerializer.propertyToSerializerMap[propertyName](value);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = ContractAssetSerializer;
