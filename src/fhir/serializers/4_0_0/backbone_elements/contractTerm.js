// This file is auto-generated by generate_classes so do not edit manually

/** @type {import('../complex_types/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complex_types/identifier.js')} */
let IdentifierSerializer;
/** @type {import('../complex_types/period.js')} */
let PeriodSerializer;
/** @type {import('../complex_types/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../complex_types/reference.js')} */
let ReferenceSerializer;
/** @type {import('../backbone_elements/contractSecurityLabel.js')} */
let ContractSecurityLabelSerializer;
/** @type {import('../backbone_elements/contractOffer.js')} */
let ContractOfferSerializer;
/** @type {import('../backbone_elements/contractAsset.js')} */
let ContractAssetSerializer;
/** @type {import('../backbone_elements/contractAction.js')} */
let ContractActionSerializer;

/**
 * Lazy loads the required serializers
 * It esnures that require is called only once for each serializer to minimize the call stack for require()
 * @returns {any}
 */
function initializeSerializers(serializerName) {
    initializeResourceSerializer()
    if (serializerName === 'Extension' && !ExtensionSerializer) {
        ExtensionSerializer = require('../complex_types/extension.js');
        return ExtensionSerializer;
    }
    if (serializerName === 'Identifier' && !IdentifierSerializer) {
        IdentifierSerializer = require('../complex_types/identifier.js');
        return IdentifierSerializer;
    }
    if (serializerName === 'Period' && !PeriodSerializer) {
        PeriodSerializer = require('../complex_types/period.js');
        return PeriodSerializer;
    }
    if (serializerName === 'CodeableConcept' && !CodeableConceptSerializer) {
        CodeableConceptSerializer = require('../complex_types/codeableConcept.js');
        return CodeableConceptSerializer;
    }
    if (serializerName === 'Reference' && !ReferenceSerializer) {
        ReferenceSerializer = require('../complex_types/reference.js');
        return ReferenceSerializer;
    }
    if (serializerName === 'ContractSecurityLabel' && !ContractSecurityLabelSerializer) {
        ContractSecurityLabelSerializer = require('../backbone_elements/contractSecurityLabel.js');
        return ContractSecurityLabelSerializer;
    }
    if (serializerName === 'ContractOffer' && !ContractOfferSerializer) {
        ContractOfferSerializer = require('../backbone_elements/contractOffer.js');
        return ContractOfferSerializer;
    }
    if (serializerName === 'ContractAsset' && !ContractAssetSerializer) {
        ContractAssetSerializer = require('../backbone_elements/contractAsset.js');
        return ContractAssetSerializer;
    }
    if (serializerName === 'ContractAction' && !ContractActionSerializer) {
        ContractActionSerializer = require('../backbone_elements/contractAction.js');
        return ContractActionSerializer;
    }
}

/** @type {import('../../../fhirResourceSerializer.js').FhirResourceSerializer} */
let FhirResourceSerializer;

function initializeResourceSerializer() {
    if (!FhirResourceSerializer) {
        FhirResourceSerializer = require('../../../fhirResourceSerializer.js').FhirResourceSerializer;
    }

    return FhirResourceSerializer;
}

class ContractTermSerializer {
    static propertyToSerializerMap = {
        id: null,
        extension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer);
        },
        modifierExtension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer);
        },
        identifier: (value) => {
            initializeSerializers('Identifier');
            return FhirResourceSerializer.serialize(value, IdentifierSerializer);
        },
        issued: null,
        applies: (value) => {
            initializeSerializers('Period');
            return FhirResourceSerializer.serialize(value, PeriodSerializer);
        },
        topicCodeableConcept: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serialize(value, CodeableConceptSerializer);
        },
        topicReference: (value) => {
            initializeSerializers('Reference');
            return FhirResourceSerializer.serialize(value, ReferenceSerializer);
        },
        type: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serialize(value, CodeableConceptSerializer);
        },
        subType: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serialize(value, CodeableConceptSerializer);
        },
        text: null,
        securityLabel: (value) => {
            initializeSerializers('ContractSecurityLabel');
            return FhirResourceSerializer.serializeArray(value, ContractSecurityLabelSerializer);
        },
        offer: (value) => {
            initializeSerializers('ContractOffer');
            return FhirResourceSerializer.serialize(value, ContractOfferSerializer);
        },
        asset: (value) => {
            initializeSerializers('ContractAsset');
            return FhirResourceSerializer.serializeArray(value, ContractAssetSerializer);
        },
        action: (value) => {
            initializeSerializers('ContractAction');
            return FhirResourceSerializer.serializeArray(value, ContractActionSerializer);
        },
        group: (value) => {
            initializeSerializers('ContractTerm');
            return FhirResourceSerializer.serializeArray(value, ContractTermSerializer);
        }
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @returns {any} Cleaned object
     */
    static serialize(rawJson) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => ContractTermSerializer.serialize(item));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in ContractTermSerializer.propertyToSerializerMap) {
                if (ContractTermSerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = ContractTermSerializer.propertyToSerializerMap[propertyName](value);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = ContractTermSerializer;
