// This file is auto-generated by generate_classes so do not edit manually

/** @type {import('../complex_types/extension.js')} */
let Extension;
/** @type {import('../complex_types/reference.js')} */
let Reference;
/** @type {import('../backbone_elements/medicationKnowledgeSubstitution.js')} */
let MedicationKnowledgeSubstitution;
/** @type {import('../backbone_elements/medicationKnowledgeSchedule.js')} */
let MedicationKnowledgeSchedule;
/** @type {import('../backbone_elements/medicationKnowledgeMaxDispense.js')} */
let MedicationKnowledgeMaxDispense;

/**
 * Lazy loads the required serializers
 * It esnures that require is called only once for each serializer to minimize the call stack for require()
 * @returns {any}
 */
function initializeSerializers(serializerName) {
    initializeResourceSerializer()
    if (serializerName === 'Extension' && !Extension) {
        Extension = require('../complex_types/extension.js');
        return Extension;
    }
    if (serializerName === 'Reference' && !Reference) {
        Reference = require('../complex_types/reference.js');
        return Reference;
    }
    if (serializerName === 'MedicationKnowledgeSubstitution' && !MedicationKnowledgeSubstitution) {
        MedicationKnowledgeSubstitution = require('../backbone_elements/medicationKnowledgeSubstitution.js');
        return MedicationKnowledgeSubstitution;
    }
    if (serializerName === 'MedicationKnowledgeSchedule' && !MedicationKnowledgeSchedule) {
        MedicationKnowledgeSchedule = require('../backbone_elements/medicationKnowledgeSchedule.js');
        return MedicationKnowledgeSchedule;
    }
    if (serializerName === 'MedicationKnowledgeMaxDispense' && !MedicationKnowledgeMaxDispense) {
        MedicationKnowledgeMaxDispense = require('../backbone_elements/medicationKnowledgeMaxDispense.js');
        return MedicationKnowledgeMaxDispense;
    }
}

/** @type {import('../../../fhirResourceSerializer.js').FhirResourceSerializer} */
let FhirResourceSerializer;

function initializeResourceSerializer() {
    if (!FhirResourceSerializer) {
        FhirResourceSerializer = require('../../../fhirResourceSerializer.js').FhirResourceSerializer;
    }

    return FhirResourceSerializer;
}

class MedicationKnowledgeRegulatorySerializer {
    static propertyToSerializerMap = {
        id: null,
        extension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, Extension);
        },
        modifierExtension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, Extension);
        },
        regulatoryAuthority: (value) => {
            initializeSerializers('Reference');
            return FhirResourceSerializer.serialize(value, Reference);
        },
        substitution: (value) => {
            initializeSerializers('MedicationKnowledgeSubstitution');
            return FhirResourceSerializer.serializeArray(value, MedicationKnowledgeSubstitution);
        },
        schedule: (value) => {
            initializeSerializers('MedicationKnowledgeSchedule');
            return FhirResourceSerializer.serializeArray(value, MedicationKnowledgeSchedule);
        },
        maxDispense: (value) => {
            initializeSerializers('MedicationKnowledgeMaxDispense');
            return FhirResourceSerializer.serialize(value, MedicationKnowledgeMaxDispense);
        }
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @returns {any} Cleaned object
     */
    static serialize(rawJson) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => MedicationKnowledgeRegulatorySerializer.serialize(item));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in MedicationKnowledgeRegulatorySerializer.propertyToSerializerMap) {
                if (MedicationKnowledgeRegulatorySerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = MedicationKnowledgeRegulatorySerializer.propertyToSerializerMap[propertyName](value);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = MedicationKnowledgeRegulatorySerializer;
