// This file is auto-generated by generate_classes so do not edit manually

/** @type {import('../complex_types/extension.js')} */
let ExtensionSerializer;
/** @type {import('../backbone_elements/chargeItemDefinitionApplicability.js')} */
let ChargeItemDefinitionApplicabilitySerializer;
/** @type {import('../backbone_elements/chargeItemDefinitionPriceComponent.js')} */
let ChargeItemDefinitionPriceComponentSerializer;

/**
 * Lazy loads the required serializers
 * It esnures that require is called only once for each serializer to minimize the call stack for require()
 * @returns {any}
 */
function initializeSerializers(serializerName) {
    initializeResourceSerializer()
    if (serializerName === 'Extension' && !ExtensionSerializer) {
        ExtensionSerializer = require('../complex_types/extension.js');
        return ExtensionSerializer;
    }
    if (serializerName === 'ChargeItemDefinitionApplicability' && !ChargeItemDefinitionApplicabilitySerializer) {
        ChargeItemDefinitionApplicabilitySerializer = require('../backbone_elements/chargeItemDefinitionApplicability.js');
        return ChargeItemDefinitionApplicabilitySerializer;
    }
    if (serializerName === 'ChargeItemDefinitionPriceComponent' && !ChargeItemDefinitionPriceComponentSerializer) {
        ChargeItemDefinitionPriceComponentSerializer = require('../backbone_elements/chargeItemDefinitionPriceComponent.js');
        return ChargeItemDefinitionPriceComponentSerializer;
    }
}

/** @type {import('../../../fhirResourceSerializer.js').FhirResourceSerializer} */
let FhirResourceSerializer;

function initializeResourceSerializer() {
    if (!FhirResourceSerializer) {
        FhirResourceSerializer = require('../../../fhirResourceSerializer.js').FhirResourceSerializer;
    }

    return FhirResourceSerializer;
}

class ChargeItemDefinitionPropertyGroupSerializer {
    static propertyToSerializerMap = {
        id: null,
        extension: (value, context) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer, context);
        },
        modifierExtension: (value, context) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer, context);
        },
        applicability: (value, context) => {
            initializeSerializers('ChargeItemDefinitionApplicability');
            return FhirResourceSerializer.serializeArray(value, ChargeItemDefinitionApplicabilitySerializer, context);
        },
        priceComponent: (value, context) => {
            initializeSerializers('ChargeItemDefinitionPriceComponent');
            return FhirResourceSerializer.serializeArray(value, ChargeItemDefinitionPriceComponentSerializer, context);
        }
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @param {Object} context
     * @returns {any} Cleaned object
     */
    static serialize(rawJson, context = {}) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => ChargeItemDefinitionPropertyGroupSerializer.serialize(item, context));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in ChargeItemDefinitionPropertyGroupSerializer.propertyToSerializerMap) {
                if (ChargeItemDefinitionPropertyGroupSerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = ChargeItemDefinitionPropertyGroupSerializer.propertyToSerializerMap[propertyName](value, context);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = ChargeItemDefinitionPropertyGroupSerializer;
