// This file is auto-generated by generate_classes so do not edit manually

/** @type {import('../complex_types/extension.js')} */
let ExtensionSerializer;
/** @type {import('../backbone_elements/graphDefinitionCompartment.js')} */
let GraphDefinitionCompartmentSerializer;
/** @type {import('../backbone_elements/graphDefinitionLink.js')} */
let GraphDefinitionLinkSerializer;

/**
 * Lazy loads the required serializers
 * It esnures that require is called only once for each serializer to minimize the call stack for require()
 * @returns {any}
 */
function initializeSerializers(serializerName) {
    initializeResourceSerializer()
    if (serializerName === 'Extension' && !ExtensionSerializer) {
        ExtensionSerializer = require('../complex_types/extension.js');
        return ExtensionSerializer;
    }
    if (serializerName === 'GraphDefinitionCompartment' && !GraphDefinitionCompartmentSerializer) {
        GraphDefinitionCompartmentSerializer = require('../backbone_elements/graphDefinitionCompartment.js');
        return GraphDefinitionCompartmentSerializer;
    }
    if (serializerName === 'GraphDefinitionLink' && !GraphDefinitionLinkSerializer) {
        GraphDefinitionLinkSerializer = require('../backbone_elements/graphDefinitionLink.js');
        return GraphDefinitionLinkSerializer;
    }
}

/** @type {import('../../../fhirResourceSerializer.js').FhirResourceSerializer} */
let FhirResourceSerializer;

function initializeResourceSerializer() {
    if (!FhirResourceSerializer) {
        FhirResourceSerializer = require('../../../fhirResourceSerializer.js').FhirResourceSerializer;
    }

    return FhirResourceSerializer;
}

class GraphDefinitionTargetSerializer {
    static propertyToSerializerMap = {
        id: null,
        extension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer);
        },
        modifierExtension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer);
        },
        type: null,
        params: null,
        profile: null,
        compartment: (value) => {
            initializeSerializers('GraphDefinitionCompartment');
            return FhirResourceSerializer.serializeArray(value, GraphDefinitionCompartmentSerializer);
        },
        link: (value) => {
            initializeSerializers('GraphDefinitionLink');
            return FhirResourceSerializer.serializeArray(value, GraphDefinitionLinkSerializer);
        }
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @returns {any} Cleaned object
     */
    static serialize(rawJson) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => GraphDefinitionTargetSerializer.serialize(item));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in GraphDefinitionTargetSerializer.propertyToSerializerMap) {
                if (GraphDefinitionTargetSerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = GraphDefinitionTargetSerializer.propertyToSerializerMap[propertyName](value);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = GraphDefinitionTargetSerializer;
