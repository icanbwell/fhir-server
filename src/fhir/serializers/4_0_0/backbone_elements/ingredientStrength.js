// This file is auto-generated by generate_classes so do not edit manually

/** @type {import('../complex_types/extension.js')} */
let Extension;
/** @type {import('../complex_types/ratio.js')} */
let Ratio;
/** @type {import('../complex_types/ratioRange.js')} */
let RatioRange;
/** @type {import('../complex_types/codeableConcept.js')} */
let CodeableConcept;
/** @type {import('../backbone_elements/ingredientReferenceStrength.js')} */
let IngredientReferenceStrength;

/**
 * Lazy loads the required serializers
 * It esnures that require is called only once for each serializer to minimize the call stack for require()
 * @returns {any}
 */
function initializeSerializers(serializerName) {
    initializeResourceSerializer()
    if (serializerName === 'Extension' && !Extension) {
        Extension = require('../complex_types/extension.js');
        return Extension;
    }
    if (serializerName === 'Ratio' && !Ratio) {
        Ratio = require('../complex_types/ratio.js');
        return Ratio;
    }
    if (serializerName === 'RatioRange' && !RatioRange) {
        RatioRange = require('../complex_types/ratioRange.js');
        return RatioRange;
    }
    if (serializerName === 'CodeableConcept' && !CodeableConcept) {
        CodeableConcept = require('../complex_types/codeableConcept.js');
        return CodeableConcept;
    }
    if (serializerName === 'IngredientReferenceStrength' && !IngredientReferenceStrength) {
        IngredientReferenceStrength = require('../backbone_elements/ingredientReferenceStrength.js');
        return IngredientReferenceStrength;
    }
}

/** @type {import('../../../fhirResourceSerializer.js').FhirResourceSerializer} */
let FhirResourceSerializer;

function initializeResourceSerializer() {
    if (!FhirResourceSerializer) {
        FhirResourceSerializer = require('../../../fhirResourceSerializer.js').FhirResourceSerializer;
    }

    return FhirResourceSerializer;
}

class IngredientStrengthSerializer {
    static propertyToSerializerMap = {
        id: null,
        extension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, Extension);
        },
        modifierExtension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, Extension);
        },
        presentationRatio: (value) => {
            initializeSerializers('Ratio');
            return FhirResourceSerializer.serialize(value, Ratio);
        },
        presentationRatioRange: (value) => {
            initializeSerializers('RatioRange');
            return FhirResourceSerializer.serialize(value, RatioRange);
        },
        textPresentation: null,
        concentrationRatio: (value) => {
            initializeSerializers('Ratio');
            return FhirResourceSerializer.serialize(value, Ratio);
        },
        concentrationRatioRange: (value) => {
            initializeSerializers('RatioRange');
            return FhirResourceSerializer.serialize(value, RatioRange);
        },
        textConcentration: null,
        measurementPoint: null,
        country: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serializeArray(value, CodeableConcept);
        },
        referenceStrength: (value) => {
            initializeSerializers('IngredientReferenceStrength');
            return FhirResourceSerializer.serializeArray(value, IngredientReferenceStrength);
        }
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @returns {any} Cleaned object
     */
    static serialize(rawJson) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => IngredientStrengthSerializer.serialize(item));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in IngredientStrengthSerializer.propertyToSerializerMap) {
                if (IngredientStrengthSerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = IngredientStrengthSerializer.propertyToSerializerMap[propertyName](value);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = IngredientStrengthSerializer;
