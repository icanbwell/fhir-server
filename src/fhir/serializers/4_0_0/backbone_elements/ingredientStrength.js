// This file is auto-generated by generate_classes so do not edit manually

const ExtensionSerializer = require('../complex_types/extension.js');
const RatioSerializer = require('../complex_types/ratio.js');
const RatioRangeSerializer = require('../complex_types/ratioRange.js');
const CodeableConceptSerializer = require('../complex_types/codeableConcept.js');
const IngredientReferenceStrengthSerializer = require('../backbone_elements/ingredientReferenceStrength.js');

class IngredientStrengthSerializer {
    static propertyToSerializerMap = {
        id: null,
        extension: (value) => ExtensionSerializer.serialize(value),
        modifierExtension: (value) => ExtensionSerializer.serialize(value),
        presentationRatio: (value) => RatioSerializer.serialize(value),
        presentationRatioRange: (value) => RatioRangeSerializer.serialize(value),
        textPresentation: null,
        concentrationRatio: (value) => RatioSerializer.serialize(value),
        concentrationRatioRange: (value) => RatioRangeSerializer.serialize(value),
        textConcentration: null,
        measurementPoint: null,
        country: (value) => CodeableConceptSerializer.serialize(value),
        referenceStrength: (value) => IngredientReferenceStrengthSerializer.serialize(value)
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @returns {any} Cleaned object
     */
    static serialize(rawJson) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => IngredientStrengthSerializer.serialize(item));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in IngredientStrengthSerializer.propertyToSerializerMap) {
                if (IngredientStrengthSerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = IngredientStrengthSerializer.propertyToSerializerMap[propertyName](value);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = IngredientStrengthSerializer;