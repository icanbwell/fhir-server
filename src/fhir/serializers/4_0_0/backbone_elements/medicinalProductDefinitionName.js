// This file is auto-generated by generate_classes so do not edit manually

/** @type {import('../complex_types/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complex_types/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../backbone_elements/medicinalProductDefinitionNamePart.js')} */
let MedicinalProductDefinitionNamePartSerializer;
/** @type {import('../backbone_elements/medicinalProductDefinitionCountryLanguage.js')} */
let MedicinalProductDefinitionCountryLanguageSerializer;

/**
 * Lazy loads the required serializers
 * It esnures that require is called only once for each serializer to minimize the call stack for require()
 * @returns {any}
 */
function initializeSerializers(serializerName) {
    initializeResourceSerializer()
    if (serializerName === 'Extension' && !ExtensionSerializer) {
        ExtensionSerializer = require('../complex_types/extension.js');
        return ExtensionSerializer;
    }
    if (serializerName === 'CodeableConcept' && !CodeableConceptSerializer) {
        CodeableConceptSerializer = require('../complex_types/codeableConcept.js');
        return CodeableConceptSerializer;
    }
    if (serializerName === 'MedicinalProductDefinitionNamePart' && !MedicinalProductDefinitionNamePartSerializer) {
        MedicinalProductDefinitionNamePartSerializer = require('../backbone_elements/medicinalProductDefinitionNamePart.js');
        return MedicinalProductDefinitionNamePartSerializer;
    }
    if (serializerName === 'MedicinalProductDefinitionCountryLanguage' && !MedicinalProductDefinitionCountryLanguageSerializer) {
        MedicinalProductDefinitionCountryLanguageSerializer = require('../backbone_elements/medicinalProductDefinitionCountryLanguage.js');
        return MedicinalProductDefinitionCountryLanguageSerializer;
    }
}

/** @type {import('../../../fhirResourceSerializer.js').FhirResourceSerializer} */
let FhirResourceSerializer;

function initializeResourceSerializer() {
    if (!FhirResourceSerializer) {
        FhirResourceSerializer = require('../../../fhirResourceSerializer.js').FhirResourceSerializer;
    }

    return FhirResourceSerializer;
}

class MedicinalProductDefinitionNameSerializer {
    static propertyToSerializerMap = {
        id: null,
        extension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer);
        },
        modifierExtension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer);
        },
        productName: null,
        type: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serialize(value, CodeableConceptSerializer);
        },
        namePart: (value) => {
            initializeSerializers('MedicinalProductDefinitionNamePart');
            return FhirResourceSerializer.serializeArray(value, MedicinalProductDefinitionNamePartSerializer);
        },
        countryLanguage: (value) => {
            initializeSerializers('MedicinalProductDefinitionCountryLanguage');
            return FhirResourceSerializer.serializeArray(value, MedicinalProductDefinitionCountryLanguageSerializer);
        }
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @returns {any} Cleaned object
     */
    static serialize(rawJson) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => MedicinalProductDefinitionNameSerializer.serialize(item));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in MedicinalProductDefinitionNameSerializer.propertyToSerializerMap) {
                if (MedicinalProductDefinitionNameSerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = MedicinalProductDefinitionNameSerializer.propertyToSerializerMap[propertyName](value);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = MedicinalProductDefinitionNameSerializer;
