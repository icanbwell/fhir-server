// This file is auto-generated by generate_classes so do not edit manually

/** @type {import('../complex_types/extension.js')} */
let Extension;
/** @type {import('../complex_types/coding.js')} */
let Coding;
/** @type {import('../backbone_elements/questionnaireEnableWhen.js')} */
let QuestionnaireEnableWhen;
/** @type {import('../backbone_elements/questionnaireAnswerOption.js')} */
let QuestionnaireAnswerOption;
/** @type {import('../backbone_elements/questionnaireInitial.js')} */
let QuestionnaireInitial;

/**
 * Lazy loads the required serializers
 * It esnures that require is called only once for each serializer to minimize the call stack for require()
 * @returns {any}
 */
function initializeSerializers(serializerName) {
    initializeResourceSerializer()
    if (serializerName === 'Extension' && !Extension) {
        Extension = require('../complex_types/extension.js');
        return Extension;
    }
    if (serializerName === 'Coding' && !Coding) {
        Coding = require('../complex_types/coding.js');
        return Coding;
    }
    if (serializerName === 'QuestionnaireEnableWhen' && !QuestionnaireEnableWhen) {
        QuestionnaireEnableWhen = require('../backbone_elements/questionnaireEnableWhen.js');
        return QuestionnaireEnableWhen;
    }
    if (serializerName === 'QuestionnaireAnswerOption' && !QuestionnaireAnswerOption) {
        QuestionnaireAnswerOption = require('../backbone_elements/questionnaireAnswerOption.js');
        return QuestionnaireAnswerOption;
    }
    if (serializerName === 'QuestionnaireInitial' && !QuestionnaireInitial) {
        QuestionnaireInitial = require('../backbone_elements/questionnaireInitial.js');
        return QuestionnaireInitial;
    }
}

/** @type {import('../../../fhirResourceSerializer.js').FhirResourceSerializer} */
let FhirResourceSerializer;

function initializeResourceSerializer() {
    if (!FhirResourceSerializer) {
        FhirResourceSerializer = require('../../../fhirResourceSerializer.js').FhirResourceSerializer;
    }

    return FhirResourceSerializer;
}

class QuestionnaireItemSerializer {
    static propertyToSerializerMap = {
        id: null,
        extension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, Extension);
        },
        modifierExtension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, Extension);
        },
        linkId: null,
        definition: null,
        code: (value) => {
            initializeSerializers('Coding');
            return FhirResourceSerializer.serializeArray(value, Coding);
        },
        prefix: null,
        text: null,
        type: null,
        enableWhen: (value) => {
            initializeSerializers('QuestionnaireEnableWhen');
            return FhirResourceSerializer.serializeArray(value, QuestionnaireEnableWhen);
        },
        enableBehavior: null,
        required: null,
        repeats: null,
        readOnly: null,
        maxLength: null,
        answerValueSet: null,
        answerOption: (value) => {
            initializeSerializers('QuestionnaireAnswerOption');
            return FhirResourceSerializer.serializeArray(value, QuestionnaireAnswerOption);
        },
        initial: (value) => {
            initializeSerializers('QuestionnaireInitial');
            return FhirResourceSerializer.serializeArray(value, QuestionnaireInitial);
        },
        item: (value) => {
            initializeSerializers('QuestionnaireItem');
            return FhirResourceSerializer.serializeArray(value, QuestionnaireItem);
        }
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @returns {any} Cleaned object
     */
    static serialize(rawJson) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => QuestionnaireItemSerializer.serialize(item));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in QuestionnaireItemSerializer.propertyToSerializerMap) {
                if (QuestionnaireItemSerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = QuestionnaireItemSerializer.propertyToSerializerMap[propertyName](value);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = QuestionnaireItemSerializer;
