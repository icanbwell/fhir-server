// This file is auto-generated by generate_classes so do not edit manually

/** @type {import('../complex_types/extension.js')} */
let Extension;
/** @type {import('../backbone_elements/codeSystemDesignation.js')} */
let CodeSystemDesignation;
/** @type {import('../backbone_elements/codeSystemProperty1.js')} */
let CodeSystemProperty1;

/**
 * Lazy loads the required serializers
 * It esnures that require is called only once for each serializer to minimize the call stack for require()
 * @returns {any}
 */
function initializeSerializers(serializerName) {
    initializeResourceSerializer()
    if (serializerName === 'Extension' && !Extension) {
        Extension = require('../complex_types/extension.js');
        return Extension;
    }
    if (serializerName === 'CodeSystemDesignation' && !CodeSystemDesignation) {
        CodeSystemDesignation = require('../backbone_elements/codeSystemDesignation.js');
        return CodeSystemDesignation;
    }
    if (serializerName === 'CodeSystemProperty1' && !CodeSystemProperty1) {
        CodeSystemProperty1 = require('../backbone_elements/codeSystemProperty1.js');
        return CodeSystemProperty1;
    }
}

/** @type {import('../../../fhirResourceSerializer.js').FhirResourceSerializer} */
let FhirResourceSerializer;

function initializeResourceSerializer() {
    if (!FhirResourceSerializer) {
        FhirResourceSerializer = require('../../../fhirResourceSerializer.js').FhirResourceSerializer;
    }

    return FhirResourceSerializer;
}

class CodeSystemConceptSerializer {
    static propertyToSerializerMap = {
        id: null,
        extension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, Extension);
        },
        modifierExtension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, Extension);
        },
        code: null,
        display: null,
        definition: null,
        designation: (value) => {
            initializeSerializers('CodeSystemDesignation');
            return FhirResourceSerializer.serializeArray(value, CodeSystemDesignation);
        },
        property: (value) => {
            initializeSerializers('CodeSystemProperty1');
            return FhirResourceSerializer.serializeArray(value, CodeSystemProperty1);
        },
        concept: (value) => {
            initializeSerializers('CodeSystemConcept');
            return FhirResourceSerializer.serializeArray(value, CodeSystemConcept);
        }
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @returns {any} Cleaned object
     */
    static serialize(rawJson) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => CodeSystemConceptSerializer.serialize(item));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in CodeSystemConceptSerializer.propertyToSerializerMap) {
                if (CodeSystemConceptSerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = CodeSystemConceptSerializer.propertyToSerializerMap[propertyName](value);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = CodeSystemConceptSerializer;
