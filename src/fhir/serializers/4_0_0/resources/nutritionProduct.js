// This file is auto-generated by generate_classes so do not edit manually

/** @type {import('../complex_types/meta.js')} */
let MetaSerializer;
/** @type {import('../complex_types/narrative.js')} */
let NarrativeSerializer;
/** @type {import('../simple_types/resourceContainer.js')} */
let ResourceContainerSerializer;
/** @type {import('../complex_types/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complex_types/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../complex_types/reference.js')} */
let ReferenceSerializer;
/** @type {import('../backbone_elements/nutritionProductNutrient.js')} */
let NutritionProductNutrientSerializer;
/** @type {import('../backbone_elements/nutritionProductIngredient.js')} */
let NutritionProductIngredientSerializer;
/** @type {import('../complex_types/codeableReference.js')} */
let CodeableReferenceSerializer;
/** @type {import('../backbone_elements/nutritionProductProductCharacteristic.js')} */
let NutritionProductProductCharacteristicSerializer;
/** @type {import('../backbone_elements/nutritionProductInstance.js')} */
let NutritionProductInstanceSerializer;
/** @type {import('../complex_types/annotation.js')} */
let AnnotationSerializer;

/**
 * Lazy loads the required serializers
 * It esnures that require is called only once for each serializer to minimize the call stack for require()
 * @returns {any}
 */
function initializeSerializers(serializerName) {
    initializeResourceSerializer()
    if (serializerName === 'Meta' && !MetaSerializer) {
        MetaSerializer = require('../complex_types/meta.js');
        return MetaSerializer;
    }
    if (serializerName === 'Narrative' && !NarrativeSerializer) {
        NarrativeSerializer = require('../complex_types/narrative.js');
        return NarrativeSerializer;
    }
    if (serializerName === 'ResourceContainer' && !ResourceContainerSerializer) {
        ResourceContainerSerializer = require('../simple_types/resourceContainer.js');
        return ResourceContainerSerializer;
    }
    if (serializerName === 'Extension' && !ExtensionSerializer) {
        ExtensionSerializer = require('../complex_types/extension.js');
        return ExtensionSerializer;
    }
    if (serializerName === 'CodeableConcept' && !CodeableConceptSerializer) {
        CodeableConceptSerializer = require('../complex_types/codeableConcept.js');
        return CodeableConceptSerializer;
    }
    if (serializerName === 'Reference' && !ReferenceSerializer) {
        ReferenceSerializer = require('../complex_types/reference.js');
        return ReferenceSerializer;
    }
    if (serializerName === 'NutritionProductNutrient' && !NutritionProductNutrientSerializer) {
        NutritionProductNutrientSerializer = require('../backbone_elements/nutritionProductNutrient.js');
        return NutritionProductNutrientSerializer;
    }
    if (serializerName === 'NutritionProductIngredient' && !NutritionProductIngredientSerializer) {
        NutritionProductIngredientSerializer = require('../backbone_elements/nutritionProductIngredient.js');
        return NutritionProductIngredientSerializer;
    }
    if (serializerName === 'CodeableReference' && !CodeableReferenceSerializer) {
        CodeableReferenceSerializer = require('../complex_types/codeableReference.js');
        return CodeableReferenceSerializer;
    }
    if (serializerName === 'NutritionProductProductCharacteristic' && !NutritionProductProductCharacteristicSerializer) {
        NutritionProductProductCharacteristicSerializer = require('../backbone_elements/nutritionProductProductCharacteristic.js');
        return NutritionProductProductCharacteristicSerializer;
    }
    if (serializerName === 'NutritionProductInstance' && !NutritionProductInstanceSerializer) {
        NutritionProductInstanceSerializer = require('../backbone_elements/nutritionProductInstance.js');
        return NutritionProductInstanceSerializer;
    }
    if (serializerName === 'Annotation' && !AnnotationSerializer) {
        AnnotationSerializer = require('../complex_types/annotation.js');
        return AnnotationSerializer;
    }
}

/** @type {import('../../../fhirResourceSerializer.js').FhirResourceSerializer} */
let FhirResourceSerializer;

function initializeResourceSerializer() {
    if (!FhirResourceSerializer) {
        FhirResourceSerializer = require('../../../fhirResourceSerializer.js').FhirResourceSerializer;
    }

    return FhirResourceSerializer;
}

class NutritionProductSerializer {
    static propertyToSerializerMap = {
        id: null,
        meta: (value, context) => {
            initializeSerializers('Meta');
            return FhirResourceSerializer.serialize(value, MetaSerializer, context);
        },
        implicitRules: null,
        language: null,
        text: (value, context) => {
            initializeSerializers('Narrative');
            return FhirResourceSerializer.serialize(value, NarrativeSerializer, context);
        },
        contained: (value, context) => {
            initializeSerializers('ResourceContainer');
            return FhirResourceSerializer.serializeArray(value, undefined, context);
        },
        extension: (value, context) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer, context);
        },
        modifierExtension: (value, context) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer, context);
        },
        status: null,
        category: (value, context) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serializeArray(value, CodeableConceptSerializer, context);
        },
        code: (value, context) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serialize(value, CodeableConceptSerializer, context);
        },
        manufacturer: (value, context) => {
            initializeSerializers('Reference');
            return FhirResourceSerializer.serializeArray(value, ReferenceSerializer, context);
        },
        nutrient: (value, context) => {
            initializeSerializers('NutritionProductNutrient');
            return FhirResourceSerializer.serializeArray(value, NutritionProductNutrientSerializer, context);
        },
        ingredient: (value, context) => {
            initializeSerializers('NutritionProductIngredient');
            return FhirResourceSerializer.serializeArray(value, NutritionProductIngredientSerializer, context);
        },
        knownAllergen: (value, context) => {
            initializeSerializers('CodeableReference');
            return FhirResourceSerializer.serializeArray(value, CodeableReferenceSerializer, context);
        },
        productCharacteristic: (value, context) => {
            initializeSerializers('NutritionProductProductCharacteristic');
            return FhirResourceSerializer.serializeArray(value, NutritionProductProductCharacteristicSerializer, context);
        },
        instance: (value, context) => {
            initializeSerializers('NutritionProductInstance');
            return FhirResourceSerializer.serialize(value, NutritionProductInstanceSerializer, context);
        },
        note: (value, context) => {
            initializeSerializers('Annotation');
            return FhirResourceSerializer.serializeArray(value, AnnotationSerializer, context);
        },
        resourceType: null
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @param {Object} context
     * @returns {any} Cleaned object
     */
    static serialize(rawJson, context = {}) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => NutritionProductSerializer.serialize(item, context));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in NutritionProductSerializer.propertyToSerializerMap) {
                if (NutritionProductSerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = NutritionProductSerializer.propertyToSerializerMap[propertyName](value, context);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = NutritionProductSerializer;
