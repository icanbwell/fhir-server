// This file is auto-generated by generate_classes so do not edit manually

/** @type {import('../complex_types/meta.js')} */
let Meta;
/** @type {import('../complex_types/narrative.js')} */
let Narrative;
/** @type {import('../simple_types/resourceContainer.js')} */
let ResourceContainer;
/** @type {import('../complex_types/extension.js')} */
let Extension;
/** @type {import('../complex_types/codeableConcept.js')} */
let CodeableConcept;
/** @type {import('../complex_types/reference.js')} */
let Reference;
/** @type {import('../backbone_elements/nutritionProductNutrient.js')} */
let NutritionProductNutrient;
/** @type {import('../backbone_elements/nutritionProductIngredient.js')} */
let NutritionProductIngredient;
/** @type {import('../complex_types/codeableReference.js')} */
let CodeableReference;
/** @type {import('../backbone_elements/nutritionProductProductCharacteristic.js')} */
let NutritionProductProductCharacteristic;
/** @type {import('../backbone_elements/nutritionProductInstance.js')} */
let NutritionProductInstance;
/** @type {import('../complex_types/annotation.js')} */
let Annotation;

/**
 * Lazy loads the required serializers
 * It esnures that require is called only once for each serializer to minimize the call stack for require()
 * @returns {any}
 */
function initializeSerializers(serializerName) {
    initializeResourceSerializer()
    if (serializerName === 'Meta' && !Meta) {
        Meta = require('../complex_types/meta.js');
        return Meta;
    }
    if (serializerName === 'Narrative' && !Narrative) {
        Narrative = require('../complex_types/narrative.js');
        return Narrative;
    }
    if (serializerName === 'ResourceContainer' && !ResourceContainer) {
        ResourceContainer = require('../simple_types/resourceContainer.js');
        return ResourceContainer;
    }
    if (serializerName === 'Extension' && !Extension) {
        Extension = require('../complex_types/extension.js');
        return Extension;
    }
    if (serializerName === 'CodeableConcept' && !CodeableConcept) {
        CodeableConcept = require('../complex_types/codeableConcept.js');
        return CodeableConcept;
    }
    if (serializerName === 'Reference' && !Reference) {
        Reference = require('../complex_types/reference.js');
        return Reference;
    }
    if (serializerName === 'NutritionProductNutrient' && !NutritionProductNutrient) {
        NutritionProductNutrient = require('../backbone_elements/nutritionProductNutrient.js');
        return NutritionProductNutrient;
    }
    if (serializerName === 'NutritionProductIngredient' && !NutritionProductIngredient) {
        NutritionProductIngredient = require('../backbone_elements/nutritionProductIngredient.js');
        return NutritionProductIngredient;
    }
    if (serializerName === 'CodeableReference' && !CodeableReference) {
        CodeableReference = require('../complex_types/codeableReference.js');
        return CodeableReference;
    }
    if (serializerName === 'NutritionProductProductCharacteristic' && !NutritionProductProductCharacteristic) {
        NutritionProductProductCharacteristic = require('../backbone_elements/nutritionProductProductCharacteristic.js');
        return NutritionProductProductCharacteristic;
    }
    if (serializerName === 'NutritionProductInstance' && !NutritionProductInstance) {
        NutritionProductInstance = require('../backbone_elements/nutritionProductInstance.js');
        return NutritionProductInstance;
    }
    if (serializerName === 'Annotation' && !Annotation) {
        Annotation = require('../complex_types/annotation.js');
        return Annotation;
    }
}

/** @type {import('../../../fhirResourceSerializer.js').FhirResourceSerializer} */
let FhirResourceSerializer;

function initializeResourceSerializer() {
    if (!FhirResourceSerializer) {
        FhirResourceSerializer = require('../../../fhirResourceSerializer.js').FhirResourceSerializer;
    }

    return FhirResourceSerializer;
}

class NutritionProductSerializer {
    static propertyToSerializerMap = {
        id: null,
        meta: (value) => {
            initializeSerializers('Meta');
            return FhirResourceSerializer.serialize(value, Meta);
        },
        implicitRules: null,
        language: null,
        text: (value) => {
            initializeSerializers('Narrative');
            return FhirResourceSerializer.serialize(value, Narrative);
        },
        contained: (value) => {
            initializeSerializers('ResourceContainer');
            return FhirResourceSerializer.serializeArray(value);
        },
        extension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, Extension);
        },
        modifierExtension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, Extension);
        },
        status: null,
        category: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serializeArray(value, CodeableConcept);
        },
        code: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serialize(value, CodeableConcept);
        },
        manufacturer: (value) => {
            initializeSerializers('Reference');
            return FhirResourceSerializer.serializeArray(value, Reference);
        },
        nutrient: (value) => {
            initializeSerializers('NutritionProductNutrient');
            return FhirResourceSerializer.serializeArray(value, NutritionProductNutrient);
        },
        ingredient: (value) => {
            initializeSerializers('NutritionProductIngredient');
            return FhirResourceSerializer.serializeArray(value, NutritionProductIngredient);
        },
        knownAllergen: (value) => {
            initializeSerializers('CodeableReference');
            return FhirResourceSerializer.serializeArray(value, CodeableReference);
        },
        productCharacteristic: (value) => {
            initializeSerializers('NutritionProductProductCharacteristic');
            return FhirResourceSerializer.serializeArray(value, NutritionProductProductCharacteristic);
        },
        instance: (value) => {
            initializeSerializers('NutritionProductInstance');
            return FhirResourceSerializer.serialize(value, NutritionProductInstance);
        },
        note: (value) => {
            initializeSerializers('Annotation');
            return FhirResourceSerializer.serializeArray(value, Annotation);
        },
        resourceType: null
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @returns {any} Cleaned object
     */
    static serialize(rawJson) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => NutritionProductSerializer.serialize(item));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in NutritionProductSerializer.propertyToSerializerMap) {
                if (NutritionProductSerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = NutritionProductSerializer.propertyToSerializerMap[propertyName](value);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = NutritionProductSerializer;
