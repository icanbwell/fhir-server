// This file is auto-generated by generate_classes so do not edit manually

const MetaSerializer = require('../complex_types/meta.js');
const NarrativeSerializer = require('../complex_types/narrative.js');
const ResourceContainerSerializer = require('../simple_types/resourceContainer.js');
const ExtensionSerializer = require('../complex_types/extension.js');
const IdentifierSerializer = require('../complex_types/identifier.js');
const CodeableConceptSerializer = require('../complex_types/codeableConcept.js');
const ReferenceSerializer = require('../complex_types/reference.js');
const PeriodSerializer = require('../complex_types/period.js');
const QuantitySerializer = require('../complex_types/quantity.js');
const RangeSerializer = require('../complex_types/range.js');
const AnnotationSerializer = require('../complex_types/annotation.js');
const FamilyMemberHistoryConditionSerializer = require('../backbone_elements/familyMemberHistoryCondition.js');

class FamilyMemberHistorySerializer {
    static propertyToSerializerMap = {
        id: null,
        meta: (value) => MetaSerializer.serialize(value),
        implicitRules: null,
        language: null,
        text: (value) => NarrativeSerializer.serialize(value),
        contained: (value) => ResourceContainerSerializer.serialize(value),
        extension: (value) => ExtensionSerializer.serialize(value),
        modifierExtension: (value) => ExtensionSerializer.serialize(value),
        identifier: (value) => IdentifierSerializer.serialize(value),
        instantiatesCanonical: null,
        instantiatesUri: null,
        status: null,
        dataAbsentReason: (value) => CodeableConceptSerializer.serialize(value),
        patient: (value) => ReferenceSerializer.serialize(value),
        date: null,
        name: null,
        relationship: (value) => CodeableConceptSerializer.serialize(value),
        sex: (value) => CodeableConceptSerializer.serialize(value),
        bornPeriod: (value) => PeriodSerializer.serialize(value),
        bornDate: null,
        bornString: null,
        ageAge: (value) => QuantitySerializer.serialize(value),
        ageRange: (value) => RangeSerializer.serialize(value),
        ageString: null,
        estimatedAge: null,
        deceasedBoolean: null,
        deceasedAge: (value) => QuantitySerializer.serialize(value),
        deceasedRange: (value) => RangeSerializer.serialize(value),
        deceasedDate: null,
        deceasedString: null,
        reasonCode: (value) => CodeableConceptSerializer.serialize(value),
        reasonReference: (value) => ReferenceSerializer.serialize(value),
        note: (value) => AnnotationSerializer.serialize(value),
        condition: (value) => FamilyMemberHistoryConditionSerializer.serialize(value),
        resourceType: null
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @returns {any} Cleaned object
     */
    static serialize(rawJson) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => FamilyMemberHistorySerializer.serialize(item));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in FamilyMemberHistorySerializer.propertyToSerializerMap) {
                if (FamilyMemberHistorySerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = FamilyMemberHistorySerializer.propertyToSerializerMap[propertyName](value);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = FamilyMemberHistorySerializer;