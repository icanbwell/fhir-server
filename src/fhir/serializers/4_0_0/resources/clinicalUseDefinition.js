// This file is auto-generated by generate_classes so do not edit manually

/** @type {import('../complex_types/meta.js')} */
let MetaSerializer;
/** @type {import('../complex_types/narrative.js')} */
let NarrativeSerializer;
/** @type {import('../simple_types/resourceContainer.js')} */
let ResourceContainerSerializer;
/** @type {import('../complex_types/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complex_types/identifier.js')} */
let IdentifierSerializer;
/** @type {import('../complex_types/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../complex_types/reference.js')} */
let ReferenceSerializer;
/** @type {import('../backbone_elements/clinicalUseDefinitionContraindication.js')} */
let ClinicalUseDefinitionContraindicationSerializer;
/** @type {import('../backbone_elements/clinicalUseDefinitionIndication.js')} */
let ClinicalUseDefinitionIndicationSerializer;
/** @type {import('../backbone_elements/clinicalUseDefinitionInteraction.js')} */
let ClinicalUseDefinitionInteractionSerializer;
/** @type {import('../backbone_elements/clinicalUseDefinitionUndesirableEffect.js')} */
let ClinicalUseDefinitionUndesirableEffectSerializer;
/** @type {import('../backbone_elements/clinicalUseDefinitionWarning.js')} */
let ClinicalUseDefinitionWarningSerializer;

/**
 * Lazy loads the required serializers
 * It esnures that require is called only once for each serializer to minimize the call stack for require()
 * @returns {any}
 */
function initializeSerializers(serializerName) {
    initializeResourceSerializer()
    if (serializerName === 'Meta' && !MetaSerializer) {
        MetaSerializer = require('../complex_types/meta.js');
        return MetaSerializer;
    }
    if (serializerName === 'Narrative' && !NarrativeSerializer) {
        NarrativeSerializer = require('../complex_types/narrative.js');
        return NarrativeSerializer;
    }
    if (serializerName === 'ResourceContainer' && !ResourceContainerSerializer) {
        ResourceContainerSerializer = require('../simple_types/resourceContainer.js');
        return ResourceContainerSerializer;
    }
    if (serializerName === 'Extension' && !ExtensionSerializer) {
        ExtensionSerializer = require('../complex_types/extension.js');
        return ExtensionSerializer;
    }
    if (serializerName === 'Identifier' && !IdentifierSerializer) {
        IdentifierSerializer = require('../complex_types/identifier.js');
        return IdentifierSerializer;
    }
    if (serializerName === 'CodeableConcept' && !CodeableConceptSerializer) {
        CodeableConceptSerializer = require('../complex_types/codeableConcept.js');
        return CodeableConceptSerializer;
    }
    if (serializerName === 'Reference' && !ReferenceSerializer) {
        ReferenceSerializer = require('../complex_types/reference.js');
        return ReferenceSerializer;
    }
    if (serializerName === 'ClinicalUseDefinitionContraindication' && !ClinicalUseDefinitionContraindicationSerializer) {
        ClinicalUseDefinitionContraindicationSerializer = require('../backbone_elements/clinicalUseDefinitionContraindication.js');
        return ClinicalUseDefinitionContraindicationSerializer;
    }
    if (serializerName === 'ClinicalUseDefinitionIndication' && !ClinicalUseDefinitionIndicationSerializer) {
        ClinicalUseDefinitionIndicationSerializer = require('../backbone_elements/clinicalUseDefinitionIndication.js');
        return ClinicalUseDefinitionIndicationSerializer;
    }
    if (serializerName === 'ClinicalUseDefinitionInteraction' && !ClinicalUseDefinitionInteractionSerializer) {
        ClinicalUseDefinitionInteractionSerializer = require('../backbone_elements/clinicalUseDefinitionInteraction.js');
        return ClinicalUseDefinitionInteractionSerializer;
    }
    if (serializerName === 'ClinicalUseDefinitionUndesirableEffect' && !ClinicalUseDefinitionUndesirableEffectSerializer) {
        ClinicalUseDefinitionUndesirableEffectSerializer = require('../backbone_elements/clinicalUseDefinitionUndesirableEffect.js');
        return ClinicalUseDefinitionUndesirableEffectSerializer;
    }
    if (serializerName === 'ClinicalUseDefinitionWarning' && !ClinicalUseDefinitionWarningSerializer) {
        ClinicalUseDefinitionWarningSerializer = require('../backbone_elements/clinicalUseDefinitionWarning.js');
        return ClinicalUseDefinitionWarningSerializer;
    }
}

/** @type {import('../../../fhirResourceSerializer.js').FhirResourceSerializer} */
let FhirResourceSerializer;

function initializeResourceSerializer() {
    if (!FhirResourceSerializer) {
        FhirResourceSerializer = require('../../../fhirResourceSerializer.js').FhirResourceSerializer;
    }

    return FhirResourceSerializer;
}

class ClinicalUseDefinitionSerializer {
    static propertyToSerializerMap = {
        id: null,
        meta: (value) => {
            initializeSerializers('Meta');
            return FhirResourceSerializer.serialize(value, MetaSerializer);
        },
        implicitRules: null,
        language: null,
        text: (value) => {
            initializeSerializers('Narrative');
            return FhirResourceSerializer.serialize(value, NarrativeSerializer);
        },
        contained: (value) => {
            initializeSerializers('ResourceContainer');
            return FhirResourceSerializer.serializeArray(value);
        },
        extension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer);
        },
        modifierExtension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer);
        },
        identifier: (value) => {
            initializeSerializers('Identifier');
            return FhirResourceSerializer.serializeArray(value, IdentifierSerializer);
        },
        type: null,
        category: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serializeArray(value, CodeableConceptSerializer);
        },
        subject: (value) => {
            initializeSerializers('Reference');
            return FhirResourceSerializer.serializeArray(value, ReferenceSerializer);
        },
        status: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serialize(value, CodeableConceptSerializer);
        },
        contraindication: (value) => {
            initializeSerializers('ClinicalUseDefinitionContraindication');
            return FhirResourceSerializer.serialize(value, ClinicalUseDefinitionContraindicationSerializer);
        },
        indication: (value) => {
            initializeSerializers('ClinicalUseDefinitionIndication');
            return FhirResourceSerializer.serialize(value, ClinicalUseDefinitionIndicationSerializer);
        },
        interaction: (value) => {
            initializeSerializers('ClinicalUseDefinitionInteraction');
            return FhirResourceSerializer.serialize(value, ClinicalUseDefinitionInteractionSerializer);
        },
        population: (value) => {
            initializeSerializers('Reference');
            return FhirResourceSerializer.serializeArray(value, ReferenceSerializer);
        },
        undesirableEffect: (value) => {
            initializeSerializers('ClinicalUseDefinitionUndesirableEffect');
            return FhirResourceSerializer.serialize(value, ClinicalUseDefinitionUndesirableEffectSerializer);
        },
        warning: (value) => {
            initializeSerializers('ClinicalUseDefinitionWarning');
            return FhirResourceSerializer.serialize(value, ClinicalUseDefinitionWarningSerializer);
        },
        resourceType: null
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @returns {any} Cleaned object
     */
    static serialize(rawJson) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => ClinicalUseDefinitionSerializer.serialize(item));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in ClinicalUseDefinitionSerializer.propertyToSerializerMap) {
                if (ClinicalUseDefinitionSerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = ClinicalUseDefinitionSerializer.propertyToSerializerMap[propertyName](value);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = ClinicalUseDefinitionSerializer;
