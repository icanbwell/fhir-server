// This file is auto-generated by generate_classes so do not edit manually

/** @type {import('../complex_types/meta.js')} */
let Meta;
/** @type {import('../complex_types/narrative.js')} */
let Narrative;
/** @type {import('../simple_types/resourceContainer.js')} */
let ResourceContainer;
/** @type {import('../complex_types/extension.js')} */
let Extension;
/** @type {import('../complex_types/identifier.js')} */
let Identifier;
/** @type {import('../complex_types/contactDetail.js')} */
let ContactDetail;
/** @type {import('../complex_types/usageContext.js')} */
let UsageContext;
/** @type {import('../complex_types/codeableConcept.js')} */
let CodeableConcept;
/** @type {import('../complex_types/coding.js')} */
let Coding;
/** @type {import('../backbone_elements/structureDefinitionMapping.js')} */
let StructureDefinitionMapping;
/** @type {import('../backbone_elements/structureDefinitionContext.js')} */
let StructureDefinitionContext;
/** @type {import('../backbone_elements/structureDefinitionSnapshot.js')} */
let StructureDefinitionSnapshot;
/** @type {import('../backbone_elements/structureDefinitionDifferential.js')} */
let StructureDefinitionDifferential;

/**
 * Lazy loads the required serializers
 * It esnures that require is called only once for each serializer to minimize the call stack for require()
 * @returns {any}
 */
function initializeSerializers(serializerName) {
    initializeResourceSerializer()
    if (serializerName === 'Meta' && !Meta) {
        Meta = require('../complex_types/meta.js');
        return Meta;
    }
    if (serializerName === 'Narrative' && !Narrative) {
        Narrative = require('../complex_types/narrative.js');
        return Narrative;
    }
    if (serializerName === 'ResourceContainer' && !ResourceContainer) {
        ResourceContainer = require('../simple_types/resourceContainer.js');
        return ResourceContainer;
    }
    if (serializerName === 'Extension' && !Extension) {
        Extension = require('../complex_types/extension.js');
        return Extension;
    }
    if (serializerName === 'Identifier' && !Identifier) {
        Identifier = require('../complex_types/identifier.js');
        return Identifier;
    }
    if (serializerName === 'ContactDetail' && !ContactDetail) {
        ContactDetail = require('../complex_types/contactDetail.js');
        return ContactDetail;
    }
    if (serializerName === 'UsageContext' && !UsageContext) {
        UsageContext = require('../complex_types/usageContext.js');
        return UsageContext;
    }
    if (serializerName === 'CodeableConcept' && !CodeableConcept) {
        CodeableConcept = require('../complex_types/codeableConcept.js');
        return CodeableConcept;
    }
    if (serializerName === 'Coding' && !Coding) {
        Coding = require('../complex_types/coding.js');
        return Coding;
    }
    if (serializerName === 'StructureDefinitionMapping' && !StructureDefinitionMapping) {
        StructureDefinitionMapping = require('../backbone_elements/structureDefinitionMapping.js');
        return StructureDefinitionMapping;
    }
    if (serializerName === 'StructureDefinitionContext' && !StructureDefinitionContext) {
        StructureDefinitionContext = require('../backbone_elements/structureDefinitionContext.js');
        return StructureDefinitionContext;
    }
    if (serializerName === 'StructureDefinitionSnapshot' && !StructureDefinitionSnapshot) {
        StructureDefinitionSnapshot = require('../backbone_elements/structureDefinitionSnapshot.js');
        return StructureDefinitionSnapshot;
    }
    if (serializerName === 'StructureDefinitionDifferential' && !StructureDefinitionDifferential) {
        StructureDefinitionDifferential = require('../backbone_elements/structureDefinitionDifferential.js');
        return StructureDefinitionDifferential;
    }
}

/** @type {import('../../../fhirResourceSerializer.js').FhirResourceSerializer} */
let FhirResourceSerializer;

function initializeResourceSerializer() {
    if (!FhirResourceSerializer) {
        FhirResourceSerializer = require('../../../fhirResourceSerializer.js').FhirResourceSerializer;
    }

    return FhirResourceSerializer;
}

class StructureDefinitionSerializer {
    static propertyToSerializerMap = {
        id: null,
        meta: (value) => {
            initializeSerializers('Meta');
            return FhirResourceSerializer.serialize(value, Meta);
        },
        implicitRules: null,
        language: null,
        text: (value) => {
            initializeSerializers('Narrative');
            return FhirResourceSerializer.serialize(value, Narrative);
        },
        contained: (value) => {
            initializeSerializers('ResourceContainer');
            return FhirResourceSerializer.serializeArray(value);
        },
        extension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, Extension);
        },
        modifierExtension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, Extension);
        },
        url: null,
        identifier: (value) => {
            initializeSerializers('Identifier');
            return FhirResourceSerializer.serializeArray(value, Identifier);
        },
        version: null,
        name: null,
        title: null,
        status: null,
        experimental: null,
        date: null,
        publisher: null,
        contact: (value) => {
            initializeSerializers('ContactDetail');
            return FhirResourceSerializer.serializeArray(value, ContactDetail);
        },
        description: null,
        useContext: (value) => {
            initializeSerializers('UsageContext');
            return FhirResourceSerializer.serializeArray(value, UsageContext);
        },
        jurisdiction: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serializeArray(value, CodeableConcept);
        },
        purpose: null,
        copyright: null,
        keyword: (value) => {
            initializeSerializers('Coding');
            return FhirResourceSerializer.serializeArray(value, Coding);
        },
        fhirVersion: null,
        mapping: (value) => {
            initializeSerializers('StructureDefinitionMapping');
            return FhirResourceSerializer.serializeArray(value, StructureDefinitionMapping);
        },
        kind: null,
        abstract: null,
        context: (value) => {
            initializeSerializers('StructureDefinitionContext');
            return FhirResourceSerializer.serializeArray(value, StructureDefinitionContext);
        },
        contextInvariant: null,
        type: null,
        baseDefinition: null,
        derivation: null,
        snapshot: (value) => {
            initializeSerializers('StructureDefinitionSnapshot');
            return FhirResourceSerializer.serialize(value, StructureDefinitionSnapshot);
        },
        differential: (value) => {
            initializeSerializers('StructureDefinitionDifferential');
            return FhirResourceSerializer.serialize(value, StructureDefinitionDifferential);
        },
        resourceType: null
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @returns {any} Cleaned object
     */
    static serialize(rawJson) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => StructureDefinitionSerializer.serialize(item));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in StructureDefinitionSerializer.propertyToSerializerMap) {
                if (StructureDefinitionSerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = StructureDefinitionSerializer.propertyToSerializerMap[propertyName](value);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = StructureDefinitionSerializer;
