// This file is auto-generated by generate_classes so do not edit manually

const MetaSerializer = require('../complex_types/meta.js');
const IdentifierSerializer = require('../complex_types/identifier.js');
const BundleLinkSerializer = require('../backbone_elements/bundleLink.js');
const BundleEntrySerializer = require('../backbone_elements/bundleEntry.js');
const SignatureSerializer = require('../complex_types/signature.js');

class BundleSerializer {
    static propertyToSerializerMap = {
        id: null,
        meta: (value) => MetaSerializer.serialize(value),
        implicitRules: null,
        language: null,
        identifier: (value) => IdentifierSerializer.serialize(value),
        type: null,
        timestamp: null,
        total: null,
        link: (value) => BundleLinkSerializer.serialize(value),
        entry: (value) => BundleEntrySerializer.serialize(value),
        signature: (value) => SignatureSerializer.serialize(value),
        resourceType: null
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @returns {any} Cleaned object
     */
    static serialize(rawJson) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => BundleSerializer.serialize(item));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in BundleSerializer.propertyToSerializerMap) {
                if (BundleSerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = BundleSerializer.propertyToSerializerMap[propertyName](value);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = BundleSerializer;