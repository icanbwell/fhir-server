// This file is auto-generated by generate_classes so do not edit manually

/** @type {import('../complex_types/meta.js')} */
let Meta;
/** @type {import('../complex_types/narrative.js')} */
let Narrative;
/** @type {import('../simple_types/resourceContainer.js')} */
let ResourceContainer;
/** @type {import('../complex_types/extension.js')} */
let Extension;
/** @type {import('../complex_types/identifier.js')} */
let Identifier;
/** @type {import('../complex_types/reference.js')} */
let Reference;
/** @type {import('../complex_types/quantity.js')} */
let Quantity;
/** @type {import('../backbone_elements/molecularSequenceReferenceSeq.js')} */
let MolecularSequenceReferenceSeq;
/** @type {import('../backbone_elements/molecularSequenceVariant.js')} */
let MolecularSequenceVariant;
/** @type {import('../backbone_elements/molecularSequenceQuality.js')} */
let MolecularSequenceQuality;
/** @type {import('../backbone_elements/molecularSequenceRepository.js')} */
let MolecularSequenceRepository;
/** @type {import('../backbone_elements/molecularSequenceStructureVariant.js')} */
let MolecularSequenceStructureVariant;

/**
 * Lazy loads the required serializers
 * It esnures that require is called only once for each serializer to minimize the call stack for require()
 * @returns {any}
 */
function initializeSerializers(serializerName) {
    initializeResourceSerializer()
    if (serializerName === 'Meta' && !Meta) {
        Meta = require('../complex_types/meta.js');
        return Meta;
    }
    if (serializerName === 'Narrative' && !Narrative) {
        Narrative = require('../complex_types/narrative.js');
        return Narrative;
    }
    if (serializerName === 'ResourceContainer' && !ResourceContainer) {
        ResourceContainer = require('../simple_types/resourceContainer.js');
        return ResourceContainer;
    }
    if (serializerName === 'Extension' && !Extension) {
        Extension = require('../complex_types/extension.js');
        return Extension;
    }
    if (serializerName === 'Identifier' && !Identifier) {
        Identifier = require('../complex_types/identifier.js');
        return Identifier;
    }
    if (serializerName === 'Reference' && !Reference) {
        Reference = require('../complex_types/reference.js');
        return Reference;
    }
    if (serializerName === 'Quantity' && !Quantity) {
        Quantity = require('../complex_types/quantity.js');
        return Quantity;
    }
    if (serializerName === 'MolecularSequenceReferenceSeq' && !MolecularSequenceReferenceSeq) {
        MolecularSequenceReferenceSeq = require('../backbone_elements/molecularSequenceReferenceSeq.js');
        return MolecularSequenceReferenceSeq;
    }
    if (serializerName === 'MolecularSequenceVariant' && !MolecularSequenceVariant) {
        MolecularSequenceVariant = require('../backbone_elements/molecularSequenceVariant.js');
        return MolecularSequenceVariant;
    }
    if (serializerName === 'MolecularSequenceQuality' && !MolecularSequenceQuality) {
        MolecularSequenceQuality = require('../backbone_elements/molecularSequenceQuality.js');
        return MolecularSequenceQuality;
    }
    if (serializerName === 'MolecularSequenceRepository' && !MolecularSequenceRepository) {
        MolecularSequenceRepository = require('../backbone_elements/molecularSequenceRepository.js');
        return MolecularSequenceRepository;
    }
    if (serializerName === 'MolecularSequenceStructureVariant' && !MolecularSequenceStructureVariant) {
        MolecularSequenceStructureVariant = require('../backbone_elements/molecularSequenceStructureVariant.js');
        return MolecularSequenceStructureVariant;
    }
}

/** @type {import('../../../fhirResourceSerializer.js').FhirResourceSerializer} */
let FhirResourceSerializer;

function initializeResourceSerializer() {
    if (!FhirResourceSerializer) {
        FhirResourceSerializer = require('../../../fhirResourceSerializer.js').FhirResourceSerializer;
    }

    return FhirResourceSerializer;
}

class MolecularSequenceSerializer {
    static propertyToSerializerMap = {
        id: null,
        meta: (value) => {
            initializeSerializers('Meta');
            return FhirResourceSerializer.serialize(value, Meta);
        },
        implicitRules: null,
        language: null,
        text: (value) => {
            initializeSerializers('Narrative');
            return FhirResourceSerializer.serialize(value, Narrative);
        },
        contained: (value) => {
            initializeSerializers('ResourceContainer');
            return FhirResourceSerializer.serializeArray(value);
        },
        extension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, Extension);
        },
        modifierExtension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, Extension);
        },
        identifier: (value) => {
            initializeSerializers('Identifier');
            return FhirResourceSerializer.serializeArray(value, Identifier);
        },
        type: null,
        coordinateSystem: null,
        patient: (value) => {
            initializeSerializers('Reference');
            return FhirResourceSerializer.serialize(value, Reference);
        },
        specimen: (value) => {
            initializeSerializers('Reference');
            return FhirResourceSerializer.serialize(value, Reference);
        },
        device: (value) => {
            initializeSerializers('Reference');
            return FhirResourceSerializer.serialize(value, Reference);
        },
        performer: (value) => {
            initializeSerializers('Reference');
            return FhirResourceSerializer.serialize(value, Reference);
        },
        quantity: (value) => {
            initializeSerializers('Quantity');
            return FhirResourceSerializer.serialize(value, Quantity);
        },
        referenceSeq: (value) => {
            initializeSerializers('MolecularSequenceReferenceSeq');
            return FhirResourceSerializer.serialize(value, MolecularSequenceReferenceSeq);
        },
        variant: (value) => {
            initializeSerializers('MolecularSequenceVariant');
            return FhirResourceSerializer.serializeArray(value, MolecularSequenceVariant);
        },
        observedSeq: null,
        quality: (value) => {
            initializeSerializers('MolecularSequenceQuality');
            return FhirResourceSerializer.serializeArray(value, MolecularSequenceQuality);
        },
        readCoverage: null,
        repository: (value) => {
            initializeSerializers('MolecularSequenceRepository');
            return FhirResourceSerializer.serializeArray(value, MolecularSequenceRepository);
        },
        pointer: (value) => {
            initializeSerializers('Reference');
            return FhirResourceSerializer.serializeArray(value, Reference);
        },
        structureVariant: (value) => {
            initializeSerializers('MolecularSequenceStructureVariant');
            return FhirResourceSerializer.serializeArray(value, MolecularSequenceStructureVariant);
        },
        resourceType: null
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @returns {any} Cleaned object
     */
    static serialize(rawJson) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => MolecularSequenceSerializer.serialize(item));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in MolecularSequenceSerializer.propertyToSerializerMap) {
                if (MolecularSequenceSerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = MolecularSequenceSerializer.propertyToSerializerMap[propertyName](value);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = MolecularSequenceSerializer;
