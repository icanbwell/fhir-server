// This file is auto-generated by generate_classes so do not edit manually

/** @type {import('../complex_types/meta.js')} */
let MetaSerializer;
/** @type {import('../complex_types/narrative.js')} */
let NarrativeSerializer;
/** @type {import('../simple_types/resourceContainer.js')} */
let ResourceContainerSerializer;
/** @type {import('../complex_types/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complex_types/identifier.js')} */
let IdentifierSerializer;
/** @type {import('../complex_types/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../complex_types/reference.js')} */
let ReferenceSerializer;
/** @type {import('../complex_types/period.js')} */
let PeriodSerializer;
/** @type {import('../backbone_elements/claimRelated.js')} */
let ClaimRelatedSerializer;
/** @type {import('../backbone_elements/claimPayee.js')} */
let ClaimPayeeSerializer;
/** @type {import('../backbone_elements/claimCareTeam.js')} */
let ClaimCareTeamSerializer;
/** @type {import('../backbone_elements/claimSupportingInfo.js')} */
let ClaimSupportingInfoSerializer;
/** @type {import('../backbone_elements/claimDiagnosis.js')} */
let ClaimDiagnosisSerializer;
/** @type {import('../backbone_elements/claimProcedure.js')} */
let ClaimProcedureSerializer;
/** @type {import('../backbone_elements/claimInsurance.js')} */
let ClaimInsuranceSerializer;
/** @type {import('../backbone_elements/claimAccident.js')} */
let ClaimAccidentSerializer;
/** @type {import('../backbone_elements/claimItem.js')} */
let ClaimItemSerializer;
/** @type {import('../complex_types/money.js')} */
let MoneySerializer;

/**
 * Lazy loads the required serializers
 * It esnures that require is called only once for each serializer to minimize the call stack for require()
 * @returns {any}
 */
function initializeSerializers(serializerName) {
    initializeResourceSerializer()
    if (serializerName === 'Meta' && !MetaSerializer) {
        MetaSerializer = require('../complex_types/meta.js');
        return MetaSerializer;
    }
    if (serializerName === 'Narrative' && !NarrativeSerializer) {
        NarrativeSerializer = require('../complex_types/narrative.js');
        return NarrativeSerializer;
    }
    if (serializerName === 'ResourceContainer' && !ResourceContainerSerializer) {
        ResourceContainerSerializer = require('../simple_types/resourceContainer.js');
        return ResourceContainerSerializer;
    }
    if (serializerName === 'Extension' && !ExtensionSerializer) {
        ExtensionSerializer = require('../complex_types/extension.js');
        return ExtensionSerializer;
    }
    if (serializerName === 'Identifier' && !IdentifierSerializer) {
        IdentifierSerializer = require('../complex_types/identifier.js');
        return IdentifierSerializer;
    }
    if (serializerName === 'CodeableConcept' && !CodeableConceptSerializer) {
        CodeableConceptSerializer = require('../complex_types/codeableConcept.js');
        return CodeableConceptSerializer;
    }
    if (serializerName === 'Reference' && !ReferenceSerializer) {
        ReferenceSerializer = require('../complex_types/reference.js');
        return ReferenceSerializer;
    }
    if (serializerName === 'Period' && !PeriodSerializer) {
        PeriodSerializer = require('../complex_types/period.js');
        return PeriodSerializer;
    }
    if (serializerName === 'ClaimRelated' && !ClaimRelatedSerializer) {
        ClaimRelatedSerializer = require('../backbone_elements/claimRelated.js');
        return ClaimRelatedSerializer;
    }
    if (serializerName === 'ClaimPayee' && !ClaimPayeeSerializer) {
        ClaimPayeeSerializer = require('../backbone_elements/claimPayee.js');
        return ClaimPayeeSerializer;
    }
    if (serializerName === 'ClaimCareTeam' && !ClaimCareTeamSerializer) {
        ClaimCareTeamSerializer = require('../backbone_elements/claimCareTeam.js');
        return ClaimCareTeamSerializer;
    }
    if (serializerName === 'ClaimSupportingInfo' && !ClaimSupportingInfoSerializer) {
        ClaimSupportingInfoSerializer = require('../backbone_elements/claimSupportingInfo.js');
        return ClaimSupportingInfoSerializer;
    }
    if (serializerName === 'ClaimDiagnosis' && !ClaimDiagnosisSerializer) {
        ClaimDiagnosisSerializer = require('../backbone_elements/claimDiagnosis.js');
        return ClaimDiagnosisSerializer;
    }
    if (serializerName === 'ClaimProcedure' && !ClaimProcedureSerializer) {
        ClaimProcedureSerializer = require('../backbone_elements/claimProcedure.js');
        return ClaimProcedureSerializer;
    }
    if (serializerName === 'ClaimInsurance' && !ClaimInsuranceSerializer) {
        ClaimInsuranceSerializer = require('../backbone_elements/claimInsurance.js');
        return ClaimInsuranceSerializer;
    }
    if (serializerName === 'ClaimAccident' && !ClaimAccidentSerializer) {
        ClaimAccidentSerializer = require('../backbone_elements/claimAccident.js');
        return ClaimAccidentSerializer;
    }
    if (serializerName === 'ClaimItem' && !ClaimItemSerializer) {
        ClaimItemSerializer = require('../backbone_elements/claimItem.js');
        return ClaimItemSerializer;
    }
    if (serializerName === 'Money' && !MoneySerializer) {
        MoneySerializer = require('../complex_types/money.js');
        return MoneySerializer;
    }
}

/** @type {import('../../../fhirResourceSerializer.js').FhirResourceSerializer} */
let FhirResourceSerializer;

function initializeResourceSerializer() {
    if (!FhirResourceSerializer) {
        FhirResourceSerializer = require('../../../fhirResourceSerializer.js').FhirResourceSerializer;
    }

    return FhirResourceSerializer;
}

class ClaimSerializer {
    static propertyToSerializerMap = {
        id: null,
        meta: (value) => {
            initializeSerializers('Meta');
            return FhirResourceSerializer.serialize(value, MetaSerializer);
        },
        implicitRules: null,
        language: null,
        text: (value) => {
            initializeSerializers('Narrative');
            return FhirResourceSerializer.serialize(value, NarrativeSerializer);
        },
        contained: (value) => {
            initializeSerializers('ResourceContainer');
            return FhirResourceSerializer.serializeArray(value);
        },
        extension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer);
        },
        modifierExtension: (value) => {
            initializeSerializers('Extension');
            return FhirResourceSerializer.serializeArray(value, ExtensionSerializer);
        },
        identifier: (value) => {
            initializeSerializers('Identifier');
            return FhirResourceSerializer.serializeArray(value, IdentifierSerializer);
        },
        status: null,
        type: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serialize(value, CodeableConceptSerializer);
        },
        subType: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serialize(value, CodeableConceptSerializer);
        },
        use: null,
        patient: (value) => {
            initializeSerializers('Reference');
            return FhirResourceSerializer.serialize(value, ReferenceSerializer);
        },
        billablePeriod: (value) => {
            initializeSerializers('Period');
            return FhirResourceSerializer.serialize(value, PeriodSerializer);
        },
        created: null,
        enterer: (value) => {
            initializeSerializers('Reference');
            return FhirResourceSerializer.serialize(value, ReferenceSerializer);
        },
        insurer: (value) => {
            initializeSerializers('Reference');
            return FhirResourceSerializer.serialize(value, ReferenceSerializer);
        },
        provider: (value) => {
            initializeSerializers('Reference');
            return FhirResourceSerializer.serialize(value, ReferenceSerializer);
        },
        priority: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serialize(value, CodeableConceptSerializer);
        },
        fundsReserve: (value) => {
            initializeSerializers('CodeableConcept');
            return FhirResourceSerializer.serialize(value, CodeableConceptSerializer);
        },
        related: (value) => {
            initializeSerializers('ClaimRelated');
            return FhirResourceSerializer.serializeArray(value, ClaimRelatedSerializer);
        },
        prescription: (value) => {
            initializeSerializers('Reference');
            return FhirResourceSerializer.serialize(value, ReferenceSerializer);
        },
        originalPrescription: (value) => {
            initializeSerializers('Reference');
            return FhirResourceSerializer.serialize(value, ReferenceSerializer);
        },
        payee: (value) => {
            initializeSerializers('ClaimPayee');
            return FhirResourceSerializer.serialize(value, ClaimPayeeSerializer);
        },
        referral: (value) => {
            initializeSerializers('Reference');
            return FhirResourceSerializer.serialize(value, ReferenceSerializer);
        },
        facility: (value) => {
            initializeSerializers('Reference');
            return FhirResourceSerializer.serialize(value, ReferenceSerializer);
        },
        careTeam: (value) => {
            initializeSerializers('ClaimCareTeam');
            return FhirResourceSerializer.serializeArray(value, ClaimCareTeamSerializer);
        },
        supportingInfo: (value) => {
            initializeSerializers('ClaimSupportingInfo');
            return FhirResourceSerializer.serializeArray(value, ClaimSupportingInfoSerializer);
        },
        diagnosis: (value) => {
            initializeSerializers('ClaimDiagnosis');
            return FhirResourceSerializer.serializeArray(value, ClaimDiagnosisSerializer);
        },
        procedure: (value) => {
            initializeSerializers('ClaimProcedure');
            return FhirResourceSerializer.serializeArray(value, ClaimProcedureSerializer);
        },
        insurance: (value) => {
            initializeSerializers('ClaimInsurance');
            return FhirResourceSerializer.serializeArray(value, ClaimInsuranceSerializer);
        },
        accident: (value) => {
            initializeSerializers('ClaimAccident');
            return FhirResourceSerializer.serialize(value, ClaimAccidentSerializer);
        },
        item: (value) => {
            initializeSerializers('ClaimItem');
            return FhirResourceSerializer.serializeArray(value, ClaimItemSerializer);
        },
        total: (value) => {
            initializeSerializers('Money');
            return FhirResourceSerializer.serialize(value, MoneySerializer);
        },
        resourceType: null
    };

    /**
     * This methods cleans the raw json by removing additional fields which are not defined
     * according to FHIR Specs
     * @param {any} rawJson
     * @returns {any} Cleaned object
     */
    static serialize(rawJson) {
        if (!rawJson) return rawJson;

        // Handle array case
        if (Array.isArray(rawJson)) {
            return rawJson.map(item => ClaimSerializer.serialize(item));
        }

        // Handle non-object case
        if (typeof rawJson !== 'object') return rawJson;

        Object.keys(rawJson).forEach(propertyName => {
            const value = rawJson[propertyName];

            if (value === null || value === undefined) {
                delete rawJson[propertyName];
                return;
            }

            if (propertyName in ClaimSerializer.propertyToSerializerMap) {
                if (ClaimSerializer.propertyToSerializerMap[propertyName]) {
                    const serializedValue = ClaimSerializer.propertyToSerializerMap[propertyName](value);
                    if (serializedValue === null || serializedValue === undefined) {
                        delete rawJson[propertyName];
                    } else {
                        rawJson[propertyName] = serializedValue;
                    }
                }
            } else {
                delete rawJson[propertyName];
            }
        });

        return rawJson;
    }
}

module.exports = ClaimSerializer;
