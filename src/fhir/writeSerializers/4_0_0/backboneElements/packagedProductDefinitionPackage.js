// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/identifier.js')} */
let IdentifierSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../backboneElements/packagedProductDefinitionShelfLifeStorage.js')} */
let PackagedProductDefinitionShelfLifeStorageSerializer;
/** @type {import('../complexTypes/reference.js')} */
let ReferenceSerializer;
/** @type {import('../backboneElements/packagedProductDefinitionProperty.js')} */
let PackagedProductDefinitionPropertySerializer;
/** @type {import('../backboneElements/packagedProductDefinitionContainedItem.js')} */
let PackagedProductDefinitionContainedItemSerializer;

class PackagedProductDefinitionPackageSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        type: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['type']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['type'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['type'];
        },
        quantity: null,
        material: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['material']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['material'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['material'];
        },
        alternateMaterial: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['alternateMaterial']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['alternateMaterial'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['alternateMaterial'];
        },
        shelfLifeStorage: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['shelfLifeStorage']) {
                if (!PackagedProductDefinitionShelfLifeStorageSerializer) {
                    PackagedProductDefinitionShelfLifeStorageSerializer = require('../backboneElements/packagedProductDefinitionShelfLifeStorage.js');
                }
                this.#configCache['shelfLifeStorage'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: PackagedProductDefinitionShelfLifeStorageSerializer
                };
            }
            return this.#configCache['shelfLifeStorage'];
        },
        manufacturer: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['manufacturer']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['manufacturer'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['manufacturer'];
        },
        property: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['property']) {
                if (!PackagedProductDefinitionPropertySerializer) {
                    PackagedProductDefinitionPropertySerializer = require('../backboneElements/packagedProductDefinitionProperty.js');
                }
                this.#configCache['property'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: PackagedProductDefinitionPropertySerializer
                };
            }
            return this.#configCache['property'];
        },
        containedItem: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['containedItem']) {
                if (!PackagedProductDefinitionContainedItemSerializer) {
                    PackagedProductDefinitionContainedItemSerializer = require('../backboneElements/packagedProductDefinitionContainedItem.js');
                }
                this.#configCache['containedItem'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: PackagedProductDefinitionContainedItemSerializer
                };
            }
            return this.#configCache['containedItem'];
        },
        package: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['package']) {
                this.#configCache['package'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: module.exports // Reference to the current serializer for recursive class
                };
            }
            return this.#configCache['package'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new PackagedProductDefinitionPackageSerializer();
