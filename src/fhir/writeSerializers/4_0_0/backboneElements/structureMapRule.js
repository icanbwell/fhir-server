// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../backboneElements/structureMapSource.js')} */
let StructureMapSourceSerializer;
/** @type {import('../backboneElements/structureMapTarget.js')} */
let StructureMapTargetSerializer;
/** @type {import('../backboneElements/structureMapDependent.js')} */
let StructureMapDependentSerializer;

class StructureMapRuleSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        name: null,
        source: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['source']) {
                if (!StructureMapSourceSerializer) {
                    StructureMapSourceSerializer = require('../backboneElements/structureMapSource.js');
                }
                this.#configCache['source'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: StructureMapSourceSerializer
                };
            }
            return this.#configCache['source'];
        },
        target: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['target']) {
                if (!StructureMapTargetSerializer) {
                    StructureMapTargetSerializer = require('../backboneElements/structureMapTarget.js');
                }
                this.#configCache['target'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: StructureMapTargetSerializer
                };
            }
            return this.#configCache['target'];
        },
        rule: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['rule']) {
                if (!StructureMapRuleSerializer) {
                    StructureMapRuleSerializer = require('');
                }
                this.#configCache['rule'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: StructureMapRuleSerializer
                };
            }
            return this.#configCache['rule'];
        },
        dependent: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['dependent']) {
                if (!StructureMapDependentSerializer) {
                    StructureMapDependentSerializer = require('../backboneElements/structureMapDependent.js');
                }
                this.#configCache['dependent'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: StructureMapDependentSerializer
                };
            }
            return this.#configCache['dependent'];
        },
        documentation: null
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new StructureMapRuleSerializer();
