// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/address.js')} */
let AddressSerializer;
/** @type {import('../complexTypes/quantity.js')} */
let QuantitySerializer;
/** @type {import('../complexTypes/annotation.js')} */
let AnnotationSerializer;
/** @type {import('../complexTypes/attachment.js')} */
let AttachmentSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../complexTypes/coding.js')} */
let CodingSerializer;
/** @type {import('../complexTypes/contactPoint.js')} */
let ContactPointSerializer;
/** @type {import('../complexTypes/humanName.js')} */
let HumanNameSerializer;
/** @type {import('../complexTypes/identifier.js')} */
let IdentifierSerializer;
/** @type {import('../complexTypes/money.js')} */
let MoneySerializer;
/** @type {import('../complexTypes/period.js')} */
let PeriodSerializer;
/** @type {import('../complexTypes/range.js')} */
let RangeSerializer;
/** @type {import('../complexTypes/ratio.js')} */
let RatioSerializer;
/** @type {import('../complexTypes/reference.js')} */
let ReferenceSerializer;
/** @type {import('../complexTypes/sampledData.js')} */
let SampledDataSerializer;
/** @type {import('../complexTypes/signature.js')} */
let SignatureSerializer;
/** @type {import('../backboneElements/timing.js')} */
let TimingSerializer;
/** @type {import('../complexTypes/contactDetail.js')} */
let ContactDetailSerializer;
/** @type {import('../complexTypes/contributor.js')} */
let ContributorSerializer;
/** @type {import('../complexTypes/dataRequirement.js')} */
let DataRequirementSerializer;
/** @type {import('../complexTypes/expression.js')} */
let ExpressionSerializer;
/** @type {import('../complexTypes/parameterDefinition.js')} */
let ParameterDefinitionSerializer;
/** @type {import('../complexTypes/relatedArtifact.js')} */
let RelatedArtifactSerializer;
/** @type {import('../complexTypes/triggerDefinition.js')} */
let TriggerDefinitionSerializer;
/** @type {import('../complexTypes/usageContext.js')} */
let UsageContextSerializer;
/** @type {import('../backboneElements/dosage.js')} */
let DosageSerializer;
/** @type {import('../complexTypes/meta.js')} */
let MetaSerializer;
/** @type {import('../customSerializers/resourceContainer.js')} */
let ResourceContainerSerializer;

class ParametersParameterSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        name: null,
        valueBase64Binary: null,
        valueBoolean: null,
        valueCanonical: null,
        valueCode: null,
        valueDate: null,
        valueDateTime: null,
        valueDecimal: null,
        valueId: null,
        valueInstant: null,
        valueInteger: null,
        valueMarkdown: null,
        valueOid: null,
        valuePositiveInt: null,
        valueString: null,
        valueTime: null,
        valueUnsignedInt: null,
        valueUri: null,
        valueUrl: null,
        valueUuid: null,
        valueAddress: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueAddress']) {
                if (!AddressSerializer) {
                    AddressSerializer = require('../complexTypes/address.js');
                }
                this.#configCache['valueAddress'] = {
                    serializeFunction: 'serialize',
                    serializerClass: AddressSerializer
                };
            }
            return this.#configCache['valueAddress'];
        },
        valueAge: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueAge']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['valueAge'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['valueAge'];
        },
        valueAnnotation: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueAnnotation']) {
                if (!AnnotationSerializer) {
                    AnnotationSerializer = require('../complexTypes/annotation.js');
                }
                this.#configCache['valueAnnotation'] = {
                    serializeFunction: 'serialize',
                    serializerClass: AnnotationSerializer
                };
            }
            return this.#configCache['valueAnnotation'];
        },
        valueAttachment: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueAttachment']) {
                if (!AttachmentSerializer) {
                    AttachmentSerializer = require('../complexTypes/attachment.js');
                }
                this.#configCache['valueAttachment'] = {
                    serializeFunction: 'serialize',
                    serializerClass: AttachmentSerializer
                };
            }
            return this.#configCache['valueAttachment'];
        },
        valueCodeableConcept: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueCodeableConcept']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['valueCodeableConcept'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['valueCodeableConcept'];
        },
        valueCoding: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueCoding']) {
                if (!CodingSerializer) {
                    CodingSerializer = require('../complexTypes/coding.js');
                }
                this.#configCache['valueCoding'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodingSerializer
                };
            }
            return this.#configCache['valueCoding'];
        },
        valueContactPoint: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueContactPoint']) {
                if (!ContactPointSerializer) {
                    ContactPointSerializer = require('../complexTypes/contactPoint.js');
                }
                this.#configCache['valueContactPoint'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ContactPointSerializer
                };
            }
            return this.#configCache['valueContactPoint'];
        },
        valueCount: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueCount']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['valueCount'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['valueCount'];
        },
        valueDistance: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueDistance']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['valueDistance'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['valueDistance'];
        },
        valueDuration: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueDuration']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['valueDuration'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['valueDuration'];
        },
        valueHumanName: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueHumanName']) {
                if (!HumanNameSerializer) {
                    HumanNameSerializer = require('../complexTypes/humanName.js');
                }
                this.#configCache['valueHumanName'] = {
                    serializeFunction: 'serialize',
                    serializerClass: HumanNameSerializer
                };
            }
            return this.#configCache['valueHumanName'];
        },
        valueIdentifier: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueIdentifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['valueIdentifier'] = {
                    serializeFunction: 'serialize',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['valueIdentifier'];
        },
        valueMoney: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueMoney']) {
                if (!MoneySerializer) {
                    MoneySerializer = require('../complexTypes/money.js');
                }
                this.#configCache['valueMoney'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MoneySerializer
                };
            }
            return this.#configCache['valueMoney'];
        },
        valuePeriod: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valuePeriod']) {
                if (!PeriodSerializer) {
                    PeriodSerializer = require('../complexTypes/period.js');
                }
                this.#configCache['valuePeriod'] = {
                    serializeFunction: 'serialize',
                    serializerClass: PeriodSerializer
                };
            }
            return this.#configCache['valuePeriod'];
        },
        valueQuantity: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueQuantity']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['valueQuantity'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['valueQuantity'];
        },
        valueRange: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueRange']) {
                if (!RangeSerializer) {
                    RangeSerializer = require('../complexTypes/range.js');
                }
                this.#configCache['valueRange'] = {
                    serializeFunction: 'serialize',
                    serializerClass: RangeSerializer
                };
            }
            return this.#configCache['valueRange'];
        },
        valueRatio: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueRatio']) {
                if (!RatioSerializer) {
                    RatioSerializer = require('../complexTypes/ratio.js');
                }
                this.#configCache['valueRatio'] = {
                    serializeFunction: 'serialize',
                    serializerClass: RatioSerializer
                };
            }
            return this.#configCache['valueRatio'];
        },
        valueReference: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueReference']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['valueReference'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['valueReference'];
        },
        valueSampledData: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueSampledData']) {
                if (!SampledDataSerializer) {
                    SampledDataSerializer = require('../complexTypes/sampledData.js');
                }
                this.#configCache['valueSampledData'] = {
                    serializeFunction: 'serialize',
                    serializerClass: SampledDataSerializer
                };
            }
            return this.#configCache['valueSampledData'];
        },
        valueSignature: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueSignature']) {
                if (!SignatureSerializer) {
                    SignatureSerializer = require('../complexTypes/signature.js');
                }
                this.#configCache['valueSignature'] = {
                    serializeFunction: 'serialize',
                    serializerClass: SignatureSerializer
                };
            }
            return this.#configCache['valueSignature'];
        },
        valueTiming: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueTiming']) {
                if (!TimingSerializer) {
                    TimingSerializer = require('../backboneElements/timing.js');
                }
                this.#configCache['valueTiming'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TimingSerializer
                };
            }
            return this.#configCache['valueTiming'];
        },
        valueContactDetail: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueContactDetail']) {
                if (!ContactDetailSerializer) {
                    ContactDetailSerializer = require('../complexTypes/contactDetail.js');
                }
                this.#configCache['valueContactDetail'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ContactDetailSerializer
                };
            }
            return this.#configCache['valueContactDetail'];
        },
        valueContributor: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueContributor']) {
                if (!ContributorSerializer) {
                    ContributorSerializer = require('../complexTypes/contributor.js');
                }
                this.#configCache['valueContributor'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ContributorSerializer
                };
            }
            return this.#configCache['valueContributor'];
        },
        valueDataRequirement: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueDataRequirement']) {
                if (!DataRequirementSerializer) {
                    DataRequirementSerializer = require('../complexTypes/dataRequirement.js');
                }
                this.#configCache['valueDataRequirement'] = {
                    serializeFunction: 'serialize',
                    serializerClass: DataRequirementSerializer
                };
            }
            return this.#configCache['valueDataRequirement'];
        },
        valueExpression: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueExpression']) {
                if (!ExpressionSerializer) {
                    ExpressionSerializer = require('../complexTypes/expression.js');
                }
                this.#configCache['valueExpression'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ExpressionSerializer
                };
            }
            return this.#configCache['valueExpression'];
        },
        valueParameterDefinition: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueParameterDefinition']) {
                if (!ParameterDefinitionSerializer) {
                    ParameterDefinitionSerializer = require('../complexTypes/parameterDefinition.js');
                }
                this.#configCache['valueParameterDefinition'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ParameterDefinitionSerializer
                };
            }
            return this.#configCache['valueParameterDefinition'];
        },
        valueRelatedArtifact: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueRelatedArtifact']) {
                if (!RelatedArtifactSerializer) {
                    RelatedArtifactSerializer = require('../complexTypes/relatedArtifact.js');
                }
                this.#configCache['valueRelatedArtifact'] = {
                    serializeFunction: 'serialize',
                    serializerClass: RelatedArtifactSerializer
                };
            }
            return this.#configCache['valueRelatedArtifact'];
        },
        valueTriggerDefinition: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueTriggerDefinition']) {
                if (!TriggerDefinitionSerializer) {
                    TriggerDefinitionSerializer = require('../complexTypes/triggerDefinition.js');
                }
                this.#configCache['valueTriggerDefinition'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TriggerDefinitionSerializer
                };
            }
            return this.#configCache['valueTriggerDefinition'];
        },
        valueUsageContext: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueUsageContext']) {
                if (!UsageContextSerializer) {
                    UsageContextSerializer = require('../complexTypes/usageContext.js');
                }
                this.#configCache['valueUsageContext'] = {
                    serializeFunction: 'serialize',
                    serializerClass: UsageContextSerializer
                };
            }
            return this.#configCache['valueUsageContext'];
        },
        valueDosage: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueDosage']) {
                if (!DosageSerializer) {
                    DosageSerializer = require('../backboneElements/dosage.js');
                }
                this.#configCache['valueDosage'] = {
                    serializeFunction: 'serialize',
                    serializerClass: DosageSerializer
                };
            }
            return this.#configCache['valueDosage'];
        },
        valueMeta: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['valueMeta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['valueMeta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['valueMeta'];
        },
        resource: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['resource']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['resource'] = {
                    serializeFunction: 'serialize'
                };
            }
            return this.#configCache['resource'];
        },
        part: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['part']) {
                this.#configCache['part'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: module.exports // Reference to the current serializer for recursive class
                };
            }
            return this.#configCache['part'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new ParametersParameterSerializer();
