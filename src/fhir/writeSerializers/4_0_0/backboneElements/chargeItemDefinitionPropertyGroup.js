// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../backboneElements/chargeItemDefinitionApplicability.js')} */
let ChargeItemDefinitionApplicabilitySerializer;
/** @type {import('../backboneElements/chargeItemDefinitionPriceComponent.js')} */
let ChargeItemDefinitionPriceComponentSerializer;

class ChargeItemDefinitionPropertyGroupSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        applicability: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['applicability']) {
                if (!ChargeItemDefinitionApplicabilitySerializer) {
                    ChargeItemDefinitionApplicabilitySerializer = require('../backboneElements/chargeItemDefinitionApplicability.js');
                }
                this.#configCache['applicability'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ChargeItemDefinitionApplicabilitySerializer
                };
            }
            return this.#configCache['applicability'];
        },
        priceComponent: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['priceComponent']) {
                if (!ChargeItemDefinitionPriceComponentSerializer) {
                    ChargeItemDefinitionPriceComponentSerializer = require('../backboneElements/chargeItemDefinitionPriceComponent.js');
                }
                this.#configCache['priceComponent'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ChargeItemDefinitionPriceComponentSerializer
                };
            }
            return this.#configCache['priceComponent'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new ChargeItemDefinitionPropertyGroupSerializer();
