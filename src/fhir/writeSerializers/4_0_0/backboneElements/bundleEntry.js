// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../backboneElements/bundleLink.js')} */
let BundleLinkSerializer;
/** @type {import('../customSerializers/resourceContainer.js')} */
let ResourceContainerSerializer;
/** @type {import('../backboneElements/bundleSearch.js')} */
let BundleSearchSerializer;
/** @type {import('../backboneElements/bundleRequest.js')} */
let BundleRequestSerializer;
/** @type {import('../backboneElements/bundleResponse.js')} */
let BundleResponseSerializer;

class BundleEntrySerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        link: () => {
            if (!this.#configCache['link']) {
                if (!BundleLinkSerializer) {
                    BundleLinkSerializer = require('../backboneElements/bundleLink.js');
                }
                this.#configCache['link'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: BundleLinkSerializer
                };
            }
            return this.#configCache['link'];
        },
        fullUrl: null,
        resource: () => {
            if (!this.#configCache['resource']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['resource'] = {
                    serializeFunction: 'serialize'
                };
            }
            return this.#configCache['resource'];
        },
        search: () => {
            if (!this.#configCache['search']) {
                if (!BundleSearchSerializer) {
                    BundleSearchSerializer = require('../backboneElements/bundleSearch.js');
                }
                this.#configCache['search'] = {
                    serializeFunction: 'serialize',
                    serializerClass: BundleSearchSerializer
                };
            }
            return this.#configCache['search'];
        },
        request: () => {
            if (!this.#configCache['request']) {
                if (!BundleRequestSerializer) {
                    BundleRequestSerializer = require('../backboneElements/bundleRequest.js');
                }
                this.#configCache['request'] = {
                    serializeFunction: 'serialize',
                    serializerClass: BundleRequestSerializer
                };
            }
            return this.#configCache['request'];
        },
        response: () => {
            if (!this.#configCache['response']) {
                if (!BundleResponseSerializer) {
                    BundleResponseSerializer = require('../backboneElements/bundleResponse.js');
                }
                this.#configCache['response'] = {
                    serializeFunction: 'serialize',
                    serializerClass: BundleResponseSerializer
                };
            }
            return this.#configCache['response'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new BundleEntrySerializer();
