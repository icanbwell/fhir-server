// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../backboneElements/valueSetParameter.js')} */
let ValueSetParameterSerializer;
/** @type {import('../backboneElements/valueSetContains.js')} */
let ValueSetContainsSerializer;

class ValueSetExpansionSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: null,
        timestamp: null,
        total: null,
        offset: null,
        parameter: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['parameter']) {
                if (!ValueSetParameterSerializer) {
                    ValueSetParameterSerializer = require('../backboneElements/valueSetParameter.js');
                }
                this.#configCache['parameter'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ValueSetParameterSerializer
                };
            }
            return this.#configCache['parameter'];
        },
        contains: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contains']) {
                if (!ValueSetContainsSerializer) {
                    ValueSetContainsSerializer = require('../backboneElements/valueSetContains.js');
                }
                this.#configCache['contains'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ValueSetContainsSerializer
                };
            }
            return this.#configCache['contains'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new ValueSetExpansionSerializer();
