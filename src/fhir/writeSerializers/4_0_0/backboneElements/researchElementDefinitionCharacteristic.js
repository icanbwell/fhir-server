// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../complexTypes/expression.js')} */
let ExpressionSerializer;
/** @type {import('../complexTypes/dataRequirement.js')} */
let DataRequirementSerializer;
/** @type {import('../complexTypes/usageContext.js')} */
let UsageContextSerializer;
/** @type {import('../complexTypes/period.js')} */
let PeriodSerializer;
/** @type {import('../complexTypes/quantity.js')} */
let QuantitySerializer;
/** @type {import('../backboneElements/timing.js')} */
let TimingSerializer;

class ResearchElementDefinitionCharacteristicSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        definitionCodeableConcept: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['definitionCodeableConcept']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['definitionCodeableConcept'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['definitionCodeableConcept'];
        },
        definitionCanonical: null,
        definitionExpression: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['definitionExpression']) {
                if (!ExpressionSerializer) {
                    ExpressionSerializer = require('../complexTypes/expression.js');
                }
                this.#configCache['definitionExpression'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ExpressionSerializer
                };
            }
            return this.#configCache['definitionExpression'];
        },
        definitionDataRequirement: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['definitionDataRequirement']) {
                if (!DataRequirementSerializer) {
                    DataRequirementSerializer = require('../complexTypes/dataRequirement.js');
                }
                this.#configCache['definitionDataRequirement'] = {
                    serializeFunction: 'serialize',
                    serializerClass: DataRequirementSerializer
                };
            }
            return this.#configCache['definitionDataRequirement'];
        },
        usageContext: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['usageContext']) {
                if (!UsageContextSerializer) {
                    UsageContextSerializer = require('../complexTypes/usageContext.js');
                }
                this.#configCache['usageContext'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: UsageContextSerializer
                };
            }
            return this.#configCache['usageContext'];
        },
        exclude: null,
        unitOfMeasure: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['unitOfMeasure']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['unitOfMeasure'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['unitOfMeasure'];
        },
        studyEffectiveDescription: null,
        studyEffectiveDateTime: null,
        studyEffectivePeriod: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['studyEffectivePeriod']) {
                if (!PeriodSerializer) {
                    PeriodSerializer = require('../complexTypes/period.js');
                }
                this.#configCache['studyEffectivePeriod'] = {
                    serializeFunction: 'serialize',
                    serializerClass: PeriodSerializer
                };
            }
            return this.#configCache['studyEffectivePeriod'];
        },
        studyEffectiveDuration: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['studyEffectiveDuration']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['studyEffectiveDuration'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['studyEffectiveDuration'];
        },
        studyEffectiveTiming: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['studyEffectiveTiming']) {
                if (!TimingSerializer) {
                    TimingSerializer = require('../backboneElements/timing.js');
                }
                this.#configCache['studyEffectiveTiming'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TimingSerializer
                };
            }
            return this.#configCache['studyEffectiveTiming'];
        },
        studyEffectiveTimeFromStart: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['studyEffectiveTimeFromStart']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['studyEffectiveTimeFromStart'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['studyEffectiveTimeFromStart'];
        },
        studyEffectiveGroupMeasure: null,
        participantEffectiveDescription: null,
        participantEffectiveDateTime: null,
        participantEffectivePeriod: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['participantEffectivePeriod']) {
                if (!PeriodSerializer) {
                    PeriodSerializer = require('../complexTypes/period.js');
                }
                this.#configCache['participantEffectivePeriod'] = {
                    serializeFunction: 'serialize',
                    serializerClass: PeriodSerializer
                };
            }
            return this.#configCache['participantEffectivePeriod'];
        },
        participantEffectiveDuration: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['participantEffectiveDuration']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['participantEffectiveDuration'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['participantEffectiveDuration'];
        },
        participantEffectiveTiming: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['participantEffectiveTiming']) {
                if (!TimingSerializer) {
                    TimingSerializer = require('../backboneElements/timing.js');
                }
                this.#configCache['participantEffectiveTiming'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TimingSerializer
                };
            }
            return this.#configCache['participantEffectiveTiming'];
        },
        participantEffectiveTimeFromStart: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['participantEffectiveTimeFromStart']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['participantEffectiveTimeFromStart'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['participantEffectiveTimeFromStart'];
        },
        participantEffectiveGroupMeasure: null
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new ResearchElementDefinitionCharacteristicSerializer();
