// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../backboneElements/testReportOperation.js')} */
let TestReportOperationSerializer;
/** @type {import('../backboneElements/testReportAssert.js')} */
let TestReportAssertSerializer;

class TestReportActionSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        operation: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['operation']) {
                if (!TestReportOperationSerializer) {
                    TestReportOperationSerializer = require('../backboneElements/testReportOperation.js');
                }
                this.#configCache['operation'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TestReportOperationSerializer
                };
            }
            return this.#configCache['operation'];
        },
        assert: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['assert']) {
                if (!TestReportAssertSerializer) {
                    TestReportAssertSerializer = require('../backboneElements/testReportAssert.js');
                }
                this.#configCache['assert'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TestReportAssertSerializer
                };
            }
            return this.#configCache['assert'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new TestReportActionSerializer();
