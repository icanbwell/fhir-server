// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../backboneElements/testScriptLink.js')} */
let TestScriptLinkSerializer;
/** @type {import('../backboneElements/testScriptCapability.js')} */
let TestScriptCapabilitySerializer;

class TestScriptMetadataSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        link: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['link']) {
                if (!TestScriptLinkSerializer) {
                    TestScriptLinkSerializer = require('../backboneElements/testScriptLink.js');
                }
                this.#configCache['link'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: TestScriptLinkSerializer
                };
            }
            return this.#configCache['link'];
        },
        capability: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['capability']) {
                if (!TestScriptCapabilitySerializer) {
                    TestScriptCapabilitySerializer = require('../backboneElements/testScriptCapability.js');
                }
                this.#configCache['capability'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: TestScriptCapabilitySerializer
                };
            }
            return this.#configCache['capability'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new TestScriptMetadataSerializer();
