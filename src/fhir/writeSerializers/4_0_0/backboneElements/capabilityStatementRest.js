// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../backboneElements/capabilityStatementSecurity.js')} */
let CapabilityStatementSecuritySerializer;
/** @type {import('../backboneElements/capabilityStatementResource.js')} */
let CapabilityStatementResourceSerializer;
/** @type {import('../backboneElements/capabilityStatementInteraction1.js')} */
let CapabilityStatementInteraction1Serializer;
/** @type {import('../backboneElements/capabilityStatementSearchParam.js')} */
let CapabilityStatementSearchParamSerializer;
/** @type {import('../backboneElements/capabilityStatementOperation.js')} */
let CapabilityStatementOperationSerializer;

class CapabilityStatementRestSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        mode: null,
        documentation: null,
        security: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['security']) {
                if (!CapabilityStatementSecuritySerializer) {
                    CapabilityStatementSecuritySerializer = require('../backboneElements/capabilityStatementSecurity.js');
                }
                this.#configCache['security'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CapabilityStatementSecuritySerializer
                };
            }
            return this.#configCache['security'];
        },
        resource: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['resource']) {
                if (!CapabilityStatementResourceSerializer) {
                    CapabilityStatementResourceSerializer = require('../backboneElements/capabilityStatementResource.js');
                }
                this.#configCache['resource'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CapabilityStatementResourceSerializer
                };
            }
            return this.#configCache['resource'];
        },
        interaction: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['interaction']) {
                if (!CapabilityStatementInteraction1Serializer) {
                    CapabilityStatementInteraction1Serializer = require('../backboneElements/capabilityStatementInteraction1.js');
                }
                this.#configCache['interaction'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CapabilityStatementInteraction1Serializer
                };
            }
            return this.#configCache['interaction'];
        },
        searchParam: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['searchParam']) {
                if (!CapabilityStatementSearchParamSerializer) {
                    CapabilityStatementSearchParamSerializer = require('../backboneElements/capabilityStatementSearchParam.js');
                }
                this.#configCache['searchParam'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CapabilityStatementSearchParamSerializer
                };
            }
            return this.#configCache['searchParam'];
        },
        operation: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['operation']) {
                if (!CapabilityStatementOperationSerializer) {
                    CapabilityStatementOperationSerializer = require('../backboneElements/capabilityStatementOperation.js');
                }
                this.#configCache['operation'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CapabilityStatementOperationSerializer
                };
            }
            return this.#configCache['operation'];
        },
        compartment: () => {
            // No serializer class needed for primitive lists
            return {
                serializeFunction: 'serializePrimitiveArray'
            };
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new CapabilityStatementRestSerializer();
