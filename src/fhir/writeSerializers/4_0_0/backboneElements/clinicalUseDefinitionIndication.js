// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/codeableReference.js')} */
let CodeableReferenceSerializer;
/** @type {import('../complexTypes/range.js')} */
let RangeSerializer;
/** @type {import('../complexTypes/reference.js')} */
let ReferenceSerializer;
/** @type {import('../backboneElements/clinicalUseDefinitionOtherTherapy.js')} */
let ClinicalUseDefinitionOtherTherapySerializer;

class ClinicalUseDefinitionIndicationSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        diseaseSymptomProcedure: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['diseaseSymptomProcedure']) {
                if (!CodeableReferenceSerializer) {
                    CodeableReferenceSerializer = require('../complexTypes/codeableReference.js');
                }
                this.#configCache['diseaseSymptomProcedure'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableReferenceSerializer
                };
            }
            return this.#configCache['diseaseSymptomProcedure'];
        },
        diseaseStatus: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['diseaseStatus']) {
                if (!CodeableReferenceSerializer) {
                    CodeableReferenceSerializer = require('../complexTypes/codeableReference.js');
                }
                this.#configCache['diseaseStatus'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableReferenceSerializer
                };
            }
            return this.#configCache['diseaseStatus'];
        },
        comorbidity: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['comorbidity']) {
                if (!CodeableReferenceSerializer) {
                    CodeableReferenceSerializer = require('../complexTypes/codeableReference.js');
                }
                this.#configCache['comorbidity'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableReferenceSerializer
                };
            }
            return this.#configCache['comorbidity'];
        },
        intendedEffect: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['intendedEffect']) {
                if (!CodeableReferenceSerializer) {
                    CodeableReferenceSerializer = require('../complexTypes/codeableReference.js');
                }
                this.#configCache['intendedEffect'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableReferenceSerializer
                };
            }
            return this.#configCache['intendedEffect'];
        },
        durationRange: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['durationRange']) {
                if (!RangeSerializer) {
                    RangeSerializer = require('../complexTypes/range.js');
                }
                this.#configCache['durationRange'] = {
                    serializeFunction: 'serialize',
                    serializerClass: RangeSerializer
                };
            }
            return this.#configCache['durationRange'];
        },
        durationString: null,
        undesirableEffect: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['undesirableEffect']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['undesirableEffect'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['undesirableEffect'];
        },
        otherTherapy: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['otherTherapy']) {
                if (!ClinicalUseDefinitionOtherTherapySerializer) {
                    ClinicalUseDefinitionOtherTherapySerializer = require('../backboneElements/clinicalUseDefinitionOtherTherapy.js');
                }
                this.#configCache['otherTherapy'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ClinicalUseDefinitionOtherTherapySerializer
                };
            }
            return this.#configCache['otherTherapy'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new ClinicalUseDefinitionIndicationSerializer();
