// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../backboneElements/capabilityStatementEndpoint.js')} */
let CapabilityStatementEndpointSerializer;
/** @type {import('../backboneElements/capabilityStatementSupportedMessage.js')} */
let CapabilityStatementSupportedMessageSerializer;

class CapabilityStatementMessagingSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        endpoint: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['endpoint']) {
                if (!CapabilityStatementEndpointSerializer) {
                    CapabilityStatementEndpointSerializer = require('../backboneElements/capabilityStatementEndpoint.js');
                }
                this.#configCache['endpoint'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CapabilityStatementEndpointSerializer
                };
            }
            return this.#configCache['endpoint'];
        },
        reliableCache: null,
        documentation: null,
        supportedMessage: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['supportedMessage']) {
                if (!CapabilityStatementSupportedMessageSerializer) {
                    CapabilityStatementSupportedMessageSerializer = require('../backboneElements/capabilityStatementSupportedMessage.js');
                }
                this.#configCache['supportedMessage'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CapabilityStatementSupportedMessageSerializer
                };
            }
            return this.#configCache['supportedMessage'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new CapabilityStatementMessagingSerializer();
