// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../backboneElements/operationDefinitionBinding.js')} */
let OperationDefinitionBindingSerializer;
/** @type {import('../backboneElements/operationDefinitionReferencedFrom.js')} */
let OperationDefinitionReferencedFromSerializer;

class OperationDefinitionParameterSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        name: null,
        use: null,
        min: null,
        max: null,
        documentation: null,
        type: null,
        targetProfile: null,
        searchType: null,
        binding: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['binding']) {
                if (!OperationDefinitionBindingSerializer) {
                    OperationDefinitionBindingSerializer = require('../backboneElements/operationDefinitionBinding.js');
                }
                this.#configCache['binding'] = {
                    serializeFunction: 'serialize',
                    serializerClass: OperationDefinitionBindingSerializer
                };
            }
            return this.#configCache['binding'];
        },
        referencedFrom: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['referencedFrom']) {
                if (!OperationDefinitionReferencedFromSerializer) {
                    OperationDefinitionReferencedFromSerializer = require('../backboneElements/operationDefinitionReferencedFrom.js');
                }
                this.#configCache['referencedFrom'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: OperationDefinitionReferencedFromSerializer
                };
            }
            return this.#configCache['referencedFrom'];
        },
        part: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['part']) {
                this.#configCache['part'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: module.exports // Reference to the current serializer for recursive class
                };
            }
            return this.#configCache['part'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new OperationDefinitionParameterSerializer();
