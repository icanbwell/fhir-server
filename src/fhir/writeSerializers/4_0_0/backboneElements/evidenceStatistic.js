// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/annotation.js')} */
let AnnotationSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../complexTypes/quantity.js')} */
let QuantitySerializer;
/** @type {import('../backboneElements/evidenceSampleSize.js')} */
let EvidenceSampleSizeSerializer;
/** @type {import('../backboneElements/evidenceAttributeEstimate.js')} */
let EvidenceAttributeEstimateSerializer;
/** @type {import('../backboneElements/evidenceModelCharacteristic.js')} */
let EvidenceModelCharacteristicSerializer;

class EvidenceStatisticSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        description: null,
        note: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['note']) {
                if (!AnnotationSerializer) {
                    AnnotationSerializer = require('../complexTypes/annotation.js');
                }
                this.#configCache['note'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: AnnotationSerializer
                };
            }
            return this.#configCache['note'];
        },
        statisticType: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['statisticType']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['statisticType'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['statisticType'];
        },
        category: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['category']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['category'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['category'];
        },
        quantity: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['quantity']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['quantity'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['quantity'];
        },
        numberOfEvents: null,
        numberAffected: null,
        sampleSize: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['sampleSize']) {
                if (!EvidenceSampleSizeSerializer) {
                    EvidenceSampleSizeSerializer = require('../backboneElements/evidenceSampleSize.js');
                }
                this.#configCache['sampleSize'] = {
                    serializeFunction: 'serialize',
                    serializerClass: EvidenceSampleSizeSerializer
                };
            }
            return this.#configCache['sampleSize'];
        },
        attributeEstimate: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['attributeEstimate']) {
                if (!EvidenceAttributeEstimateSerializer) {
                    EvidenceAttributeEstimateSerializer = require('../backboneElements/evidenceAttributeEstimate.js');
                }
                this.#configCache['attributeEstimate'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: EvidenceAttributeEstimateSerializer
                };
            }
            return this.#configCache['attributeEstimate'];
        },
        modelCharacteristic: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modelCharacteristic']) {
                if (!EvidenceModelCharacteristicSerializer) {
                    EvidenceModelCharacteristicSerializer = require('../backboneElements/evidenceModelCharacteristic.js');
                }
                this.#configCache['modelCharacteristic'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: EvidenceModelCharacteristicSerializer
                };
            }
            return this.#configCache['modelCharacteristic'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new EvidenceStatisticSerializer();
