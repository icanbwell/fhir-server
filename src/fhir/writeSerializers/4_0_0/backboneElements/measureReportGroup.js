// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../backboneElements/measureReportPopulation.js')} */
let MeasureReportPopulationSerializer;
/** @type {import('../complexTypes/quantity.js')} */
let QuantitySerializer;
/** @type {import('../backboneElements/measureReportStratifier.js')} */
let MeasureReportStratifierSerializer;

class MeasureReportGroupSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        code: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['code']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['code'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['code'];
        },
        population: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['population']) {
                if (!MeasureReportPopulationSerializer) {
                    MeasureReportPopulationSerializer = require('../backboneElements/measureReportPopulation.js');
                }
                this.#configCache['population'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MeasureReportPopulationSerializer
                };
            }
            return this.#configCache['population'];
        },
        measureScore: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['measureScore']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['measureScore'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['measureScore'];
        },
        stratifier: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['stratifier']) {
                if (!MeasureReportStratifierSerializer) {
                    MeasureReportStratifierSerializer = require('../backboneElements/measureReportStratifier.js');
                }
                this.#configCache['stratifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MeasureReportStratifierSerializer
                };
            }
            return this.#configCache['stratifier'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new MeasureReportGroupSerializer();
