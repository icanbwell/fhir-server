// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../backboneElements/exampleScenarioVersion.js')} */
let ExampleScenarioVersionSerializer;
/** @type {import('../backboneElements/exampleScenarioContainedInstance.js')} */
let ExampleScenarioContainedInstanceSerializer;

class ExampleScenarioInstanceSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        resourceId: null,
        resourceType: null,
        name: null,
        description: null,
        version: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['version']) {
                if (!ExampleScenarioVersionSerializer) {
                    ExampleScenarioVersionSerializer = require('../backboneElements/exampleScenarioVersion.js');
                }
                this.#configCache['version'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExampleScenarioVersionSerializer
                };
            }
            return this.#configCache['version'];
        },
        containedInstance: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['containedInstance']) {
                if (!ExampleScenarioContainedInstanceSerializer) {
                    ExampleScenarioContainedInstanceSerializer = require('../backboneElements/exampleScenarioContainedInstance.js');
                }
                this.#configCache['containedInstance'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExampleScenarioContainedInstanceSerializer
                };
            }
            return this.#configCache['containedInstance'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new ExampleScenarioInstanceSerializer();
