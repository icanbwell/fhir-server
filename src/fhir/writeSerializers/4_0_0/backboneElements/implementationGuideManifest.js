// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../backboneElements/implementationGuideResource1.js')} */
let ImplementationGuideResource1Serializer;
/** @type {import('../backboneElements/implementationGuidePage1.js')} */
let ImplementationGuidePage1Serializer;

class ImplementationGuideManifestSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        rendering: null,
        resource: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['resource']) {
                if (!ImplementationGuideResource1Serializer) {
                    ImplementationGuideResource1Serializer = require('../backboneElements/implementationGuideResource1.js');
                }
                this.#configCache['resource'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ImplementationGuideResource1Serializer
                };
            }
            return this.#configCache['resource'];
        },
        page: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['page']) {
                if (!ImplementationGuidePage1Serializer) {
                    ImplementationGuidePage1Serializer = require('../backboneElements/implementationGuidePage1.js');
                }
                this.#configCache['page'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ImplementationGuidePage1Serializer
                };
            }
            return this.#configCache['page'];
        },
        image: null,
        other: null
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new ImplementationGuideManifestSerializer();
