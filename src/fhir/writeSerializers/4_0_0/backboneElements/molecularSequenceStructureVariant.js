// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../backboneElements/molecularSequenceOuter.js')} */
let MolecularSequenceOuterSerializer;
/** @type {import('../backboneElements/molecularSequenceInner.js')} */
let MolecularSequenceInnerSerializer;

class MolecularSequenceStructureVariantSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        variantType: () => {
            if (!this.#configCache['variantType']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['variantType'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['variantType'];
        },
        exact: null,
        length: null,
        outer: () => {
            if (!this.#configCache['outer']) {
                if (!MolecularSequenceOuterSerializer) {
                    MolecularSequenceOuterSerializer = require('../backboneElements/molecularSequenceOuter.js');
                }
                this.#configCache['outer'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MolecularSequenceOuterSerializer
                };
            }
            return this.#configCache['outer'];
        },
        inner: () => {
            if (!this.#configCache['inner']) {
                if (!MolecularSequenceInnerSerializer) {
                    MolecularSequenceInnerSerializer = require('../backboneElements/molecularSequenceInner.js');
                }
                this.#configCache['inner'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MolecularSequenceInnerSerializer
                };
            }
            return this.#configCache['inner'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new MolecularSequenceStructureVariantSerializer();
