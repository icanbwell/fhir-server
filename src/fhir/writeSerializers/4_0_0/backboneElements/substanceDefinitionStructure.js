// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../backboneElements/substanceDefinitionMolecularWeight.js')} */
let SubstanceDefinitionMolecularWeightSerializer;
/** @type {import('../complexTypes/reference.js')} */
let ReferenceSerializer;
/** @type {import('../backboneElements/substanceDefinitionRepresentation.js')} */
let SubstanceDefinitionRepresentationSerializer;

class SubstanceDefinitionStructureSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        stereochemistry: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['stereochemistry']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['stereochemistry'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['stereochemistry'];
        },
        opticalActivity: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['opticalActivity']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['opticalActivity'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['opticalActivity'];
        },
        molecularFormula: null,
        molecularFormulaByMoiety: null,
        molecularWeight: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['molecularWeight']) {
                if (!SubstanceDefinitionMolecularWeightSerializer) {
                    SubstanceDefinitionMolecularWeightSerializer = require('../backboneElements/substanceDefinitionMolecularWeight.js');
                }
                this.#configCache['molecularWeight'] = {
                    serializeFunction: 'serialize',
                    serializerClass: SubstanceDefinitionMolecularWeightSerializer
                };
            }
            return this.#configCache['molecularWeight'];
        },
        technique: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['technique']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['technique'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['technique'];
        },
        sourceDocument: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['sourceDocument']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['sourceDocument'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['sourceDocument'];
        },
        representation: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['representation']) {
                if (!SubstanceDefinitionRepresentationSerializer) {
                    SubstanceDefinitionRepresentationSerializer = require('../backboneElements/substanceDefinitionRepresentation.js');
                }
                this.#configCache['representation'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: SubstanceDefinitionRepresentationSerializer
                };
            }
            return this.#configCache['representation'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new SubstanceDefinitionStructureSerializer();
