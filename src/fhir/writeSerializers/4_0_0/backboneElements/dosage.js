// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../backboneElements/timing.js')} */
let TimingSerializer;
/** @type {import('../backboneElements/dosageDoseAndRate.js')} */
let DosageDoseAndRateSerializer;
/** @type {import('../complexTypes/ratio.js')} */
let RatioSerializer;
/** @type {import('../complexTypes/quantity.js')} */
let QuantitySerializer;

class DosageSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        sequence: null,
        text: null,
        additionalInstruction: () => {
            if (!this.#configCache['additionalInstruction']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['additionalInstruction'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['additionalInstruction'];
        },
        patientInstruction: null,
        timing: () => {
            if (!this.#configCache['timing']) {
                if (!TimingSerializer) {
                    TimingSerializer = require('../backboneElements/timing.js');
                }
                this.#configCache['timing'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TimingSerializer
                };
            }
            return this.#configCache['timing'];
        },
        asNeededBoolean: null,
        asNeededCodeableConcept: () => {
            if (!this.#configCache['asNeededCodeableConcept']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['asNeededCodeableConcept'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['asNeededCodeableConcept'];
        },
        site: () => {
            if (!this.#configCache['site']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['site'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['site'];
        },
        route: () => {
            if (!this.#configCache['route']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['route'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['route'];
        },
        method: () => {
            if (!this.#configCache['method']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['method'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['method'];
        },
        doseAndRate: () => {
            if (!this.#configCache['doseAndRate']) {
                if (!DosageDoseAndRateSerializer) {
                    DosageDoseAndRateSerializer = require('../backboneElements/dosageDoseAndRate.js');
                }
                this.#configCache['doseAndRate'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: DosageDoseAndRateSerializer
                };
            }
            return this.#configCache['doseAndRate'];
        },
        maxDosePerPeriod: () => {
            if (!this.#configCache['maxDosePerPeriod']) {
                if (!RatioSerializer) {
                    RatioSerializer = require('../complexTypes/ratio.js');
                }
                this.#configCache['maxDosePerPeriod'] = {
                    serializeFunction: 'serialize',
                    serializerClass: RatioSerializer
                };
            }
            return this.#configCache['maxDosePerPeriod'];
        },
        maxDosePerAdministration: () => {
            if (!this.#configCache['maxDosePerAdministration']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['maxDosePerAdministration'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['maxDosePerAdministration'];
        },
        maxDosePerLifetime: () => {
            if (!this.#configCache['maxDosePerLifetime']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['maxDosePerLifetime'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['maxDosePerLifetime'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new DosageSerializer();
