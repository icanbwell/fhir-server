// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/coding.js')} */
let CodingSerializer;
/** @type {import('../backboneElements/questionnaireEnableWhen.js')} */
let QuestionnaireEnableWhenSerializer;
/** @type {import('../backboneElements/questionnaireAnswerOption.js')} */
let QuestionnaireAnswerOptionSerializer;
/** @type {import('../backboneElements/questionnaireInitial.js')} */
let QuestionnaireInitialSerializer;

class QuestionnaireItemSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        linkId: null,
        definition: null,
        code: () => {
            if (!this.#configCache['code']) {
                if (!CodingSerializer) {
                    CodingSerializer = require('../complexTypes/coding.js');
                }
                this.#configCache['code'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodingSerializer
                };
            }
            return this.#configCache['code'];
        },
        prefix: null,
        text: null,
        type: null,
        enableWhen: () => {
            if (!this.#configCache['enableWhen']) {
                if (!QuestionnaireEnableWhenSerializer) {
                    QuestionnaireEnableWhenSerializer = require('../backboneElements/questionnaireEnableWhen.js');
                }
                this.#configCache['enableWhen'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: QuestionnaireEnableWhenSerializer
                };
            }
            return this.#configCache['enableWhen'];
        },
        enableBehavior: null,
        required: null,
        repeats: null,
        readOnly: null,
        maxLength: null,
        answerValueSet: null,
        answerOption: () => {
            if (!this.#configCache['answerOption']) {
                if (!QuestionnaireAnswerOptionSerializer) {
                    QuestionnaireAnswerOptionSerializer = require('../backboneElements/questionnaireAnswerOption.js');
                }
                this.#configCache['answerOption'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: QuestionnaireAnswerOptionSerializer
                };
            }
            return this.#configCache['answerOption'];
        },
        initial: () => {
            if (!this.#configCache['initial']) {
                if (!QuestionnaireInitialSerializer) {
                    QuestionnaireInitialSerializer = require('../backboneElements/questionnaireInitial.js');
                }
                this.#configCache['initial'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: QuestionnaireInitialSerializer
                };
            }
            return this.#configCache['initial'];
        },
        item: () => {
            if (!this.#configCache['item']) {
                this.#configCache['item'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: module.exports // Reference to the current serializer for recursive class
                };
            }
            return this.#configCache['item'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new QuestionnaireItemSerializer();
