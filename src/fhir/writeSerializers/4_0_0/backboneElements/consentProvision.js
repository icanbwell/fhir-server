// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/period.js')} */
let PeriodSerializer;
/** @type {import('../backboneElements/consentActor.js')} */
let ConsentActorSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../complexTypes/coding.js')} */
let CodingSerializer;
/** @type {import('../backboneElements/consentData.js')} */
let ConsentDataSerializer;

class ConsentProvisionSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        type: null,
        period: () => {
            if (!this.#configCache['period']) {
                if (!PeriodSerializer) {
                    PeriodSerializer = require('../complexTypes/period.js');
                }
                this.#configCache['period'] = {
                    serializeFunction: 'serialize',
                    serializerClass: PeriodSerializer
                };
            }
            return this.#configCache['period'];
        },
        actor: () => {
            if (!this.#configCache['actor']) {
                if (!ConsentActorSerializer) {
                    ConsentActorSerializer = require('../backboneElements/consentActor.js');
                }
                this.#configCache['actor'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ConsentActorSerializer
                };
            }
            return this.#configCache['actor'];
        },
        action: () => {
            if (!this.#configCache['action']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['action'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['action'];
        },
        securityLabel: () => {
            if (!this.#configCache['securityLabel']) {
                if (!CodingSerializer) {
                    CodingSerializer = require('../complexTypes/coding.js');
                }
                this.#configCache['securityLabel'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodingSerializer
                };
            }
            return this.#configCache['securityLabel'];
        },
        purpose: () => {
            if (!this.#configCache['purpose']) {
                if (!CodingSerializer) {
                    CodingSerializer = require('../complexTypes/coding.js');
                }
                this.#configCache['purpose'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodingSerializer
                };
            }
            return this.#configCache['purpose'];
        },
        class: () => {
            if (!this.#configCache['class']) {
                if (!CodingSerializer) {
                    CodingSerializer = require('../complexTypes/coding.js');
                }
                this.#configCache['class'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodingSerializer
                };
            }
            return this.#configCache['class'];
        },
        code: () => {
            if (!this.#configCache['code']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['code'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['code'];
        },
        dataPeriod: () => {
            if (!this.#configCache['dataPeriod']) {
                if (!PeriodSerializer) {
                    PeriodSerializer = require('../complexTypes/period.js');
                }
                this.#configCache['dataPeriod'] = {
                    serializeFunction: 'serialize',
                    serializerClass: PeriodSerializer
                };
            }
            return this.#configCache['dataPeriod'];
        },
        data: () => {
            if (!this.#configCache['data']) {
                if (!ConsentDataSerializer) {
                    ConsentDataSerializer = require('../backboneElements/consentData.js');
                }
                this.#configCache['data'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ConsentDataSerializer
                };
            }
            return this.#configCache['data'];
        },
        provision: () => {
            if (!this.#configCache['provision']) {
                this.#configCache['provision'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: module.exports // Reference to the current serializer for recursive class
                };
            }
            return this.#configCache['provision'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new ConsentProvisionSerializer();
