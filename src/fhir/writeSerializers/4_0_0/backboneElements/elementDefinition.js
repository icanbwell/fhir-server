// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/coding.js')} */
let CodingSerializer;
/** @type {import('../backboneElements/elementDefinitionSlicing.js')} */
let ElementDefinitionSlicingSerializer;
/** @type {import('../backboneElements/elementDefinitionBase.js')} */
let ElementDefinitionBaseSerializer;
/** @type {import('../backboneElements/elementDefinitionType.js')} */
let ElementDefinitionTypeSerializer;
/** @type {import('../complexTypes/address.js')} */
let AddressSerializer;
/** @type {import('../complexTypes/quantity.js')} */
let QuantitySerializer;
/** @type {import('../complexTypes/annotation.js')} */
let AnnotationSerializer;
/** @type {import('../complexTypes/attachment.js')} */
let AttachmentSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../complexTypes/codeableReference.js')} */
let CodeableReferenceSerializer;
/** @type {import('../complexTypes/contactPoint.js')} */
let ContactPointSerializer;
/** @type {import('../complexTypes/humanName.js')} */
let HumanNameSerializer;
/** @type {import('../complexTypes/identifier.js')} */
let IdentifierSerializer;
/** @type {import('../complexTypes/money.js')} */
let MoneySerializer;
/** @type {import('../complexTypes/period.js')} */
let PeriodSerializer;
/** @type {import('../complexTypes/range.js')} */
let RangeSerializer;
/** @type {import('../complexTypes/ratio.js')} */
let RatioSerializer;
/** @type {import('../complexTypes/ratioRange.js')} */
let RatioRangeSerializer;
/** @type {import('../complexTypes/reference.js')} */
let ReferenceSerializer;
/** @type {import('../complexTypes/sampledData.js')} */
let SampledDataSerializer;
/** @type {import('../complexTypes/signature.js')} */
let SignatureSerializer;
/** @type {import('../backboneElements/timing.js')} */
let TimingSerializer;
/** @type {import('../complexTypes/contactDetail.js')} */
let ContactDetailSerializer;
/** @type {import('../complexTypes/contributor.js')} */
let ContributorSerializer;
/** @type {import('../complexTypes/dataRequirement.js')} */
let DataRequirementSerializer;
/** @type {import('../complexTypes/expression.js')} */
let ExpressionSerializer;
/** @type {import('../complexTypes/parameterDefinition.js')} */
let ParameterDefinitionSerializer;
/** @type {import('../complexTypes/relatedArtifact.js')} */
let RelatedArtifactSerializer;
/** @type {import('../complexTypes/triggerDefinition.js')} */
let TriggerDefinitionSerializer;
/** @type {import('../complexTypes/usageContext.js')} */
let UsageContextSerializer;
/** @type {import('../backboneElements/dosage.js')} */
let DosageSerializer;
/** @type {import('../backboneElements/elementDefinitionExample.js')} */
let ElementDefinitionExampleSerializer;
/** @type {import('../backboneElements/elementDefinitionConstraint.js')} */
let ElementDefinitionConstraintSerializer;
/** @type {import('../backboneElements/elementDefinitionBinding.js')} */
let ElementDefinitionBindingSerializer;
/** @type {import('../backboneElements/elementDefinitionMapping.js')} */
let ElementDefinitionMappingSerializer;

class ElementDefinitionSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        path: null,
        representation: () => {
            // No serializer class needed for primitive lists
            return {
                serializeFunction: 'serializePrimitiveArray'
            };
        },
        sliceName: null,
        sliceIsConstraining: null,
        label: null,
        code: () => {
            if (!this.#configCache['code']) {
                if (!CodingSerializer) {
                    CodingSerializer = require('../complexTypes/coding.js');
                }
                this.#configCache['code'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodingSerializer
                };
            }
            return this.#configCache['code'];
        },
        slicing: () => {
            if (!this.#configCache['slicing']) {
                if (!ElementDefinitionSlicingSerializer) {
                    ElementDefinitionSlicingSerializer = require('../backboneElements/elementDefinitionSlicing.js');
                }
                this.#configCache['slicing'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ElementDefinitionSlicingSerializer
                };
            }
            return this.#configCache['slicing'];
        },
        short: null,
        definition: null,
        comment: null,
        requirements: null,
        alias: () => {
            // No serializer class needed for primitive lists
            return {
                serializeFunction: 'serializePrimitiveArray'
            };
        },
        min: null,
        max: null,
        base: () => {
            if (!this.#configCache['base']) {
                if (!ElementDefinitionBaseSerializer) {
                    ElementDefinitionBaseSerializer = require('../backboneElements/elementDefinitionBase.js');
                }
                this.#configCache['base'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ElementDefinitionBaseSerializer
                };
            }
            return this.#configCache['base'];
        },
        contentReference: null,
        type: () => {
            if (!this.#configCache['type']) {
                if (!ElementDefinitionTypeSerializer) {
                    ElementDefinitionTypeSerializer = require('../backboneElements/elementDefinitionType.js');
                }
                this.#configCache['type'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ElementDefinitionTypeSerializer
                };
            }
            return this.#configCache['type'];
        },
        defaultValueBase64Binary: null,
        defaultValueBoolean: null,
        defaultValueCanonical: null,
        defaultValueCode: null,
        defaultValueDate: null,
        defaultValueDateTime: null,
        defaultValueDecimal: null,
        defaultValueId: null,
        defaultValueInstant: null,
        defaultValueInteger: null,
        defaultValueMarkdown: null,
        defaultValueOid: null,
        defaultValuePositiveInt: null,
        defaultValueString: null,
        defaultValueTime: null,
        defaultValueUnsignedInt: null,
        defaultValueUri: null,
        defaultValueUrl: null,
        defaultValueUuid: null,
        defaultValueAddress: () => {
            if (!this.#configCache['defaultValueAddress']) {
                if (!AddressSerializer) {
                    AddressSerializer = require('../complexTypes/address.js');
                }
                this.#configCache['defaultValueAddress'] = {
                    serializeFunction: 'serialize',
                    serializerClass: AddressSerializer
                };
            }
            return this.#configCache['defaultValueAddress'];
        },
        defaultValueAge: () => {
            if (!this.#configCache['defaultValueAge']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['defaultValueAge'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['defaultValueAge'];
        },
        defaultValueAnnotation: () => {
            if (!this.#configCache['defaultValueAnnotation']) {
                if (!AnnotationSerializer) {
                    AnnotationSerializer = require('../complexTypes/annotation.js');
                }
                this.#configCache['defaultValueAnnotation'] = {
                    serializeFunction: 'serialize',
                    serializerClass: AnnotationSerializer
                };
            }
            return this.#configCache['defaultValueAnnotation'];
        },
        defaultValueAttachment: () => {
            if (!this.#configCache['defaultValueAttachment']) {
                if (!AttachmentSerializer) {
                    AttachmentSerializer = require('../complexTypes/attachment.js');
                }
                this.#configCache['defaultValueAttachment'] = {
                    serializeFunction: 'serialize',
                    serializerClass: AttachmentSerializer
                };
            }
            return this.#configCache['defaultValueAttachment'];
        },
        defaultValueCodeableConcept: () => {
            if (!this.#configCache['defaultValueCodeableConcept']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['defaultValueCodeableConcept'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['defaultValueCodeableConcept'];
        },
        defaultValueCodeableReference: () => {
            if (!this.#configCache['defaultValueCodeableReference']) {
                if (!CodeableReferenceSerializer) {
                    CodeableReferenceSerializer = require('../complexTypes/codeableReference.js');
                }
                this.#configCache['defaultValueCodeableReference'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableReferenceSerializer
                };
            }
            return this.#configCache['defaultValueCodeableReference'];
        },
        defaultValueCoding: () => {
            if (!this.#configCache['defaultValueCoding']) {
                if (!CodingSerializer) {
                    CodingSerializer = require('../complexTypes/coding.js');
                }
                this.#configCache['defaultValueCoding'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodingSerializer
                };
            }
            return this.#configCache['defaultValueCoding'];
        },
        defaultValueContactPoint: () => {
            if (!this.#configCache['defaultValueContactPoint']) {
                if (!ContactPointSerializer) {
                    ContactPointSerializer = require('../complexTypes/contactPoint.js');
                }
                this.#configCache['defaultValueContactPoint'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ContactPointSerializer
                };
            }
            return this.#configCache['defaultValueContactPoint'];
        },
        defaultValueCount: () => {
            if (!this.#configCache['defaultValueCount']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['defaultValueCount'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['defaultValueCount'];
        },
        defaultValueDistance: () => {
            if (!this.#configCache['defaultValueDistance']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['defaultValueDistance'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['defaultValueDistance'];
        },
        defaultValueDuration: () => {
            if (!this.#configCache['defaultValueDuration']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['defaultValueDuration'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['defaultValueDuration'];
        },
        defaultValueHumanName: () => {
            if (!this.#configCache['defaultValueHumanName']) {
                if (!HumanNameSerializer) {
                    HumanNameSerializer = require('../complexTypes/humanName.js');
                }
                this.#configCache['defaultValueHumanName'] = {
                    serializeFunction: 'serialize',
                    serializerClass: HumanNameSerializer
                };
            }
            return this.#configCache['defaultValueHumanName'];
        },
        defaultValueIdentifier: () => {
            if (!this.#configCache['defaultValueIdentifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['defaultValueIdentifier'] = {
                    serializeFunction: 'serialize',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['defaultValueIdentifier'];
        },
        defaultValueMoney: () => {
            if (!this.#configCache['defaultValueMoney']) {
                if (!MoneySerializer) {
                    MoneySerializer = require('../complexTypes/money.js');
                }
                this.#configCache['defaultValueMoney'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MoneySerializer
                };
            }
            return this.#configCache['defaultValueMoney'];
        },
        defaultValuePeriod: () => {
            if (!this.#configCache['defaultValuePeriod']) {
                if (!PeriodSerializer) {
                    PeriodSerializer = require('../complexTypes/period.js');
                }
                this.#configCache['defaultValuePeriod'] = {
                    serializeFunction: 'serialize',
                    serializerClass: PeriodSerializer
                };
            }
            return this.#configCache['defaultValuePeriod'];
        },
        defaultValueQuantity: () => {
            if (!this.#configCache['defaultValueQuantity']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['defaultValueQuantity'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['defaultValueQuantity'];
        },
        defaultValueRange: () => {
            if (!this.#configCache['defaultValueRange']) {
                if (!RangeSerializer) {
                    RangeSerializer = require('../complexTypes/range.js');
                }
                this.#configCache['defaultValueRange'] = {
                    serializeFunction: 'serialize',
                    serializerClass: RangeSerializer
                };
            }
            return this.#configCache['defaultValueRange'];
        },
        defaultValueRatio: () => {
            if (!this.#configCache['defaultValueRatio']) {
                if (!RatioSerializer) {
                    RatioSerializer = require('../complexTypes/ratio.js');
                }
                this.#configCache['defaultValueRatio'] = {
                    serializeFunction: 'serialize',
                    serializerClass: RatioSerializer
                };
            }
            return this.#configCache['defaultValueRatio'];
        },
        defaultValueRatioRange: () => {
            if (!this.#configCache['defaultValueRatioRange']) {
                if (!RatioRangeSerializer) {
                    RatioRangeSerializer = require('../complexTypes/ratioRange.js');
                }
                this.#configCache['defaultValueRatioRange'] = {
                    serializeFunction: 'serialize',
                    serializerClass: RatioRangeSerializer
                };
            }
            return this.#configCache['defaultValueRatioRange'];
        },
        defaultValueReference: () => {
            if (!this.#configCache['defaultValueReference']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['defaultValueReference'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['defaultValueReference'];
        },
        defaultValueSampledData: () => {
            if (!this.#configCache['defaultValueSampledData']) {
                if (!SampledDataSerializer) {
                    SampledDataSerializer = require('../complexTypes/sampledData.js');
                }
                this.#configCache['defaultValueSampledData'] = {
                    serializeFunction: 'serialize',
                    serializerClass: SampledDataSerializer
                };
            }
            return this.#configCache['defaultValueSampledData'];
        },
        defaultValueSignature: () => {
            if (!this.#configCache['defaultValueSignature']) {
                if (!SignatureSerializer) {
                    SignatureSerializer = require('../complexTypes/signature.js');
                }
                this.#configCache['defaultValueSignature'] = {
                    serializeFunction: 'serialize',
                    serializerClass: SignatureSerializer
                };
            }
            return this.#configCache['defaultValueSignature'];
        },
        defaultValueTiming: () => {
            if (!this.#configCache['defaultValueTiming']) {
                if (!TimingSerializer) {
                    TimingSerializer = require('../backboneElements/timing.js');
                }
                this.#configCache['defaultValueTiming'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TimingSerializer
                };
            }
            return this.#configCache['defaultValueTiming'];
        },
        defaultValueContactDetail: () => {
            if (!this.#configCache['defaultValueContactDetail']) {
                if (!ContactDetailSerializer) {
                    ContactDetailSerializer = require('../complexTypes/contactDetail.js');
                }
                this.#configCache['defaultValueContactDetail'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ContactDetailSerializer
                };
            }
            return this.#configCache['defaultValueContactDetail'];
        },
        defaultValueContributor: () => {
            if (!this.#configCache['defaultValueContributor']) {
                if (!ContributorSerializer) {
                    ContributorSerializer = require('../complexTypes/contributor.js');
                }
                this.#configCache['defaultValueContributor'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ContributorSerializer
                };
            }
            return this.#configCache['defaultValueContributor'];
        },
        defaultValueDataRequirement: () => {
            if (!this.#configCache['defaultValueDataRequirement']) {
                if (!DataRequirementSerializer) {
                    DataRequirementSerializer = require('../complexTypes/dataRequirement.js');
                }
                this.#configCache['defaultValueDataRequirement'] = {
                    serializeFunction: 'serialize',
                    serializerClass: DataRequirementSerializer
                };
            }
            return this.#configCache['defaultValueDataRequirement'];
        },
        defaultValueExpression: () => {
            if (!this.#configCache['defaultValueExpression']) {
                if (!ExpressionSerializer) {
                    ExpressionSerializer = require('../complexTypes/expression.js');
                }
                this.#configCache['defaultValueExpression'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ExpressionSerializer
                };
            }
            return this.#configCache['defaultValueExpression'];
        },
        defaultValueParameterDefinition: () => {
            if (!this.#configCache['defaultValueParameterDefinition']) {
                if (!ParameterDefinitionSerializer) {
                    ParameterDefinitionSerializer = require('../complexTypes/parameterDefinition.js');
                }
                this.#configCache['defaultValueParameterDefinition'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ParameterDefinitionSerializer
                };
            }
            return this.#configCache['defaultValueParameterDefinition'];
        },
        defaultValueRelatedArtifact: () => {
            if (!this.#configCache['defaultValueRelatedArtifact']) {
                if (!RelatedArtifactSerializer) {
                    RelatedArtifactSerializer = require('../complexTypes/relatedArtifact.js');
                }
                this.#configCache['defaultValueRelatedArtifact'] = {
                    serializeFunction: 'serialize',
                    serializerClass: RelatedArtifactSerializer
                };
            }
            return this.#configCache['defaultValueRelatedArtifact'];
        },
        defaultValueTriggerDefinition: () => {
            if (!this.#configCache['defaultValueTriggerDefinition']) {
                if (!TriggerDefinitionSerializer) {
                    TriggerDefinitionSerializer = require('../complexTypes/triggerDefinition.js');
                }
                this.#configCache['defaultValueTriggerDefinition'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TriggerDefinitionSerializer
                };
            }
            return this.#configCache['defaultValueTriggerDefinition'];
        },
        defaultValueUsageContext: () => {
            if (!this.#configCache['defaultValueUsageContext']) {
                if (!UsageContextSerializer) {
                    UsageContextSerializer = require('../complexTypes/usageContext.js');
                }
                this.#configCache['defaultValueUsageContext'] = {
                    serializeFunction: 'serialize',
                    serializerClass: UsageContextSerializer
                };
            }
            return this.#configCache['defaultValueUsageContext'];
        },
        defaultValueDosage: () => {
            if (!this.#configCache['defaultValueDosage']) {
                if (!DosageSerializer) {
                    DosageSerializer = require('../backboneElements/dosage.js');
                }
                this.#configCache['defaultValueDosage'] = {
                    serializeFunction: 'serialize',
                    serializerClass: DosageSerializer
                };
            }
            return this.#configCache['defaultValueDosage'];
        },
        meaningWhenMissing: null,
        orderMeaning: null,
        fixedBase64Binary: null,
        fixedBoolean: null,
        fixedCanonical: null,
        fixedCode: null,
        fixedDate: null,
        fixedDateTime: null,
        fixedDecimal: null,
        fixedId: null,
        fixedInstant: null,
        fixedInteger: null,
        fixedMarkdown: null,
        fixedOid: null,
        fixedPositiveInt: null,
        fixedString: null,
        fixedTime: null,
        fixedUnsignedInt: null,
        fixedUri: null,
        fixedUrl: null,
        fixedUuid: null,
        fixedAddress: () => {
            if (!this.#configCache['fixedAddress']) {
                if (!AddressSerializer) {
                    AddressSerializer = require('../complexTypes/address.js');
                }
                this.#configCache['fixedAddress'] = {
                    serializeFunction: 'serialize',
                    serializerClass: AddressSerializer
                };
            }
            return this.#configCache['fixedAddress'];
        },
        fixedAge: () => {
            if (!this.#configCache['fixedAge']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['fixedAge'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['fixedAge'];
        },
        fixedAnnotation: () => {
            if (!this.#configCache['fixedAnnotation']) {
                if (!AnnotationSerializer) {
                    AnnotationSerializer = require('../complexTypes/annotation.js');
                }
                this.#configCache['fixedAnnotation'] = {
                    serializeFunction: 'serialize',
                    serializerClass: AnnotationSerializer
                };
            }
            return this.#configCache['fixedAnnotation'];
        },
        fixedAttachment: () => {
            if (!this.#configCache['fixedAttachment']) {
                if (!AttachmentSerializer) {
                    AttachmentSerializer = require('../complexTypes/attachment.js');
                }
                this.#configCache['fixedAttachment'] = {
                    serializeFunction: 'serialize',
                    serializerClass: AttachmentSerializer
                };
            }
            return this.#configCache['fixedAttachment'];
        },
        fixedCodeableConcept: () => {
            if (!this.#configCache['fixedCodeableConcept']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['fixedCodeableConcept'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['fixedCodeableConcept'];
        },
        fixedCodeableReference: () => {
            if (!this.#configCache['fixedCodeableReference']) {
                if (!CodeableReferenceSerializer) {
                    CodeableReferenceSerializer = require('../complexTypes/codeableReference.js');
                }
                this.#configCache['fixedCodeableReference'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableReferenceSerializer
                };
            }
            return this.#configCache['fixedCodeableReference'];
        },
        fixedCoding: () => {
            if (!this.#configCache['fixedCoding']) {
                if (!CodingSerializer) {
                    CodingSerializer = require('../complexTypes/coding.js');
                }
                this.#configCache['fixedCoding'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodingSerializer
                };
            }
            return this.#configCache['fixedCoding'];
        },
        fixedContactPoint: () => {
            if (!this.#configCache['fixedContactPoint']) {
                if (!ContactPointSerializer) {
                    ContactPointSerializer = require('../complexTypes/contactPoint.js');
                }
                this.#configCache['fixedContactPoint'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ContactPointSerializer
                };
            }
            return this.#configCache['fixedContactPoint'];
        },
        fixedCount: () => {
            if (!this.#configCache['fixedCount']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['fixedCount'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['fixedCount'];
        },
        fixedDistance: () => {
            if (!this.#configCache['fixedDistance']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['fixedDistance'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['fixedDistance'];
        },
        fixedDuration: () => {
            if (!this.#configCache['fixedDuration']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['fixedDuration'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['fixedDuration'];
        },
        fixedHumanName: () => {
            if (!this.#configCache['fixedHumanName']) {
                if (!HumanNameSerializer) {
                    HumanNameSerializer = require('../complexTypes/humanName.js');
                }
                this.#configCache['fixedHumanName'] = {
                    serializeFunction: 'serialize',
                    serializerClass: HumanNameSerializer
                };
            }
            return this.#configCache['fixedHumanName'];
        },
        fixedIdentifier: () => {
            if (!this.#configCache['fixedIdentifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['fixedIdentifier'] = {
                    serializeFunction: 'serialize',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['fixedIdentifier'];
        },
        fixedMoney: () => {
            if (!this.#configCache['fixedMoney']) {
                if (!MoneySerializer) {
                    MoneySerializer = require('../complexTypes/money.js');
                }
                this.#configCache['fixedMoney'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MoneySerializer
                };
            }
            return this.#configCache['fixedMoney'];
        },
        fixedPeriod: () => {
            if (!this.#configCache['fixedPeriod']) {
                if (!PeriodSerializer) {
                    PeriodSerializer = require('../complexTypes/period.js');
                }
                this.#configCache['fixedPeriod'] = {
                    serializeFunction: 'serialize',
                    serializerClass: PeriodSerializer
                };
            }
            return this.#configCache['fixedPeriod'];
        },
        fixedQuantity: () => {
            if (!this.#configCache['fixedQuantity']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['fixedQuantity'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['fixedQuantity'];
        },
        fixedRange: () => {
            if (!this.#configCache['fixedRange']) {
                if (!RangeSerializer) {
                    RangeSerializer = require('../complexTypes/range.js');
                }
                this.#configCache['fixedRange'] = {
                    serializeFunction: 'serialize',
                    serializerClass: RangeSerializer
                };
            }
            return this.#configCache['fixedRange'];
        },
        fixedRatio: () => {
            if (!this.#configCache['fixedRatio']) {
                if (!RatioSerializer) {
                    RatioSerializer = require('../complexTypes/ratio.js');
                }
                this.#configCache['fixedRatio'] = {
                    serializeFunction: 'serialize',
                    serializerClass: RatioSerializer
                };
            }
            return this.#configCache['fixedRatio'];
        },
        fixedRatioRange: () => {
            if (!this.#configCache['fixedRatioRange']) {
                if (!RatioRangeSerializer) {
                    RatioRangeSerializer = require('../complexTypes/ratioRange.js');
                }
                this.#configCache['fixedRatioRange'] = {
                    serializeFunction: 'serialize',
                    serializerClass: RatioRangeSerializer
                };
            }
            return this.#configCache['fixedRatioRange'];
        },
        fixedReference: () => {
            if (!this.#configCache['fixedReference']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['fixedReference'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['fixedReference'];
        },
        fixedSampledData: () => {
            if (!this.#configCache['fixedSampledData']) {
                if (!SampledDataSerializer) {
                    SampledDataSerializer = require('../complexTypes/sampledData.js');
                }
                this.#configCache['fixedSampledData'] = {
                    serializeFunction: 'serialize',
                    serializerClass: SampledDataSerializer
                };
            }
            return this.#configCache['fixedSampledData'];
        },
        fixedSignature: () => {
            if (!this.#configCache['fixedSignature']) {
                if (!SignatureSerializer) {
                    SignatureSerializer = require('../complexTypes/signature.js');
                }
                this.#configCache['fixedSignature'] = {
                    serializeFunction: 'serialize',
                    serializerClass: SignatureSerializer
                };
            }
            return this.#configCache['fixedSignature'];
        },
        fixedTiming: () => {
            if (!this.#configCache['fixedTiming']) {
                if (!TimingSerializer) {
                    TimingSerializer = require('../backboneElements/timing.js');
                }
                this.#configCache['fixedTiming'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TimingSerializer
                };
            }
            return this.#configCache['fixedTiming'];
        },
        fixedContactDetail: () => {
            if (!this.#configCache['fixedContactDetail']) {
                if (!ContactDetailSerializer) {
                    ContactDetailSerializer = require('../complexTypes/contactDetail.js');
                }
                this.#configCache['fixedContactDetail'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ContactDetailSerializer
                };
            }
            return this.#configCache['fixedContactDetail'];
        },
        fixedContributor: () => {
            if (!this.#configCache['fixedContributor']) {
                if (!ContributorSerializer) {
                    ContributorSerializer = require('../complexTypes/contributor.js');
                }
                this.#configCache['fixedContributor'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ContributorSerializer
                };
            }
            return this.#configCache['fixedContributor'];
        },
        fixedDataRequirement: () => {
            if (!this.#configCache['fixedDataRequirement']) {
                if (!DataRequirementSerializer) {
                    DataRequirementSerializer = require('../complexTypes/dataRequirement.js');
                }
                this.#configCache['fixedDataRequirement'] = {
                    serializeFunction: 'serialize',
                    serializerClass: DataRequirementSerializer
                };
            }
            return this.#configCache['fixedDataRequirement'];
        },
        fixedExpression: () => {
            if (!this.#configCache['fixedExpression']) {
                if (!ExpressionSerializer) {
                    ExpressionSerializer = require('../complexTypes/expression.js');
                }
                this.#configCache['fixedExpression'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ExpressionSerializer
                };
            }
            return this.#configCache['fixedExpression'];
        },
        fixedParameterDefinition: () => {
            if (!this.#configCache['fixedParameterDefinition']) {
                if (!ParameterDefinitionSerializer) {
                    ParameterDefinitionSerializer = require('../complexTypes/parameterDefinition.js');
                }
                this.#configCache['fixedParameterDefinition'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ParameterDefinitionSerializer
                };
            }
            return this.#configCache['fixedParameterDefinition'];
        },
        fixedRelatedArtifact: () => {
            if (!this.#configCache['fixedRelatedArtifact']) {
                if (!RelatedArtifactSerializer) {
                    RelatedArtifactSerializer = require('../complexTypes/relatedArtifact.js');
                }
                this.#configCache['fixedRelatedArtifact'] = {
                    serializeFunction: 'serialize',
                    serializerClass: RelatedArtifactSerializer
                };
            }
            return this.#configCache['fixedRelatedArtifact'];
        },
        fixedTriggerDefinition: () => {
            if (!this.#configCache['fixedTriggerDefinition']) {
                if (!TriggerDefinitionSerializer) {
                    TriggerDefinitionSerializer = require('../complexTypes/triggerDefinition.js');
                }
                this.#configCache['fixedTriggerDefinition'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TriggerDefinitionSerializer
                };
            }
            return this.#configCache['fixedTriggerDefinition'];
        },
        fixedUsageContext: () => {
            if (!this.#configCache['fixedUsageContext']) {
                if (!UsageContextSerializer) {
                    UsageContextSerializer = require('../complexTypes/usageContext.js');
                }
                this.#configCache['fixedUsageContext'] = {
                    serializeFunction: 'serialize',
                    serializerClass: UsageContextSerializer
                };
            }
            return this.#configCache['fixedUsageContext'];
        },
        fixedDosage: () => {
            if (!this.#configCache['fixedDosage']) {
                if (!DosageSerializer) {
                    DosageSerializer = require('../backboneElements/dosage.js');
                }
                this.#configCache['fixedDosage'] = {
                    serializeFunction: 'serialize',
                    serializerClass: DosageSerializer
                };
            }
            return this.#configCache['fixedDosage'];
        },
        patternBase64Binary: null,
        patternBoolean: null,
        patternCanonical: null,
        patternCode: null,
        patternDate: null,
        patternDateTime: null,
        patternDecimal: null,
        patternId: null,
        patternInstant: null,
        patternInteger: null,
        patternMarkdown: null,
        patternOid: null,
        patternPositiveInt: null,
        patternString: null,
        patternTime: null,
        patternUnsignedInt: null,
        patternUri: null,
        patternUrl: null,
        patternUuid: null,
        patternAddress: () => {
            if (!this.#configCache['patternAddress']) {
                if (!AddressSerializer) {
                    AddressSerializer = require('../complexTypes/address.js');
                }
                this.#configCache['patternAddress'] = {
                    serializeFunction: 'serialize',
                    serializerClass: AddressSerializer
                };
            }
            return this.#configCache['patternAddress'];
        },
        patternAge: () => {
            if (!this.#configCache['patternAge']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['patternAge'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['patternAge'];
        },
        patternAnnotation: () => {
            if (!this.#configCache['patternAnnotation']) {
                if (!AnnotationSerializer) {
                    AnnotationSerializer = require('../complexTypes/annotation.js');
                }
                this.#configCache['patternAnnotation'] = {
                    serializeFunction: 'serialize',
                    serializerClass: AnnotationSerializer
                };
            }
            return this.#configCache['patternAnnotation'];
        },
        patternAttachment: () => {
            if (!this.#configCache['patternAttachment']) {
                if (!AttachmentSerializer) {
                    AttachmentSerializer = require('../complexTypes/attachment.js');
                }
                this.#configCache['patternAttachment'] = {
                    serializeFunction: 'serialize',
                    serializerClass: AttachmentSerializer
                };
            }
            return this.#configCache['patternAttachment'];
        },
        patternCodeableConcept: () => {
            if (!this.#configCache['patternCodeableConcept']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['patternCodeableConcept'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['patternCodeableConcept'];
        },
        patternCodeableReference: () => {
            if (!this.#configCache['patternCodeableReference']) {
                if (!CodeableReferenceSerializer) {
                    CodeableReferenceSerializer = require('../complexTypes/codeableReference.js');
                }
                this.#configCache['patternCodeableReference'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableReferenceSerializer
                };
            }
            return this.#configCache['patternCodeableReference'];
        },
        patternCoding: () => {
            if (!this.#configCache['patternCoding']) {
                if (!CodingSerializer) {
                    CodingSerializer = require('../complexTypes/coding.js');
                }
                this.#configCache['patternCoding'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodingSerializer
                };
            }
            return this.#configCache['patternCoding'];
        },
        patternContactPoint: () => {
            if (!this.#configCache['patternContactPoint']) {
                if (!ContactPointSerializer) {
                    ContactPointSerializer = require('../complexTypes/contactPoint.js');
                }
                this.#configCache['patternContactPoint'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ContactPointSerializer
                };
            }
            return this.#configCache['patternContactPoint'];
        },
        patternCount: () => {
            if (!this.#configCache['patternCount']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['patternCount'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['patternCount'];
        },
        patternDistance: () => {
            if (!this.#configCache['patternDistance']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['patternDistance'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['patternDistance'];
        },
        patternDuration: () => {
            if (!this.#configCache['patternDuration']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['patternDuration'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['patternDuration'];
        },
        patternHumanName: () => {
            if (!this.#configCache['patternHumanName']) {
                if (!HumanNameSerializer) {
                    HumanNameSerializer = require('../complexTypes/humanName.js');
                }
                this.#configCache['patternHumanName'] = {
                    serializeFunction: 'serialize',
                    serializerClass: HumanNameSerializer
                };
            }
            return this.#configCache['patternHumanName'];
        },
        patternIdentifier: () => {
            if (!this.#configCache['patternIdentifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['patternIdentifier'] = {
                    serializeFunction: 'serialize',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['patternIdentifier'];
        },
        patternMoney: () => {
            if (!this.#configCache['patternMoney']) {
                if (!MoneySerializer) {
                    MoneySerializer = require('../complexTypes/money.js');
                }
                this.#configCache['patternMoney'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MoneySerializer
                };
            }
            return this.#configCache['patternMoney'];
        },
        patternPeriod: () => {
            if (!this.#configCache['patternPeriod']) {
                if (!PeriodSerializer) {
                    PeriodSerializer = require('../complexTypes/period.js');
                }
                this.#configCache['patternPeriod'] = {
                    serializeFunction: 'serialize',
                    serializerClass: PeriodSerializer
                };
            }
            return this.#configCache['patternPeriod'];
        },
        patternQuantity: () => {
            if (!this.#configCache['patternQuantity']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['patternQuantity'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['patternQuantity'];
        },
        patternRange: () => {
            if (!this.#configCache['patternRange']) {
                if (!RangeSerializer) {
                    RangeSerializer = require('../complexTypes/range.js');
                }
                this.#configCache['patternRange'] = {
                    serializeFunction: 'serialize',
                    serializerClass: RangeSerializer
                };
            }
            return this.#configCache['patternRange'];
        },
        patternRatio: () => {
            if (!this.#configCache['patternRatio']) {
                if (!RatioSerializer) {
                    RatioSerializer = require('../complexTypes/ratio.js');
                }
                this.#configCache['patternRatio'] = {
                    serializeFunction: 'serialize',
                    serializerClass: RatioSerializer
                };
            }
            return this.#configCache['patternRatio'];
        },
        patternRatioRange: () => {
            if (!this.#configCache['patternRatioRange']) {
                if (!RatioRangeSerializer) {
                    RatioRangeSerializer = require('../complexTypes/ratioRange.js');
                }
                this.#configCache['patternRatioRange'] = {
                    serializeFunction: 'serialize',
                    serializerClass: RatioRangeSerializer
                };
            }
            return this.#configCache['patternRatioRange'];
        },
        patternReference: () => {
            if (!this.#configCache['patternReference']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['patternReference'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['patternReference'];
        },
        patternSampledData: () => {
            if (!this.#configCache['patternSampledData']) {
                if (!SampledDataSerializer) {
                    SampledDataSerializer = require('../complexTypes/sampledData.js');
                }
                this.#configCache['patternSampledData'] = {
                    serializeFunction: 'serialize',
                    serializerClass: SampledDataSerializer
                };
            }
            return this.#configCache['patternSampledData'];
        },
        patternSignature: () => {
            if (!this.#configCache['patternSignature']) {
                if (!SignatureSerializer) {
                    SignatureSerializer = require('../complexTypes/signature.js');
                }
                this.#configCache['patternSignature'] = {
                    serializeFunction: 'serialize',
                    serializerClass: SignatureSerializer
                };
            }
            return this.#configCache['patternSignature'];
        },
        patternTiming: () => {
            if (!this.#configCache['patternTiming']) {
                if (!TimingSerializer) {
                    TimingSerializer = require('../backboneElements/timing.js');
                }
                this.#configCache['patternTiming'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TimingSerializer
                };
            }
            return this.#configCache['patternTiming'];
        },
        patternContactDetail: () => {
            if (!this.#configCache['patternContactDetail']) {
                if (!ContactDetailSerializer) {
                    ContactDetailSerializer = require('../complexTypes/contactDetail.js');
                }
                this.#configCache['patternContactDetail'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ContactDetailSerializer
                };
            }
            return this.#configCache['patternContactDetail'];
        },
        patternContributor: () => {
            if (!this.#configCache['patternContributor']) {
                if (!ContributorSerializer) {
                    ContributorSerializer = require('../complexTypes/contributor.js');
                }
                this.#configCache['patternContributor'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ContributorSerializer
                };
            }
            return this.#configCache['patternContributor'];
        },
        patternDataRequirement: () => {
            if (!this.#configCache['patternDataRequirement']) {
                if (!DataRequirementSerializer) {
                    DataRequirementSerializer = require('../complexTypes/dataRequirement.js');
                }
                this.#configCache['patternDataRequirement'] = {
                    serializeFunction: 'serialize',
                    serializerClass: DataRequirementSerializer
                };
            }
            return this.#configCache['patternDataRequirement'];
        },
        patternExpression: () => {
            if (!this.#configCache['patternExpression']) {
                if (!ExpressionSerializer) {
                    ExpressionSerializer = require('../complexTypes/expression.js');
                }
                this.#configCache['patternExpression'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ExpressionSerializer
                };
            }
            return this.#configCache['patternExpression'];
        },
        patternParameterDefinition: () => {
            if (!this.#configCache['patternParameterDefinition']) {
                if (!ParameterDefinitionSerializer) {
                    ParameterDefinitionSerializer = require('../complexTypes/parameterDefinition.js');
                }
                this.#configCache['patternParameterDefinition'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ParameterDefinitionSerializer
                };
            }
            return this.#configCache['patternParameterDefinition'];
        },
        patternRelatedArtifact: () => {
            if (!this.#configCache['patternRelatedArtifact']) {
                if (!RelatedArtifactSerializer) {
                    RelatedArtifactSerializer = require('../complexTypes/relatedArtifact.js');
                }
                this.#configCache['patternRelatedArtifact'] = {
                    serializeFunction: 'serialize',
                    serializerClass: RelatedArtifactSerializer
                };
            }
            return this.#configCache['patternRelatedArtifact'];
        },
        patternTriggerDefinition: () => {
            if (!this.#configCache['patternTriggerDefinition']) {
                if (!TriggerDefinitionSerializer) {
                    TriggerDefinitionSerializer = require('../complexTypes/triggerDefinition.js');
                }
                this.#configCache['patternTriggerDefinition'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TriggerDefinitionSerializer
                };
            }
            return this.#configCache['patternTriggerDefinition'];
        },
        patternUsageContext: () => {
            if (!this.#configCache['patternUsageContext']) {
                if (!UsageContextSerializer) {
                    UsageContextSerializer = require('../complexTypes/usageContext.js');
                }
                this.#configCache['patternUsageContext'] = {
                    serializeFunction: 'serialize',
                    serializerClass: UsageContextSerializer
                };
            }
            return this.#configCache['patternUsageContext'];
        },
        patternDosage: () => {
            if (!this.#configCache['patternDosage']) {
                if (!DosageSerializer) {
                    DosageSerializer = require('../backboneElements/dosage.js');
                }
                this.#configCache['patternDosage'] = {
                    serializeFunction: 'serialize',
                    serializerClass: DosageSerializer
                };
            }
            return this.#configCache['patternDosage'];
        },
        example: () => {
            if (!this.#configCache['example']) {
                if (!ElementDefinitionExampleSerializer) {
                    ElementDefinitionExampleSerializer = require('../backboneElements/elementDefinitionExample.js');
                }
                this.#configCache['example'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ElementDefinitionExampleSerializer
                };
            }
            return this.#configCache['example'];
        },
        minValueDate: null,
        minValueDateTime: null,
        minValueInstant: null,
        minValueTime: null,
        minValueDecimal: null,
        minValueInteger: null,
        minValuePositiveInt: null,
        minValueUnsignedInt: null,
        minValueQuantity: () => {
            if (!this.#configCache['minValueQuantity']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['minValueQuantity'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['minValueQuantity'];
        },
        maxValueDate: null,
        maxValueDateTime: null,
        maxValueInstant: null,
        maxValueTime: null,
        maxValueDecimal: null,
        maxValueInteger: null,
        maxValuePositiveInt: null,
        maxValueUnsignedInt: null,
        maxValueQuantity: () => {
            if (!this.#configCache['maxValueQuantity']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['maxValueQuantity'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['maxValueQuantity'];
        },
        maxLength: null,
        condition: () => {
            // No serializer class needed for primitive lists
            return {
                serializeFunction: 'serializePrimitiveArray'
            };
        },
        constraint: () => {
            if (!this.#configCache['constraint']) {
                if (!ElementDefinitionConstraintSerializer) {
                    ElementDefinitionConstraintSerializer = require('../backboneElements/elementDefinitionConstraint.js');
                }
                this.#configCache['constraint'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ElementDefinitionConstraintSerializer
                };
            }
            return this.#configCache['constraint'];
        },
        mustSupport: null,
        isModifier: null,
        isModifierReason: null,
        isSummary: null,
        binding: () => {
            if (!this.#configCache['binding']) {
                if (!ElementDefinitionBindingSerializer) {
                    ElementDefinitionBindingSerializer = require('../backboneElements/elementDefinitionBinding.js');
                }
                this.#configCache['binding'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ElementDefinitionBindingSerializer
                };
            }
            return this.#configCache['binding'];
        },
        mapping: () => {
            if (!this.#configCache['mapping']) {
                if (!ElementDefinitionMappingSerializer) {
                    ElementDefinitionMappingSerializer = require('../backboneElements/elementDefinitionMapping.js');
                }
                this.#configCache['mapping'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ElementDefinitionMappingSerializer
                };
            }
            return this.#configCache['mapping'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new ElementDefinitionSerializer();
