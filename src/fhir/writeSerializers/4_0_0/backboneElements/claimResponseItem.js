// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../backboneElements/claimResponseAdjudication.js')} */
let ClaimResponseAdjudicationSerializer;
/** @type {import('../backboneElements/claimResponseDetail.js')} */
let ClaimResponseDetailSerializer;

class ClaimResponseItemSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        itemSequence: null,
        noteNumber: null,
        adjudication: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['adjudication']) {
                if (!ClaimResponseAdjudicationSerializer) {
                    ClaimResponseAdjudicationSerializer = require('../backboneElements/claimResponseAdjudication.js');
                }
                this.#configCache['adjudication'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ClaimResponseAdjudicationSerializer
                };
            }
            return this.#configCache['adjudication'];
        },
        detail: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['detail']) {
                if (!ClaimResponseDetailSerializer) {
                    ClaimResponseDetailSerializer = require('../backboneElements/claimResponseDetail.js');
                }
                this.#configCache['detail'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ClaimResponseDetailSerializer
                };
            }
            return this.#configCache['detail'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new ClaimResponseItemSerializer();
