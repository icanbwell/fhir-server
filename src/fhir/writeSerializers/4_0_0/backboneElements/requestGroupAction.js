// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../complexTypes/relatedArtifact.js')} */
let RelatedArtifactSerializer;
/** @type {import('../backboneElements/requestGroupCondition.js')} */
let RequestGroupConditionSerializer;
/** @type {import('../backboneElements/requestGroupRelatedAction.js')} */
let RequestGroupRelatedActionSerializer;
/** @type {import('../complexTypes/quantity.js')} */
let QuantitySerializer;
/** @type {import('../complexTypes/period.js')} */
let PeriodSerializer;
/** @type {import('../complexTypes/range.js')} */
let RangeSerializer;
/** @type {import('../backboneElements/timing.js')} */
let TimingSerializer;
/** @type {import('../complexTypes/reference.js')} */
let ReferenceSerializer;

class RequestGroupActionSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        prefix: null,
        title: null,
        description: null,
        textEquivalent: null,
        priority: null,
        code: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['code']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['code'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['code'];
        },
        documentation: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['documentation']) {
                if (!RelatedArtifactSerializer) {
                    RelatedArtifactSerializer = require('../complexTypes/relatedArtifact.js');
                }
                this.#configCache['documentation'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: RelatedArtifactSerializer
                };
            }
            return this.#configCache['documentation'];
        },
        condition: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['condition']) {
                if (!RequestGroupConditionSerializer) {
                    RequestGroupConditionSerializer = require('../backboneElements/requestGroupCondition.js');
                }
                this.#configCache['condition'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: RequestGroupConditionSerializer
                };
            }
            return this.#configCache['condition'];
        },
        relatedAction: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['relatedAction']) {
                if (!RequestGroupRelatedActionSerializer) {
                    RequestGroupRelatedActionSerializer = require('../backboneElements/requestGroupRelatedAction.js');
                }
                this.#configCache['relatedAction'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: RequestGroupRelatedActionSerializer
                };
            }
            return this.#configCache['relatedAction'];
        },
        timingDateTime: null,
        timingAge: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['timingAge']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['timingAge'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['timingAge'];
        },
        timingPeriod: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['timingPeriod']) {
                if (!PeriodSerializer) {
                    PeriodSerializer = require('../complexTypes/period.js');
                }
                this.#configCache['timingPeriod'] = {
                    serializeFunction: 'serialize',
                    serializerClass: PeriodSerializer
                };
            }
            return this.#configCache['timingPeriod'];
        },
        timingDuration: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['timingDuration']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['timingDuration'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['timingDuration'];
        },
        timingRange: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['timingRange']) {
                if (!RangeSerializer) {
                    RangeSerializer = require('../complexTypes/range.js');
                }
                this.#configCache['timingRange'] = {
                    serializeFunction: 'serialize',
                    serializerClass: RangeSerializer
                };
            }
            return this.#configCache['timingRange'];
        },
        timingTiming: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['timingTiming']) {
                if (!TimingSerializer) {
                    TimingSerializer = require('../backboneElements/timing.js');
                }
                this.#configCache['timingTiming'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TimingSerializer
                };
            }
            return this.#configCache['timingTiming'];
        },
        participant: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['participant']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['participant'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['participant'];
        },
        type: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['type']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['type'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['type'];
        },
        groupingBehavior: null,
        selectionBehavior: null,
        requiredBehavior: null,
        precheckBehavior: null,
        cardinalityBehavior: null,
        resource: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['resource']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['resource'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['resource'];
        },
        action: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['action']) {
                if (!RequestGroupActionSerializer) {
                    RequestGroupActionSerializer = require('');
                }
                this.#configCache['action'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: RequestGroupActionSerializer
                };
            }
            return this.#configCache['action'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new RequestGroupActionSerializer();
