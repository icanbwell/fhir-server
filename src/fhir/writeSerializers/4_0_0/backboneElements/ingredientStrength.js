// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/ratio.js')} */
let RatioSerializer;
/** @type {import('../complexTypes/ratioRange.js')} */
let RatioRangeSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../backboneElements/ingredientReferenceStrength.js')} */
let IngredientReferenceStrengthSerializer;

class IngredientStrengthSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        presentationRatio: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['presentationRatio']) {
                if (!RatioSerializer) {
                    RatioSerializer = require('../complexTypes/ratio.js');
                }
                this.#configCache['presentationRatio'] = {
                    serializeFunction: 'serialize',
                    serializerClass: RatioSerializer
                };
            }
            return this.#configCache['presentationRatio'];
        },
        presentationRatioRange: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['presentationRatioRange']) {
                if (!RatioRangeSerializer) {
                    RatioRangeSerializer = require('../complexTypes/ratioRange.js');
                }
                this.#configCache['presentationRatioRange'] = {
                    serializeFunction: 'serialize',
                    serializerClass: RatioRangeSerializer
                };
            }
            return this.#configCache['presentationRatioRange'];
        },
        textPresentation: null,
        concentrationRatio: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['concentrationRatio']) {
                if (!RatioSerializer) {
                    RatioSerializer = require('../complexTypes/ratio.js');
                }
                this.#configCache['concentrationRatio'] = {
                    serializeFunction: 'serialize',
                    serializerClass: RatioSerializer
                };
            }
            return this.#configCache['concentrationRatio'];
        },
        concentrationRatioRange: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['concentrationRatioRange']) {
                if (!RatioRangeSerializer) {
                    RatioRangeSerializer = require('../complexTypes/ratioRange.js');
                }
                this.#configCache['concentrationRatioRange'] = {
                    serializeFunction: 'serialize',
                    serializerClass: RatioRangeSerializer
                };
            }
            return this.#configCache['concentrationRatioRange'];
        },
        textConcentration: null,
        measurementPoint: null,
        country: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['country']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['country'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['country'];
        },
        referenceStrength: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['referenceStrength']) {
                if (!IngredientReferenceStrengthSerializer) {
                    IngredientReferenceStrengthSerializer = require('../backboneElements/ingredientReferenceStrength.js');
                }
                this.#configCache['referenceStrength'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IngredientReferenceStrengthSerializer
                };
            }
            return this.#configCache['referenceStrength'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new IngredientStrengthSerializer();
