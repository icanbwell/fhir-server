// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/reference.js')} */
let ReferenceSerializer;
/** @type {import('../backboneElements/medicationKnowledgeSubstitution.js')} */
let MedicationKnowledgeSubstitutionSerializer;
/** @type {import('../backboneElements/medicationKnowledgeSchedule.js')} */
let MedicationKnowledgeScheduleSerializer;
/** @type {import('../backboneElements/medicationKnowledgeMaxDispense.js')} */
let MedicationKnowledgeMaxDispenseSerializer;

class MedicationKnowledgeRegulatorySerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        regulatoryAuthority: () => {
            if (!this.#configCache['regulatoryAuthority']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['regulatoryAuthority'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['regulatoryAuthority'];
        },
        substitution: () => {
            if (!this.#configCache['substitution']) {
                if (!MedicationKnowledgeSubstitutionSerializer) {
                    MedicationKnowledgeSubstitutionSerializer = require('../backboneElements/medicationKnowledgeSubstitution.js');
                }
                this.#configCache['substitution'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicationKnowledgeSubstitutionSerializer
                };
            }
            return this.#configCache['substitution'];
        },
        schedule: () => {
            if (!this.#configCache['schedule']) {
                if (!MedicationKnowledgeScheduleSerializer) {
                    MedicationKnowledgeScheduleSerializer = require('../backboneElements/medicationKnowledgeSchedule.js');
                }
                this.#configCache['schedule'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicationKnowledgeScheduleSerializer
                };
            }
            return this.#configCache['schedule'];
        },
        maxDispense: () => {
            if (!this.#configCache['maxDispense']) {
                if (!MedicationKnowledgeMaxDispenseSerializer) {
                    MedicationKnowledgeMaxDispenseSerializer = require('../backboneElements/medicationKnowledgeMaxDispense.js');
                }
                this.#configCache['maxDispense'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MedicationKnowledgeMaxDispenseSerializer
                };
            }
            return this.#configCache['maxDispense'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new MedicationKnowledgeRegulatorySerializer();
