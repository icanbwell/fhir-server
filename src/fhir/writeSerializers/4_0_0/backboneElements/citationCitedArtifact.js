// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/identifier.js')} */
let IdentifierSerializer;
/** @type {import('../backboneElements/citationVersion.js')} */
let CitationVersionSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../backboneElements/citationStatusDate1.js')} */
let CitationStatusDate1Serializer;
/** @type {import('../backboneElements/citationTitle.js')} */
let CitationTitleSerializer;
/** @type {import('../backboneElements/citationAbstract.js')} */
let CitationAbstractSerializer;
/** @type {import('../backboneElements/citationPart.js')} */
let CitationPartSerializer;
/** @type {import('../backboneElements/citationRelatesTo1.js')} */
let CitationRelatesTo1Serializer;
/** @type {import('../backboneElements/citationPublicationForm.js')} */
let CitationPublicationFormSerializer;
/** @type {import('../backboneElements/citationWebLocation.js')} */
let CitationWebLocationSerializer;
/** @type {import('../backboneElements/citationClassification1.js')} */
let CitationClassification1Serializer;
/** @type {import('../backboneElements/citationContributorship.js')} */
let CitationContributorshipSerializer;
/** @type {import('../complexTypes/annotation.js')} */
let AnnotationSerializer;

class CitationCitedArtifactSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        relatedIdentifier: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['relatedIdentifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['relatedIdentifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['relatedIdentifier'];
        },
        dateAccessed: null,
        version: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['version']) {
                if (!CitationVersionSerializer) {
                    CitationVersionSerializer = require('../backboneElements/citationVersion.js');
                }
                this.#configCache['version'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CitationVersionSerializer
                };
            }
            return this.#configCache['version'];
        },
        currentState: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['currentState']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['currentState'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['currentState'];
        },
        statusDate: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['statusDate']) {
                if (!CitationStatusDate1Serializer) {
                    CitationStatusDate1Serializer = require('../backboneElements/citationStatusDate1.js');
                }
                this.#configCache['statusDate'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CitationStatusDate1Serializer
                };
            }
            return this.#configCache['statusDate'];
        },
        title: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['title']) {
                if (!CitationTitleSerializer) {
                    CitationTitleSerializer = require('../backboneElements/citationTitle.js');
                }
                this.#configCache['title'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CitationTitleSerializer
                };
            }
            return this.#configCache['title'];
        },
        abstract: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['abstract']) {
                if (!CitationAbstractSerializer) {
                    CitationAbstractSerializer = require('../backboneElements/citationAbstract.js');
                }
                this.#configCache['abstract'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CitationAbstractSerializer
                };
            }
            return this.#configCache['abstract'];
        },
        part: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['part']) {
                if (!CitationPartSerializer) {
                    CitationPartSerializer = require('../backboneElements/citationPart.js');
                }
                this.#configCache['part'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CitationPartSerializer
                };
            }
            return this.#configCache['part'];
        },
        relatesTo: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['relatesTo']) {
                if (!CitationRelatesTo1Serializer) {
                    CitationRelatesTo1Serializer = require('../backboneElements/citationRelatesTo1.js');
                }
                this.#configCache['relatesTo'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CitationRelatesTo1Serializer
                };
            }
            return this.#configCache['relatesTo'];
        },
        publicationForm: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['publicationForm']) {
                if (!CitationPublicationFormSerializer) {
                    CitationPublicationFormSerializer = require('../backboneElements/citationPublicationForm.js');
                }
                this.#configCache['publicationForm'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CitationPublicationFormSerializer
                };
            }
            return this.#configCache['publicationForm'];
        },
        webLocation: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['webLocation']) {
                if (!CitationWebLocationSerializer) {
                    CitationWebLocationSerializer = require('../backboneElements/citationWebLocation.js');
                }
                this.#configCache['webLocation'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CitationWebLocationSerializer
                };
            }
            return this.#configCache['webLocation'];
        },
        classification: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['classification']) {
                if (!CitationClassification1Serializer) {
                    CitationClassification1Serializer = require('../backboneElements/citationClassification1.js');
                }
                this.#configCache['classification'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CitationClassification1Serializer
                };
            }
            return this.#configCache['classification'];
        },
        contributorship: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contributorship']) {
                if (!CitationContributorshipSerializer) {
                    CitationContributorshipSerializer = require('../backboneElements/citationContributorship.js');
                }
                this.#configCache['contributorship'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CitationContributorshipSerializer
                };
            }
            return this.#configCache['contributorship'];
        },
        note: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['note']) {
                if (!AnnotationSerializer) {
                    AnnotationSerializer = require('../complexTypes/annotation.js');
                }
                this.#configCache['note'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: AnnotationSerializer
                };
            }
            return this.#configCache['note'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new CitationCitedArtifactSerializer();
