// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../backboneElements/timing.js')} */
let TimingSerializer;
/** @type {import('../backboneElements/nutritionOrderNutrient.js')} */
let NutritionOrderNutrientSerializer;
/** @type {import('../backboneElements/nutritionOrderTexture.js')} */
let NutritionOrderTextureSerializer;

class NutritionOrderOralDietSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        type: () => {
            if (!this.#configCache['type']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['type'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['type'];
        },
        schedule: () => {
            if (!this.#configCache['schedule']) {
                if (!TimingSerializer) {
                    TimingSerializer = require('../backboneElements/timing.js');
                }
                this.#configCache['schedule'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: TimingSerializer
                };
            }
            return this.#configCache['schedule'];
        },
        nutrient: () => {
            if (!this.#configCache['nutrient']) {
                if (!NutritionOrderNutrientSerializer) {
                    NutritionOrderNutrientSerializer = require('../backboneElements/nutritionOrderNutrient.js');
                }
                this.#configCache['nutrient'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: NutritionOrderNutrientSerializer
                };
            }
            return this.#configCache['nutrient'];
        },
        texture: () => {
            if (!this.#configCache['texture']) {
                if (!NutritionOrderTextureSerializer) {
                    NutritionOrderTextureSerializer = require('../backboneElements/nutritionOrderTexture.js');
                }
                this.#configCache['texture'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: NutritionOrderTextureSerializer
                };
            }
            return this.#configCache['texture'];
        },
        fluidConsistencyType: () => {
            if (!this.#configCache['fluidConsistencyType']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['fluidConsistencyType'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['fluidConsistencyType'];
        },
        instruction: null
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new NutritionOrderOralDietSerializer();
