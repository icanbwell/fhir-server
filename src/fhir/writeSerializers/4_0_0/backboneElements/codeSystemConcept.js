// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../backboneElements/codeSystemDesignation.js')} */
let CodeSystemDesignationSerializer;
/** @type {import('../backboneElements/codeSystemProperty1.js')} */
let CodeSystemProperty1Serializer;

class CodeSystemConceptSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        code: null,
        display: null,
        definition: null,
        designation: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['designation']) {
                if (!CodeSystemDesignationSerializer) {
                    CodeSystemDesignationSerializer = require('../backboneElements/codeSystemDesignation.js');
                }
                this.#configCache['designation'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeSystemDesignationSerializer
                };
            }
            return this.#configCache['designation'];
        },
        property: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['property']) {
                if (!CodeSystemProperty1Serializer) {
                    CodeSystemProperty1Serializer = require('../backboneElements/codeSystemProperty1.js');
                }
                this.#configCache['property'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeSystemProperty1Serializer
                };
            }
            return this.#configCache['property'];
        },
        concept: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['concept']) {
                this.#configCache['concept'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeSystemConceptSerializer
                };
            }
            return this.#configCache['concept'];
        }
    };

    allPropertyToSerializerMap = this.fhirPropertyToSerializerMap;
}

module.exports = new CodeSystemConceptSerializer();
