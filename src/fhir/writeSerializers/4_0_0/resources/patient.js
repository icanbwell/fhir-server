// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/meta.js')} */
let MetaSerializer;
/** @type {import('../complexTypes/narrative.js')} */
let NarrativeSerializer;
/** @type {import('../customSerializers/resourceContainer.js')} */
let ResourceContainerSerializer;
/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/identifier.js')} */
let IdentifierSerializer;
/** @type {import('../complexTypes/humanName.js')} */
let HumanNameSerializer;
/** @type {import('../complexTypes/contactPoint.js')} */
let ContactPointSerializer;
/** @type {import('../complexTypes/address.js')} */
let AddressSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../complexTypes/attachment.js')} */
let AttachmentSerializer;
/** @type {import('../backboneElements/patientContact.js')} */
let PatientContactSerializer;
/** @type {import('../backboneElements/patientCommunication.js')} */
let PatientCommunicationSerializer;
/** @type {import('../complexTypes/reference.js')} */
let ReferenceSerializer;
/** @type {import('../backboneElements/patientLink.js')} */
let PatientLinkSerializer;

class PatientSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        meta: () => {
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: () => {
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        active: null,
        name: () => {
            if (!this.#configCache['name']) {
                if (!HumanNameSerializer) {
                    HumanNameSerializer = require('../complexTypes/humanName.js');
                }
                this.#configCache['name'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: HumanNameSerializer
                };
            }
            return this.#configCache['name'];
        },
        telecom: () => {
            if (!this.#configCache['telecom']) {
                if (!ContactPointSerializer) {
                    ContactPointSerializer = require('../complexTypes/contactPoint.js');
                }
                this.#configCache['telecom'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ContactPointSerializer
                };
            }
            return this.#configCache['telecom'];
        },
        gender: null,
        birthDate: null,
        deceasedBoolean: null,
        deceasedDateTime: null,
        address: () => {
            if (!this.#configCache['address']) {
                if (!AddressSerializer) {
                    AddressSerializer = require('../complexTypes/address.js');
                }
                this.#configCache['address'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: AddressSerializer
                };
            }
            return this.#configCache['address'];
        },
        maritalStatus: () => {
            if (!this.#configCache['maritalStatus']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['maritalStatus'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['maritalStatus'];
        },
        multipleBirthBoolean: null,
        multipleBirthInteger: null,
        photo: () => {
            if (!this.#configCache['photo']) {
                if (!AttachmentSerializer) {
                    AttachmentSerializer = require('../complexTypes/attachment.js');
                }
                this.#configCache['photo'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: AttachmentSerializer
                };
            }
            return this.#configCache['photo'];
        },
        contact: () => {
            if (!this.#configCache['contact']) {
                if (!PatientContactSerializer) {
                    PatientContactSerializer = require('../backboneElements/patientContact.js');
                }
                this.#configCache['contact'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: PatientContactSerializer
                };
            }
            return this.#configCache['contact'];
        },
        communication: () => {
            if (!this.#configCache['communication']) {
                if (!PatientCommunicationSerializer) {
                    PatientCommunicationSerializer = require('../backboneElements/patientCommunication.js');
                }
                this.#configCache['communication'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: PatientCommunicationSerializer
                };
            }
            return this.#configCache['communication'];
        },
        generalPractitioner: () => {
            if (!this.#configCache['generalPractitioner']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['generalPractitioner'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['generalPractitioner'];
        },
        managingOrganization: () => {
            if (!this.#configCache['managingOrganization']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['managingOrganization'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['managingOrganization'];
        },
        link: () => {
            if (!this.#configCache['link']) {
                if (!PatientLinkSerializer) {
                    PatientLinkSerializer = require('../backboneElements/patientLink.js');
                }
                this.#configCache['link'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: PatientLinkSerializer
                };
            }
            return this.#configCache['link'];
        },
        resourceType: null
    };

    allPropertyToSerializerMap = {
        id: null,
        meta: () => {
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: () => {
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        active: null,
        name: () => {
            if (!this.#configCache['name']) {
                if (!HumanNameSerializer) {
                    HumanNameSerializer = require('../complexTypes/humanName.js');
                }
                this.#configCache['name'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: HumanNameSerializer
                };
            }
            return this.#configCache['name'];
        },
        telecom: () => {
            if (!this.#configCache['telecom']) {
                if (!ContactPointSerializer) {
                    ContactPointSerializer = require('../complexTypes/contactPoint.js');
                }
                this.#configCache['telecom'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ContactPointSerializer
                };
            }
            return this.#configCache['telecom'];
        },
        gender: null,
        birthDate: null,
        deceasedBoolean: null,
        deceasedDateTime: null,
        address: () => {
            if (!this.#configCache['address']) {
                if (!AddressSerializer) {
                    AddressSerializer = require('../complexTypes/address.js');
                }
                this.#configCache['address'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: AddressSerializer
                };
            }
            return this.#configCache['address'];
        },
        maritalStatus: () => {
            if (!this.#configCache['maritalStatus']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['maritalStatus'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['maritalStatus'];
        },
        multipleBirthBoolean: null,
        multipleBirthInteger: null,
        photo: () => {
            if (!this.#configCache['photo']) {
                if (!AttachmentSerializer) {
                    AttachmentSerializer = require('../complexTypes/attachment.js');
                }
                this.#configCache['photo'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: AttachmentSerializer
                };
            }
            return this.#configCache['photo'];
        },
        contact: () => {
            if (!this.#configCache['contact']) {
                if (!PatientContactSerializer) {
                    PatientContactSerializer = require('../backboneElements/patientContact.js');
                }
                this.#configCache['contact'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: PatientContactSerializer
                };
            }
            return this.#configCache['contact'];
        },
        communication: () => {
            if (!this.#configCache['communication']) {
                if (!PatientCommunicationSerializer) {
                    PatientCommunicationSerializer = require('../backboneElements/patientCommunication.js');
                }
                this.#configCache['communication'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: PatientCommunicationSerializer
                };
            }
            return this.#configCache['communication'];
        },
        generalPractitioner: () => {
            if (!this.#configCache['generalPractitioner']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['generalPractitioner'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['generalPractitioner'];
        },
        managingOrganization: () => {
            if (!this.#configCache['managingOrganization']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['managingOrganization'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['managingOrganization'];
        },
        link: () => {
            if (!this.#configCache['link']) {
                if (!PatientLinkSerializer) {
                    PatientLinkSerializer = require('../backboneElements/patientLink.js');
                }
                this.#configCache['link'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: PatientLinkSerializer
                };
            }
            return this.#configCache['link'];
        },
        resourceType: null,
        _access: null,
        _sourceAssigningAuthority: null,
        _uuid: null,
        _sourceId: null
    };
}

module.exports = new PatientSerializer();
