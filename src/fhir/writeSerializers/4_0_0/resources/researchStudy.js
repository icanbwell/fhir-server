// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/meta.js')} */
let MetaSerializer;
/** @type {import('../complexTypes/narrative.js')} */
let NarrativeSerializer;
/** @type {import('../customSerializers/resourceContainer.js')} */
let ResourceContainerSerializer;
/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/identifier.js')} */
let IdentifierSerializer;
/** @type {import('../complexTypes/reference.js')} */
let ReferenceSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../complexTypes/contactDetail.js')} */
let ContactDetailSerializer;
/** @type {import('../complexTypes/relatedArtifact.js')} */
let RelatedArtifactSerializer;
/** @type {import('../complexTypes/period.js')} */
let PeriodSerializer;
/** @type {import('../complexTypes/annotation.js')} */
let AnnotationSerializer;
/** @type {import('../backboneElements/researchStudyArm.js')} */
let ResearchStudyArmSerializer;
/** @type {import('../backboneElements/researchStudyObjective.js')} */
let ResearchStudyObjectiveSerializer;

class ResearchStudySerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        meta: () => {
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: () => {
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        title: null,
        protocol: () => {
            if (!this.#configCache['protocol']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['protocol'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['protocol'];
        },
        partOf: () => {
            if (!this.#configCache['partOf']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['partOf'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['partOf'];
        },
        status: null,
        primaryPurposeType: () => {
            if (!this.#configCache['primaryPurposeType']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['primaryPurposeType'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['primaryPurposeType'];
        },
        phase: () => {
            if (!this.#configCache['phase']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['phase'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['phase'];
        },
        category: () => {
            if (!this.#configCache['category']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['category'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['category'];
        },
        focus: () => {
            if (!this.#configCache['focus']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['focus'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['focus'];
        },
        condition: () => {
            if (!this.#configCache['condition']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['condition'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['condition'];
        },
        contact: () => {
            if (!this.#configCache['contact']) {
                if (!ContactDetailSerializer) {
                    ContactDetailSerializer = require('../complexTypes/contactDetail.js');
                }
                this.#configCache['contact'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ContactDetailSerializer
                };
            }
            return this.#configCache['contact'];
        },
        relatedArtifact: () => {
            if (!this.#configCache['relatedArtifact']) {
                if (!RelatedArtifactSerializer) {
                    RelatedArtifactSerializer = require('../complexTypes/relatedArtifact.js');
                }
                this.#configCache['relatedArtifact'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: RelatedArtifactSerializer
                };
            }
            return this.#configCache['relatedArtifact'];
        },
        keyword: () => {
            if (!this.#configCache['keyword']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['keyword'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['keyword'];
        },
        location: () => {
            if (!this.#configCache['location']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['location'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['location'];
        },
        description: null,
        enrollment: () => {
            if (!this.#configCache['enrollment']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['enrollment'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['enrollment'];
        },
        period: () => {
            if (!this.#configCache['period']) {
                if (!PeriodSerializer) {
                    PeriodSerializer = require('../complexTypes/period.js');
                }
                this.#configCache['period'] = {
                    serializeFunction: 'serialize',
                    serializerClass: PeriodSerializer
                };
            }
            return this.#configCache['period'];
        },
        sponsor: () => {
            if (!this.#configCache['sponsor']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['sponsor'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['sponsor'];
        },
        principalInvestigator: () => {
            if (!this.#configCache['principalInvestigator']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['principalInvestigator'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['principalInvestigator'];
        },
        site: () => {
            if (!this.#configCache['site']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['site'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['site'];
        },
        reasonStopped: () => {
            if (!this.#configCache['reasonStopped']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['reasonStopped'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['reasonStopped'];
        },
        note: () => {
            if (!this.#configCache['note']) {
                if (!AnnotationSerializer) {
                    AnnotationSerializer = require('../complexTypes/annotation.js');
                }
                this.#configCache['note'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: AnnotationSerializer
                };
            }
            return this.#configCache['note'];
        },
        arm: () => {
            if (!this.#configCache['arm']) {
                if (!ResearchStudyArmSerializer) {
                    ResearchStudyArmSerializer = require('../backboneElements/researchStudyArm.js');
                }
                this.#configCache['arm'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ResearchStudyArmSerializer
                };
            }
            return this.#configCache['arm'];
        },
        objective: () => {
            if (!this.#configCache['objective']) {
                if (!ResearchStudyObjectiveSerializer) {
                    ResearchStudyObjectiveSerializer = require('../backboneElements/researchStudyObjective.js');
                }
                this.#configCache['objective'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ResearchStudyObjectiveSerializer
                };
            }
            return this.#configCache['objective'];
        },
        resourceType: null
    };

    allPropertyToSerializerMap = {
        id: null,
        meta: () => {
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: () => {
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        title: null,
        protocol: () => {
            if (!this.#configCache['protocol']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['protocol'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['protocol'];
        },
        partOf: () => {
            if (!this.#configCache['partOf']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['partOf'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['partOf'];
        },
        status: null,
        primaryPurposeType: () => {
            if (!this.#configCache['primaryPurposeType']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['primaryPurposeType'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['primaryPurposeType'];
        },
        phase: () => {
            if (!this.#configCache['phase']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['phase'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['phase'];
        },
        category: () => {
            if (!this.#configCache['category']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['category'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['category'];
        },
        focus: () => {
            if (!this.#configCache['focus']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['focus'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['focus'];
        },
        condition: () => {
            if (!this.#configCache['condition']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['condition'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['condition'];
        },
        contact: () => {
            if (!this.#configCache['contact']) {
                if (!ContactDetailSerializer) {
                    ContactDetailSerializer = require('../complexTypes/contactDetail.js');
                }
                this.#configCache['contact'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ContactDetailSerializer
                };
            }
            return this.#configCache['contact'];
        },
        relatedArtifact: () => {
            if (!this.#configCache['relatedArtifact']) {
                if (!RelatedArtifactSerializer) {
                    RelatedArtifactSerializer = require('../complexTypes/relatedArtifact.js');
                }
                this.#configCache['relatedArtifact'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: RelatedArtifactSerializer
                };
            }
            return this.#configCache['relatedArtifact'];
        },
        keyword: () => {
            if (!this.#configCache['keyword']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['keyword'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['keyword'];
        },
        location: () => {
            if (!this.#configCache['location']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['location'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['location'];
        },
        description: null,
        enrollment: () => {
            if (!this.#configCache['enrollment']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['enrollment'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['enrollment'];
        },
        period: () => {
            if (!this.#configCache['period']) {
                if (!PeriodSerializer) {
                    PeriodSerializer = require('../complexTypes/period.js');
                }
                this.#configCache['period'] = {
                    serializeFunction: 'serialize',
                    serializerClass: PeriodSerializer
                };
            }
            return this.#configCache['period'];
        },
        sponsor: () => {
            if (!this.#configCache['sponsor']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['sponsor'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['sponsor'];
        },
        principalInvestigator: () => {
            if (!this.#configCache['principalInvestigator']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['principalInvestigator'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['principalInvestigator'];
        },
        site: () => {
            if (!this.#configCache['site']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['site'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['site'];
        },
        reasonStopped: () => {
            if (!this.#configCache['reasonStopped']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['reasonStopped'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['reasonStopped'];
        },
        note: () => {
            if (!this.#configCache['note']) {
                if (!AnnotationSerializer) {
                    AnnotationSerializer = require('../complexTypes/annotation.js');
                }
                this.#configCache['note'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: AnnotationSerializer
                };
            }
            return this.#configCache['note'];
        },
        arm: () => {
            if (!this.#configCache['arm']) {
                if (!ResearchStudyArmSerializer) {
                    ResearchStudyArmSerializer = require('../backboneElements/researchStudyArm.js');
                }
                this.#configCache['arm'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ResearchStudyArmSerializer
                };
            }
            return this.#configCache['arm'];
        },
        objective: () => {
            if (!this.#configCache['objective']) {
                if (!ResearchStudyObjectiveSerializer) {
                    ResearchStudyObjectiveSerializer = require('../backboneElements/researchStudyObjective.js');
                }
                this.#configCache['objective'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ResearchStudyObjectiveSerializer
                };
            }
            return this.#configCache['objective'];
        },
        resourceType: null,
        _access: null,
        _sourceAssigningAuthority: null,
        _uuid: null,
        _sourceId: null
    };
}

module.exports = new ResearchStudySerializer();
