// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/meta.js')} */
let MetaSerializer;
/** @type {import('../complexTypes/narrative.js')} */
let NarrativeSerializer;
/** @type {import('../customSerializers/resourceContainer.js')} */
let ResourceContainerSerializer;
/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/identifier.js')} */
let IdentifierSerializer;
/** @type {import('../complexTypes/reference.js')} */
let ReferenceSerializer;
/** @type {import('../complexTypes/quantity.js')} */
let QuantitySerializer;
/** @type {import('../backboneElements/molecularSequenceReferenceSeq.js')} */
let MolecularSequenceReferenceSeqSerializer;
/** @type {import('../backboneElements/molecularSequenceVariant.js')} */
let MolecularSequenceVariantSerializer;
/** @type {import('../backboneElements/molecularSequenceQuality.js')} */
let MolecularSequenceQualitySerializer;
/** @type {import('../backboneElements/molecularSequenceRepository.js')} */
let MolecularSequenceRepositorySerializer;
/** @type {import('../backboneElements/molecularSequenceStructureVariant.js')} */
let MolecularSequenceStructureVariantSerializer;

class MolecularSequenceSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        meta: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        type: null,
        coordinateSystem: null,
        patient: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['patient']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['patient'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['patient'];
        },
        specimen: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['specimen']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['specimen'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['specimen'];
        },
        device: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['device']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['device'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['device'];
        },
        performer: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['performer']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['performer'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['performer'];
        },
        quantity: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['quantity']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['quantity'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['quantity'];
        },
        referenceSeq: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['referenceSeq']) {
                if (!MolecularSequenceReferenceSeqSerializer) {
                    MolecularSequenceReferenceSeqSerializer = require('../backboneElements/molecularSequenceReferenceSeq.js');
                }
                this.#configCache['referenceSeq'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MolecularSequenceReferenceSeqSerializer
                };
            }
            return this.#configCache['referenceSeq'];
        },
        variant: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['variant']) {
                if (!MolecularSequenceVariantSerializer) {
                    MolecularSequenceVariantSerializer = require('../backboneElements/molecularSequenceVariant.js');
                }
                this.#configCache['variant'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MolecularSequenceVariantSerializer
                };
            }
            return this.#configCache['variant'];
        },
        observedSeq: null,
        quality: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['quality']) {
                if (!MolecularSequenceQualitySerializer) {
                    MolecularSequenceQualitySerializer = require('../backboneElements/molecularSequenceQuality.js');
                }
                this.#configCache['quality'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MolecularSequenceQualitySerializer
                };
            }
            return this.#configCache['quality'];
        },
        readCoverage: null,
        repository: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['repository']) {
                if (!MolecularSequenceRepositorySerializer) {
                    MolecularSequenceRepositorySerializer = require('../backboneElements/molecularSequenceRepository.js');
                }
                this.#configCache['repository'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MolecularSequenceRepositorySerializer
                };
            }
            return this.#configCache['repository'];
        },
        pointer: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['pointer']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['pointer'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['pointer'];
        },
        structureVariant: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['structureVariant']) {
                if (!MolecularSequenceStructureVariantSerializer) {
                    MolecularSequenceStructureVariantSerializer = require('../backboneElements/molecularSequenceStructureVariant.js');
                }
                this.#configCache['structureVariant'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MolecularSequenceStructureVariantSerializer
                };
            }
            return this.#configCache['structureVariant'];
        },
        resourceType: null
    };

    allPropertyToSerializerMap = {
        id: null,
        meta: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        type: null,
        coordinateSystem: null,
        patient: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['patient']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['patient'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['patient'];
        },
        specimen: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['specimen']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['specimen'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['specimen'];
        },
        device: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['device']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['device'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['device'];
        },
        performer: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['performer']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['performer'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['performer'];
        },
        quantity: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['quantity']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['quantity'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['quantity'];
        },
        referenceSeq: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['referenceSeq']) {
                if (!MolecularSequenceReferenceSeqSerializer) {
                    MolecularSequenceReferenceSeqSerializer = require('../backboneElements/molecularSequenceReferenceSeq.js');
                }
                this.#configCache['referenceSeq'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MolecularSequenceReferenceSeqSerializer
                };
            }
            return this.#configCache['referenceSeq'];
        },
        variant: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['variant']) {
                if (!MolecularSequenceVariantSerializer) {
                    MolecularSequenceVariantSerializer = require('../backboneElements/molecularSequenceVariant.js');
                }
                this.#configCache['variant'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MolecularSequenceVariantSerializer
                };
            }
            return this.#configCache['variant'];
        },
        observedSeq: null,
        quality: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['quality']) {
                if (!MolecularSequenceQualitySerializer) {
                    MolecularSequenceQualitySerializer = require('../backboneElements/molecularSequenceQuality.js');
                }
                this.#configCache['quality'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MolecularSequenceQualitySerializer
                };
            }
            return this.#configCache['quality'];
        },
        readCoverage: null,
        repository: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['repository']) {
                if (!MolecularSequenceRepositorySerializer) {
                    MolecularSequenceRepositorySerializer = require('../backboneElements/molecularSequenceRepository.js');
                }
                this.#configCache['repository'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MolecularSequenceRepositorySerializer
                };
            }
            return this.#configCache['repository'];
        },
        pointer: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['pointer']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['pointer'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['pointer'];
        },
        structureVariant: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['structureVariant']) {
                if (!MolecularSequenceStructureVariantSerializer) {
                    MolecularSequenceStructureVariantSerializer = require('../backboneElements/molecularSequenceStructureVariant.js');
                }
                this.#configCache['structureVariant'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MolecularSequenceStructureVariantSerializer
                };
            }
            return this.#configCache['structureVariant'];
        },
        resourceType: null,
        _access: null,
        _sourceAssigningAuthority: null,
        _uuid: null,
        _sourceId: null
    };
}

module.exports = new MolecularSequenceSerializer();
