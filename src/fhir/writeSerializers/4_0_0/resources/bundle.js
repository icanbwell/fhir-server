// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/meta.js')} */
let MetaSerializer;
/** @type {import('../complexTypes/identifier.js')} */
let IdentifierSerializer;
/** @type {import('../backboneElements/bundleLink.js')} */
let BundleLinkSerializer;
/** @type {import('../backboneElements/bundleEntry.js')} */
let BundleEntrySerializer;
/** @type {import('../complexTypes/signature.js')} */
let SignatureSerializer;

class BundleSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        meta: () => {
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        identifier: () => {
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serialize',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        type: null,
        timestamp: null,
        total: null,
        link: () => {
            if (!this.#configCache['link']) {
                if (!BundleLinkSerializer) {
                    BundleLinkSerializer = require('../backboneElements/bundleLink.js');
                }
                this.#configCache['link'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: BundleLinkSerializer
                };
            }
            return this.#configCache['link'];
        },
        entry: () => {
            if (!this.#configCache['entry']) {
                if (!BundleEntrySerializer) {
                    BundleEntrySerializer = require('../backboneElements/bundleEntry.js');
                }
                this.#configCache['entry'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: BundleEntrySerializer
                };
            }
            return this.#configCache['entry'];
        },
        signature: () => {
            if (!this.#configCache['signature']) {
                if (!SignatureSerializer) {
                    SignatureSerializer = require('../complexTypes/signature.js');
                }
                this.#configCache['signature'] = {
                    serializeFunction: 'serialize',
                    serializerClass: SignatureSerializer
                };
            }
            return this.#configCache['signature'];
        },
        resourceType: null
    };

    allPropertyToSerializerMap = {
        id: null,
        meta: () => {
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        identifier: () => {
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serialize',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        type: null,
        timestamp: null,
        total: null,
        link: () => {
            if (!this.#configCache['link']) {
                if (!BundleLinkSerializer) {
                    BundleLinkSerializer = require('../backboneElements/bundleLink.js');
                }
                this.#configCache['link'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: BundleLinkSerializer
                };
            }
            return this.#configCache['link'];
        },
        entry: () => {
            if (!this.#configCache['entry']) {
                if (!BundleEntrySerializer) {
                    BundleEntrySerializer = require('../backboneElements/bundleEntry.js');
                }
                this.#configCache['entry'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: BundleEntrySerializer
                };
            }
            return this.#configCache['entry'];
        },
        signature: () => {
            if (!this.#configCache['signature']) {
                if (!SignatureSerializer) {
                    SignatureSerializer = require('../complexTypes/signature.js');
                }
                this.#configCache['signature'] = {
                    serializeFunction: 'serialize',
                    serializerClass: SignatureSerializer
                };
            }
            return this.#configCache['signature'];
        },
        resourceType: null,
        _access: null,
        _sourceAssigningAuthority: null,
        _uuid: null,
        _sourceId: null
    };
}

module.exports = new BundleSerializer();
