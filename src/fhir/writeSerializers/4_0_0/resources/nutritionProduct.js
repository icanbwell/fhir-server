// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/meta.js')} */
let MetaSerializer;
/** @type {import('../complexTypes/narrative.js')} */
let NarrativeSerializer;
/** @type {import('../customSerializers/resourceContainer.js')} */
let ResourceContainerSerializer;
/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../complexTypes/reference.js')} */
let ReferenceSerializer;
/** @type {import('../backboneElements/nutritionProductNutrient.js')} */
let NutritionProductNutrientSerializer;
/** @type {import('../backboneElements/nutritionProductIngredient.js')} */
let NutritionProductIngredientSerializer;
/** @type {import('../complexTypes/codeableReference.js')} */
let CodeableReferenceSerializer;
/** @type {import('../backboneElements/nutritionProductProductCharacteristic.js')} */
let NutritionProductProductCharacteristicSerializer;
/** @type {import('../backboneElements/nutritionProductInstance.js')} */
let NutritionProductInstanceSerializer;
/** @type {import('../complexTypes/annotation.js')} */
let AnnotationSerializer;

class NutritionProductSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        meta: () => {
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        status: null,
        category: () => {
            if (!this.#configCache['category']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['category'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['category'];
        },
        code: () => {
            if (!this.#configCache['code']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['code'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['code'];
        },
        manufacturer: () => {
            if (!this.#configCache['manufacturer']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['manufacturer'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['manufacturer'];
        },
        nutrient: () => {
            if (!this.#configCache['nutrient']) {
                if (!NutritionProductNutrientSerializer) {
                    NutritionProductNutrientSerializer = require('../backboneElements/nutritionProductNutrient.js');
                }
                this.#configCache['nutrient'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: NutritionProductNutrientSerializer
                };
            }
            return this.#configCache['nutrient'];
        },
        ingredient: () => {
            if (!this.#configCache['ingredient']) {
                if (!NutritionProductIngredientSerializer) {
                    NutritionProductIngredientSerializer = require('../backboneElements/nutritionProductIngredient.js');
                }
                this.#configCache['ingredient'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: NutritionProductIngredientSerializer
                };
            }
            return this.#configCache['ingredient'];
        },
        knownAllergen: () => {
            if (!this.#configCache['knownAllergen']) {
                if (!CodeableReferenceSerializer) {
                    CodeableReferenceSerializer = require('../complexTypes/codeableReference.js');
                }
                this.#configCache['knownAllergen'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableReferenceSerializer
                };
            }
            return this.#configCache['knownAllergen'];
        },
        productCharacteristic: () => {
            if (!this.#configCache['productCharacteristic']) {
                if (!NutritionProductProductCharacteristicSerializer) {
                    NutritionProductProductCharacteristicSerializer = require('../backboneElements/nutritionProductProductCharacteristic.js');
                }
                this.#configCache['productCharacteristic'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: NutritionProductProductCharacteristicSerializer
                };
            }
            return this.#configCache['productCharacteristic'];
        },
        instance: () => {
            if (!this.#configCache['instance']) {
                if (!NutritionProductInstanceSerializer) {
                    NutritionProductInstanceSerializer = require('../backboneElements/nutritionProductInstance.js');
                }
                this.#configCache['instance'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NutritionProductInstanceSerializer
                };
            }
            return this.#configCache['instance'];
        },
        note: () => {
            if (!this.#configCache['note']) {
                if (!AnnotationSerializer) {
                    AnnotationSerializer = require('../complexTypes/annotation.js');
                }
                this.#configCache['note'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: AnnotationSerializer
                };
            }
            return this.#configCache['note'];
        },
        resourceType: null
    };

    allPropertyToSerializerMap = {
        id: null,
        meta: () => {
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        status: null,
        category: () => {
            if (!this.#configCache['category']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['category'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['category'];
        },
        code: () => {
            if (!this.#configCache['code']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['code'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['code'];
        },
        manufacturer: () => {
            if (!this.#configCache['manufacturer']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['manufacturer'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['manufacturer'];
        },
        nutrient: () => {
            if (!this.#configCache['nutrient']) {
                if (!NutritionProductNutrientSerializer) {
                    NutritionProductNutrientSerializer = require('../backboneElements/nutritionProductNutrient.js');
                }
                this.#configCache['nutrient'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: NutritionProductNutrientSerializer
                };
            }
            return this.#configCache['nutrient'];
        },
        ingredient: () => {
            if (!this.#configCache['ingredient']) {
                if (!NutritionProductIngredientSerializer) {
                    NutritionProductIngredientSerializer = require('../backboneElements/nutritionProductIngredient.js');
                }
                this.#configCache['ingredient'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: NutritionProductIngredientSerializer
                };
            }
            return this.#configCache['ingredient'];
        },
        knownAllergen: () => {
            if (!this.#configCache['knownAllergen']) {
                if (!CodeableReferenceSerializer) {
                    CodeableReferenceSerializer = require('../complexTypes/codeableReference.js');
                }
                this.#configCache['knownAllergen'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableReferenceSerializer
                };
            }
            return this.#configCache['knownAllergen'];
        },
        productCharacteristic: () => {
            if (!this.#configCache['productCharacteristic']) {
                if (!NutritionProductProductCharacteristicSerializer) {
                    NutritionProductProductCharacteristicSerializer = require('../backboneElements/nutritionProductProductCharacteristic.js');
                }
                this.#configCache['productCharacteristic'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: NutritionProductProductCharacteristicSerializer
                };
            }
            return this.#configCache['productCharacteristic'];
        },
        instance: () => {
            if (!this.#configCache['instance']) {
                if (!NutritionProductInstanceSerializer) {
                    NutritionProductInstanceSerializer = require('../backboneElements/nutritionProductInstance.js');
                }
                this.#configCache['instance'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NutritionProductInstanceSerializer
                };
            }
            return this.#configCache['instance'];
        },
        note: () => {
            if (!this.#configCache['note']) {
                if (!AnnotationSerializer) {
                    AnnotationSerializer = require('../complexTypes/annotation.js');
                }
                this.#configCache['note'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: AnnotationSerializer
                };
            }
            return this.#configCache['note'];
        },
        resourceType: null,
        _access: null,
        _sourceAssigningAuthority: null,
        _uuid: null,
        _sourceId: null
    };
}

module.exports = new NutritionProductSerializer();
