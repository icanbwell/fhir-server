// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/meta.js')} */
let MetaSerializer;
/** @type {import('../complexTypes/narrative.js')} */
let NarrativeSerializer;
/** @type {import('../customSerializers/resourceContainer.js')} */
let ResourceContainerSerializer;
/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/contactDetail.js')} */
let ContactDetailSerializer;
/** @type {import('../complexTypes/usageContext.js')} */
let UsageContextSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../backboneElements/terminologyCapabilitiesSoftware.js')} */
let TerminologyCapabilitiesSoftwareSerializer;
/** @type {import('../backboneElements/terminologyCapabilitiesImplementation.js')} */
let TerminologyCapabilitiesImplementationSerializer;
/** @type {import('../backboneElements/terminologyCapabilitiesCodeSystem.js')} */
let TerminologyCapabilitiesCodeSystemSerializer;
/** @type {import('../backboneElements/terminologyCapabilitiesExpansion.js')} */
let TerminologyCapabilitiesExpansionSerializer;
/** @type {import('../backboneElements/terminologyCapabilitiesValidateCode.js')} */
let TerminologyCapabilitiesValidateCodeSerializer;
/** @type {import('../backboneElements/terminologyCapabilitiesTranslation.js')} */
let TerminologyCapabilitiesTranslationSerializer;
/** @type {import('../backboneElements/terminologyCapabilitiesClosure.js')} */
let TerminologyCapabilitiesClosureSerializer;

class TerminologyCapabilitiesSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        meta: () => {
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        url: null,
        version: null,
        name: null,
        title: null,
        status: null,
        experimental: null,
        date: null,
        publisher: null,
        contact: () => {
            if (!this.#configCache['contact']) {
                if (!ContactDetailSerializer) {
                    ContactDetailSerializer = require('../complexTypes/contactDetail.js');
                }
                this.#configCache['contact'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ContactDetailSerializer
                };
            }
            return this.#configCache['contact'];
        },
        description: null,
        useContext: () => {
            if (!this.#configCache['useContext']) {
                if (!UsageContextSerializer) {
                    UsageContextSerializer = require('../complexTypes/usageContext.js');
                }
                this.#configCache['useContext'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: UsageContextSerializer
                };
            }
            return this.#configCache['useContext'];
        },
        jurisdiction: () => {
            if (!this.#configCache['jurisdiction']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['jurisdiction'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['jurisdiction'];
        },
        purpose: null,
        copyright: null,
        kind: null,
        software: () => {
            if (!this.#configCache['software']) {
                if (!TerminologyCapabilitiesSoftwareSerializer) {
                    TerminologyCapabilitiesSoftwareSerializer = require('../backboneElements/terminologyCapabilitiesSoftware.js');
                }
                this.#configCache['software'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TerminologyCapabilitiesSoftwareSerializer
                };
            }
            return this.#configCache['software'];
        },
        implementation: () => {
            if (!this.#configCache['implementation']) {
                if (!TerminologyCapabilitiesImplementationSerializer) {
                    TerminologyCapabilitiesImplementationSerializer = require('../backboneElements/terminologyCapabilitiesImplementation.js');
                }
                this.#configCache['implementation'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TerminologyCapabilitiesImplementationSerializer
                };
            }
            return this.#configCache['implementation'];
        },
        lockedDate: null,
        codeSystem: () => {
            if (!this.#configCache['codeSystem']) {
                if (!TerminologyCapabilitiesCodeSystemSerializer) {
                    TerminologyCapabilitiesCodeSystemSerializer = require('../backboneElements/terminologyCapabilitiesCodeSystem.js');
                }
                this.#configCache['codeSystem'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: TerminologyCapabilitiesCodeSystemSerializer
                };
            }
            return this.#configCache['codeSystem'];
        },
        expansion: () => {
            if (!this.#configCache['expansion']) {
                if (!TerminologyCapabilitiesExpansionSerializer) {
                    TerminologyCapabilitiesExpansionSerializer = require('../backboneElements/terminologyCapabilitiesExpansion.js');
                }
                this.#configCache['expansion'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TerminologyCapabilitiesExpansionSerializer
                };
            }
            return this.#configCache['expansion'];
        },
        codeSearch: null,
        validateCode: () => {
            if (!this.#configCache['validateCode']) {
                if (!TerminologyCapabilitiesValidateCodeSerializer) {
                    TerminologyCapabilitiesValidateCodeSerializer = require('../backboneElements/terminologyCapabilitiesValidateCode.js');
                }
                this.#configCache['validateCode'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TerminologyCapabilitiesValidateCodeSerializer
                };
            }
            return this.#configCache['validateCode'];
        },
        translation: () => {
            if (!this.#configCache['translation']) {
                if (!TerminologyCapabilitiesTranslationSerializer) {
                    TerminologyCapabilitiesTranslationSerializer = require('../backboneElements/terminologyCapabilitiesTranslation.js');
                }
                this.#configCache['translation'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TerminologyCapabilitiesTranslationSerializer
                };
            }
            return this.#configCache['translation'];
        },
        closure: () => {
            if (!this.#configCache['closure']) {
                if (!TerminologyCapabilitiesClosureSerializer) {
                    TerminologyCapabilitiesClosureSerializer = require('../backboneElements/terminologyCapabilitiesClosure.js');
                }
                this.#configCache['closure'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TerminologyCapabilitiesClosureSerializer
                };
            }
            return this.#configCache['closure'];
        },
        resourceType: null
    };

    allPropertyToSerializerMap = {
        id: null,
        meta: () => {
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        url: null,
        version: null,
        name: null,
        title: null,
        status: null,
        experimental: null,
        date: null,
        publisher: null,
        contact: () => {
            if (!this.#configCache['contact']) {
                if (!ContactDetailSerializer) {
                    ContactDetailSerializer = require('../complexTypes/contactDetail.js');
                }
                this.#configCache['contact'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ContactDetailSerializer
                };
            }
            return this.#configCache['contact'];
        },
        description: null,
        useContext: () => {
            if (!this.#configCache['useContext']) {
                if (!UsageContextSerializer) {
                    UsageContextSerializer = require('../complexTypes/usageContext.js');
                }
                this.#configCache['useContext'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: UsageContextSerializer
                };
            }
            return this.#configCache['useContext'];
        },
        jurisdiction: () => {
            if (!this.#configCache['jurisdiction']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['jurisdiction'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['jurisdiction'];
        },
        purpose: null,
        copyright: null,
        kind: null,
        software: () => {
            if (!this.#configCache['software']) {
                if (!TerminologyCapabilitiesSoftwareSerializer) {
                    TerminologyCapabilitiesSoftwareSerializer = require('../backboneElements/terminologyCapabilitiesSoftware.js');
                }
                this.#configCache['software'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TerminologyCapabilitiesSoftwareSerializer
                };
            }
            return this.#configCache['software'];
        },
        implementation: () => {
            if (!this.#configCache['implementation']) {
                if (!TerminologyCapabilitiesImplementationSerializer) {
                    TerminologyCapabilitiesImplementationSerializer = require('../backboneElements/terminologyCapabilitiesImplementation.js');
                }
                this.#configCache['implementation'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TerminologyCapabilitiesImplementationSerializer
                };
            }
            return this.#configCache['implementation'];
        },
        lockedDate: null,
        codeSystem: () => {
            if (!this.#configCache['codeSystem']) {
                if (!TerminologyCapabilitiesCodeSystemSerializer) {
                    TerminologyCapabilitiesCodeSystemSerializer = require('../backboneElements/terminologyCapabilitiesCodeSystem.js');
                }
                this.#configCache['codeSystem'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: TerminologyCapabilitiesCodeSystemSerializer
                };
            }
            return this.#configCache['codeSystem'];
        },
        expansion: () => {
            if (!this.#configCache['expansion']) {
                if (!TerminologyCapabilitiesExpansionSerializer) {
                    TerminologyCapabilitiesExpansionSerializer = require('../backboneElements/terminologyCapabilitiesExpansion.js');
                }
                this.#configCache['expansion'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TerminologyCapabilitiesExpansionSerializer
                };
            }
            return this.#configCache['expansion'];
        },
        codeSearch: null,
        validateCode: () => {
            if (!this.#configCache['validateCode']) {
                if (!TerminologyCapabilitiesValidateCodeSerializer) {
                    TerminologyCapabilitiesValidateCodeSerializer = require('../backboneElements/terminologyCapabilitiesValidateCode.js');
                }
                this.#configCache['validateCode'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TerminologyCapabilitiesValidateCodeSerializer
                };
            }
            return this.#configCache['validateCode'];
        },
        translation: () => {
            if (!this.#configCache['translation']) {
                if (!TerminologyCapabilitiesTranslationSerializer) {
                    TerminologyCapabilitiesTranslationSerializer = require('../backboneElements/terminologyCapabilitiesTranslation.js');
                }
                this.#configCache['translation'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TerminologyCapabilitiesTranslationSerializer
                };
            }
            return this.#configCache['translation'];
        },
        closure: () => {
            if (!this.#configCache['closure']) {
                if (!TerminologyCapabilitiesClosureSerializer) {
                    TerminologyCapabilitiesClosureSerializer = require('../backboneElements/terminologyCapabilitiesClosure.js');
                }
                this.#configCache['closure'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TerminologyCapabilitiesClosureSerializer
                };
            }
            return this.#configCache['closure'];
        },
        resourceType: null,
        _access: null,
        _sourceAssigningAuthority: null,
        _uuid: null,
        _sourceId: null
    };
}

module.exports = new TerminologyCapabilitiesSerializer();
