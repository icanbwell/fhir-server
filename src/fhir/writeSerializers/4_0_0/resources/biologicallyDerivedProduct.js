// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/meta.js')} */
let MetaSerializer;
/** @type {import('../complexTypes/narrative.js')} */
let NarrativeSerializer;
/** @type {import('../customSerializers/resourceContainer.js')} */
let ResourceContainerSerializer;
/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/identifier.js')} */
let IdentifierSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../complexTypes/reference.js')} */
let ReferenceSerializer;
/** @type {import('../backboneElements/biologicallyDerivedProductCollection.js')} */
let BiologicallyDerivedProductCollectionSerializer;
/** @type {import('../backboneElements/biologicallyDerivedProductProcessing.js')} */
let BiologicallyDerivedProductProcessingSerializer;
/** @type {import('../backboneElements/biologicallyDerivedProductManipulation.js')} */
let BiologicallyDerivedProductManipulationSerializer;
/** @type {import('../backboneElements/biologicallyDerivedProductStorage.js')} */
let BiologicallyDerivedProductStorageSerializer;

class BiologicallyDerivedProductSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        meta: () => {
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: () => {
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        productCategory: null,
        productCode: () => {
            if (!this.#configCache['productCode']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['productCode'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['productCode'];
        },
        status: null,
        request: () => {
            if (!this.#configCache['request']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['request'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['request'];
        },
        quantity: null,
        parent: () => {
            if (!this.#configCache['parent']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['parent'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['parent'];
        },
        collection: () => {
            if (!this.#configCache['collection']) {
                if (!BiologicallyDerivedProductCollectionSerializer) {
                    BiologicallyDerivedProductCollectionSerializer = require('../backboneElements/biologicallyDerivedProductCollection.js');
                }
                this.#configCache['collection'] = {
                    serializeFunction: 'serialize',
                    serializerClass: BiologicallyDerivedProductCollectionSerializer
                };
            }
            return this.#configCache['collection'];
        },
        processing: () => {
            if (!this.#configCache['processing']) {
                if (!BiologicallyDerivedProductProcessingSerializer) {
                    BiologicallyDerivedProductProcessingSerializer = require('../backboneElements/biologicallyDerivedProductProcessing.js');
                }
                this.#configCache['processing'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: BiologicallyDerivedProductProcessingSerializer
                };
            }
            return this.#configCache['processing'];
        },
        manipulation: () => {
            if (!this.#configCache['manipulation']) {
                if (!BiologicallyDerivedProductManipulationSerializer) {
                    BiologicallyDerivedProductManipulationSerializer = require('../backboneElements/biologicallyDerivedProductManipulation.js');
                }
                this.#configCache['manipulation'] = {
                    serializeFunction: 'serialize',
                    serializerClass: BiologicallyDerivedProductManipulationSerializer
                };
            }
            return this.#configCache['manipulation'];
        },
        storage: () => {
            if (!this.#configCache['storage']) {
                if (!BiologicallyDerivedProductStorageSerializer) {
                    BiologicallyDerivedProductStorageSerializer = require('../backboneElements/biologicallyDerivedProductStorage.js');
                }
                this.#configCache['storage'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: BiologicallyDerivedProductStorageSerializer
                };
            }
            return this.#configCache['storage'];
        },
        resourceType: null
    };

    allPropertyToSerializerMap = {
        id: null,
        meta: () => {
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: () => {
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        productCategory: null,
        productCode: () => {
            if (!this.#configCache['productCode']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['productCode'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['productCode'];
        },
        status: null,
        request: () => {
            if (!this.#configCache['request']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['request'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['request'];
        },
        quantity: null,
        parent: () => {
            if (!this.#configCache['parent']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['parent'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['parent'];
        },
        collection: () => {
            if (!this.#configCache['collection']) {
                if (!BiologicallyDerivedProductCollectionSerializer) {
                    BiologicallyDerivedProductCollectionSerializer = require('../backboneElements/biologicallyDerivedProductCollection.js');
                }
                this.#configCache['collection'] = {
                    serializeFunction: 'serialize',
                    serializerClass: BiologicallyDerivedProductCollectionSerializer
                };
            }
            return this.#configCache['collection'];
        },
        processing: () => {
            if (!this.#configCache['processing']) {
                if (!BiologicallyDerivedProductProcessingSerializer) {
                    BiologicallyDerivedProductProcessingSerializer = require('../backboneElements/biologicallyDerivedProductProcessing.js');
                }
                this.#configCache['processing'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: BiologicallyDerivedProductProcessingSerializer
                };
            }
            return this.#configCache['processing'];
        },
        manipulation: () => {
            if (!this.#configCache['manipulation']) {
                if (!BiologicallyDerivedProductManipulationSerializer) {
                    BiologicallyDerivedProductManipulationSerializer = require('../backboneElements/biologicallyDerivedProductManipulation.js');
                }
                this.#configCache['manipulation'] = {
                    serializeFunction: 'serialize',
                    serializerClass: BiologicallyDerivedProductManipulationSerializer
                };
            }
            return this.#configCache['manipulation'];
        },
        storage: () => {
            if (!this.#configCache['storage']) {
                if (!BiologicallyDerivedProductStorageSerializer) {
                    BiologicallyDerivedProductStorageSerializer = require('../backboneElements/biologicallyDerivedProductStorage.js');
                }
                this.#configCache['storage'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: BiologicallyDerivedProductStorageSerializer
                };
            }
            return this.#configCache['storage'];
        },
        resourceType: null,
        _access: null,
        _sourceAssigningAuthority: null,
        _uuid: null,
        _sourceId: null
    };
}

module.exports = new BiologicallyDerivedProductSerializer();
