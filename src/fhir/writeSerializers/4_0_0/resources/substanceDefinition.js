// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/meta.js')} */
let MetaSerializer;
/** @type {import('../complexTypes/narrative.js')} */
let NarrativeSerializer;
/** @type {import('../customSerializers/resourceContainer.js')} */
let ResourceContainerSerializer;
/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/identifier.js')} */
let IdentifierSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../complexTypes/reference.js')} */
let ReferenceSerializer;
/** @type {import('../complexTypes/annotation.js')} */
let AnnotationSerializer;
/** @type {import('../backboneElements/substanceDefinitionMoiety.js')} */
let SubstanceDefinitionMoietySerializer;
/** @type {import('../backboneElements/substanceDefinitionProperty.js')} */
let SubstanceDefinitionPropertySerializer;
/** @type {import('../backboneElements/substanceDefinitionMolecularWeight.js')} */
let SubstanceDefinitionMolecularWeightSerializer;
/** @type {import('../backboneElements/substanceDefinitionStructure.js')} */
let SubstanceDefinitionStructureSerializer;
/** @type {import('../backboneElements/substanceDefinitionCode.js')} */
let SubstanceDefinitionCodeSerializer;
/** @type {import('../backboneElements/substanceDefinitionName.js')} */
let SubstanceDefinitionNameSerializer;
/** @type {import('../backboneElements/substanceDefinitionRelationship.js')} */
let SubstanceDefinitionRelationshipSerializer;
/** @type {import('../backboneElements/substanceDefinitionSourceMaterial.js')} */
let SubstanceDefinitionSourceMaterialSerializer;

class SubstanceDefinitionSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        meta: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        version: null,
        status: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['status']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['status'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['status'];
        },
        classification: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['classification']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['classification'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['classification'];
        },
        domain: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['domain']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['domain'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['domain'];
        },
        grade: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['grade']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['grade'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['grade'];
        },
        description: null,
        informationSource: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['informationSource']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['informationSource'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['informationSource'];
        },
        note: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['note']) {
                if (!AnnotationSerializer) {
                    AnnotationSerializer = require('../complexTypes/annotation.js');
                }
                this.#configCache['note'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: AnnotationSerializer
                };
            }
            return this.#configCache['note'];
        },
        manufacturer: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['manufacturer']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['manufacturer'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['manufacturer'];
        },
        supplier: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['supplier']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['supplier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['supplier'];
        },
        moiety: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['moiety']) {
                if (!SubstanceDefinitionMoietySerializer) {
                    SubstanceDefinitionMoietySerializer = require('../backboneElements/substanceDefinitionMoiety.js');
                }
                this.#configCache['moiety'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: SubstanceDefinitionMoietySerializer
                };
            }
            return this.#configCache['moiety'];
        },
        property: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['property']) {
                if (!SubstanceDefinitionPropertySerializer) {
                    SubstanceDefinitionPropertySerializer = require('../backboneElements/substanceDefinitionProperty.js');
                }
                this.#configCache['property'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: SubstanceDefinitionPropertySerializer
                };
            }
            return this.#configCache['property'];
        },
        molecularWeight: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['molecularWeight']) {
                if (!SubstanceDefinitionMolecularWeightSerializer) {
                    SubstanceDefinitionMolecularWeightSerializer = require('../backboneElements/substanceDefinitionMolecularWeight.js');
                }
                this.#configCache['molecularWeight'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: SubstanceDefinitionMolecularWeightSerializer
                };
            }
            return this.#configCache['molecularWeight'];
        },
        structure: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['structure']) {
                if (!SubstanceDefinitionStructureSerializer) {
                    SubstanceDefinitionStructureSerializer = require('../backboneElements/substanceDefinitionStructure.js');
                }
                this.#configCache['structure'] = {
                    serializeFunction: 'serialize',
                    serializerClass: SubstanceDefinitionStructureSerializer
                };
            }
            return this.#configCache['structure'];
        },
        code: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['code']) {
                if (!SubstanceDefinitionCodeSerializer) {
                    SubstanceDefinitionCodeSerializer = require('../backboneElements/substanceDefinitionCode.js');
                }
                this.#configCache['code'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: SubstanceDefinitionCodeSerializer
                };
            }
            return this.#configCache['code'];
        },
        name: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['name']) {
                if (!SubstanceDefinitionNameSerializer) {
                    SubstanceDefinitionNameSerializer = require('../backboneElements/substanceDefinitionName.js');
                }
                this.#configCache['name'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: SubstanceDefinitionNameSerializer
                };
            }
            return this.#configCache['name'];
        },
        relationship: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['relationship']) {
                if (!SubstanceDefinitionRelationshipSerializer) {
                    SubstanceDefinitionRelationshipSerializer = require('../backboneElements/substanceDefinitionRelationship.js');
                }
                this.#configCache['relationship'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: SubstanceDefinitionRelationshipSerializer
                };
            }
            return this.#configCache['relationship'];
        },
        sourceMaterial: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['sourceMaterial']) {
                if (!SubstanceDefinitionSourceMaterialSerializer) {
                    SubstanceDefinitionSourceMaterialSerializer = require('../backboneElements/substanceDefinitionSourceMaterial.js');
                }
                this.#configCache['sourceMaterial'] = {
                    serializeFunction: 'serialize',
                    serializerClass: SubstanceDefinitionSourceMaterialSerializer
                };
            }
            return this.#configCache['sourceMaterial'];
        },
        resourceType: null
    };

    allPropertyToSerializerMap = {
        id: null,
        meta: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        version: null,
        status: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['status']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['status'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['status'];
        },
        classification: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['classification']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['classification'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['classification'];
        },
        domain: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['domain']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['domain'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['domain'];
        },
        grade: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['grade']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['grade'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['grade'];
        },
        description: null,
        informationSource: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['informationSource']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['informationSource'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['informationSource'];
        },
        note: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['note']) {
                if (!AnnotationSerializer) {
                    AnnotationSerializer = require('../complexTypes/annotation.js');
                }
                this.#configCache['note'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: AnnotationSerializer
                };
            }
            return this.#configCache['note'];
        },
        manufacturer: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['manufacturer']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['manufacturer'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['manufacturer'];
        },
        supplier: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['supplier']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['supplier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['supplier'];
        },
        moiety: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['moiety']) {
                if (!SubstanceDefinitionMoietySerializer) {
                    SubstanceDefinitionMoietySerializer = require('../backboneElements/substanceDefinitionMoiety.js');
                }
                this.#configCache['moiety'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: SubstanceDefinitionMoietySerializer
                };
            }
            return this.#configCache['moiety'];
        },
        property: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['property']) {
                if (!SubstanceDefinitionPropertySerializer) {
                    SubstanceDefinitionPropertySerializer = require('../backboneElements/substanceDefinitionProperty.js');
                }
                this.#configCache['property'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: SubstanceDefinitionPropertySerializer
                };
            }
            return this.#configCache['property'];
        },
        molecularWeight: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['molecularWeight']) {
                if (!SubstanceDefinitionMolecularWeightSerializer) {
                    SubstanceDefinitionMolecularWeightSerializer = require('../backboneElements/substanceDefinitionMolecularWeight.js');
                }
                this.#configCache['molecularWeight'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: SubstanceDefinitionMolecularWeightSerializer
                };
            }
            return this.#configCache['molecularWeight'];
        },
        structure: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['structure']) {
                if (!SubstanceDefinitionStructureSerializer) {
                    SubstanceDefinitionStructureSerializer = require('../backboneElements/substanceDefinitionStructure.js');
                }
                this.#configCache['structure'] = {
                    serializeFunction: 'serialize',
                    serializerClass: SubstanceDefinitionStructureSerializer
                };
            }
            return this.#configCache['structure'];
        },
        code: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['code']) {
                if (!SubstanceDefinitionCodeSerializer) {
                    SubstanceDefinitionCodeSerializer = require('../backboneElements/substanceDefinitionCode.js');
                }
                this.#configCache['code'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: SubstanceDefinitionCodeSerializer
                };
            }
            return this.#configCache['code'];
        },
        name: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['name']) {
                if (!SubstanceDefinitionNameSerializer) {
                    SubstanceDefinitionNameSerializer = require('../backboneElements/substanceDefinitionName.js');
                }
                this.#configCache['name'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: SubstanceDefinitionNameSerializer
                };
            }
            return this.#configCache['name'];
        },
        relationship: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['relationship']) {
                if (!SubstanceDefinitionRelationshipSerializer) {
                    SubstanceDefinitionRelationshipSerializer = require('../backboneElements/substanceDefinitionRelationship.js');
                }
                this.#configCache['relationship'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: SubstanceDefinitionRelationshipSerializer
                };
            }
            return this.#configCache['relationship'];
        },
        sourceMaterial: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['sourceMaterial']) {
                if (!SubstanceDefinitionSourceMaterialSerializer) {
                    SubstanceDefinitionSourceMaterialSerializer = require('../backboneElements/substanceDefinitionSourceMaterial.js');
                }
                this.#configCache['sourceMaterial'] = {
                    serializeFunction: 'serialize',
                    serializerClass: SubstanceDefinitionSourceMaterialSerializer
                };
            }
            return this.#configCache['sourceMaterial'];
        },
        resourceType: null,
        _access: null,
        _sourceAssigningAuthority: null,
        _uuid: null,
        _sourceId: null
    };
}

module.exports = new SubstanceDefinitionSerializer();
