// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/meta.js')} */
let MetaSerializer;
/** @type {import('../complexTypes/narrative.js')} */
let NarrativeSerializer;
/** @type {import('../customSerializers/resourceContainer.js')} */
let ResourceContainerSerializer;
/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/identifier.js')} */
let IdentifierSerializer;
/** @type {import('../complexTypes/reference.js')} */
let ReferenceSerializer;
/** @type {import('../backboneElements/testReportParticipant.js')} */
let TestReportParticipantSerializer;
/** @type {import('../backboneElements/testReportSetup.js')} */
let TestReportSetupSerializer;
/** @type {import('../backboneElements/testReportTest.js')} */
let TestReportTestSerializer;
/** @type {import('../backboneElements/testReportTeardown.js')} */
let TestReportTeardownSerializer;

class TestReportSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        meta: () => {
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: () => {
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serialize',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        name: null,
        status: null,
        testScript: () => {
            if (!this.#configCache['testScript']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['testScript'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['testScript'];
        },
        result: null,
        score: null,
        tester: null,
        issued: null,
        participant: () => {
            if (!this.#configCache['participant']) {
                if (!TestReportParticipantSerializer) {
                    TestReportParticipantSerializer = require('../backboneElements/testReportParticipant.js');
                }
                this.#configCache['participant'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: TestReportParticipantSerializer
                };
            }
            return this.#configCache['participant'];
        },
        setup: () => {
            if (!this.#configCache['setup']) {
                if (!TestReportSetupSerializer) {
                    TestReportSetupSerializer = require('../backboneElements/testReportSetup.js');
                }
                this.#configCache['setup'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TestReportSetupSerializer
                };
            }
            return this.#configCache['setup'];
        },
        test: () => {
            if (!this.#configCache['test']) {
                if (!TestReportTestSerializer) {
                    TestReportTestSerializer = require('../backboneElements/testReportTest.js');
                }
                this.#configCache['test'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: TestReportTestSerializer
                };
            }
            return this.#configCache['test'];
        },
        teardown: () => {
            if (!this.#configCache['teardown']) {
                if (!TestReportTeardownSerializer) {
                    TestReportTeardownSerializer = require('../backboneElements/testReportTeardown.js');
                }
                this.#configCache['teardown'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TestReportTeardownSerializer
                };
            }
            return this.#configCache['teardown'];
        },
        resourceType: null
    };

    allPropertyToSerializerMap = {
        id: null,
        meta: () => {
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: () => {
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serialize',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        name: null,
        status: null,
        testScript: () => {
            if (!this.#configCache['testScript']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['testScript'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['testScript'];
        },
        result: null,
        score: null,
        tester: null,
        issued: null,
        participant: () => {
            if (!this.#configCache['participant']) {
                if (!TestReportParticipantSerializer) {
                    TestReportParticipantSerializer = require('../backboneElements/testReportParticipant.js');
                }
                this.#configCache['participant'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: TestReportParticipantSerializer
                };
            }
            return this.#configCache['participant'];
        },
        setup: () => {
            if (!this.#configCache['setup']) {
                if (!TestReportSetupSerializer) {
                    TestReportSetupSerializer = require('../backboneElements/testReportSetup.js');
                }
                this.#configCache['setup'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TestReportSetupSerializer
                };
            }
            return this.#configCache['setup'];
        },
        test: () => {
            if (!this.#configCache['test']) {
                if (!TestReportTestSerializer) {
                    TestReportTestSerializer = require('../backboneElements/testReportTest.js');
                }
                this.#configCache['test'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: TestReportTestSerializer
                };
            }
            return this.#configCache['test'];
        },
        teardown: () => {
            if (!this.#configCache['teardown']) {
                if (!TestReportTeardownSerializer) {
                    TestReportTeardownSerializer = require('../backboneElements/testReportTeardown.js');
                }
                this.#configCache['teardown'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TestReportTeardownSerializer
                };
            }
            return this.#configCache['teardown'];
        },
        resourceType: null,
        _access: null,
        _sourceAssigningAuthority: null,
        _uuid: null,
        _sourceId: null
    };
}

module.exports = new TestReportSerializer();
