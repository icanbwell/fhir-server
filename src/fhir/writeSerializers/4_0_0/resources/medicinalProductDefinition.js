// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/meta.js')} */
let MetaSerializer;
/** @type {import('../complexTypes/narrative.js')} */
let NarrativeSerializer;
/** @type {import('../customSerializers/resourceContainer.js')} */
let ResourceContainerSerializer;
/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/identifier.js')} */
let IdentifierSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../backboneElements/marketingStatus.js')} */
let MarketingStatusSerializer;
/** @type {import('../complexTypes/codeableReference.js')} */
let CodeableReferenceSerializer;
/** @type {import('../complexTypes/reference.js')} */
let ReferenceSerializer;
/** @type {import('../backboneElements/medicinalProductDefinitionContact.js')} */
let MedicinalProductDefinitionContactSerializer;
/** @type {import('../complexTypes/coding.js')} */
let CodingSerializer;
/** @type {import('../backboneElements/medicinalProductDefinitionName.js')} */
let MedicinalProductDefinitionNameSerializer;
/** @type {import('../backboneElements/medicinalProductDefinitionCrossReference.js')} */
let MedicinalProductDefinitionCrossReferenceSerializer;
/** @type {import('../backboneElements/medicinalProductDefinitionOperation.js')} */
let MedicinalProductDefinitionOperationSerializer;
/** @type {import('../backboneElements/medicinalProductDefinitionCharacteristic.js')} */
let MedicinalProductDefinitionCharacteristicSerializer;

class MedicinalProductDefinitionSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        meta: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        type: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['type']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['type'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['type'];
        },
        domain: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['domain']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['domain'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['domain'];
        },
        version: null,
        status: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['status']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['status'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['status'];
        },
        statusDate: null,
        description: null,
        combinedPharmaceuticalDoseForm: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['combinedPharmaceuticalDoseForm']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['combinedPharmaceuticalDoseForm'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['combinedPharmaceuticalDoseForm'];
        },
        route: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['route']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['route'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['route'];
        },
        indication: null,
        legalStatusOfSupply: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['legalStatusOfSupply']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['legalStatusOfSupply'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['legalStatusOfSupply'];
        },
        additionalMonitoringIndicator: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['additionalMonitoringIndicator']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['additionalMonitoringIndicator'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['additionalMonitoringIndicator'];
        },
        specialMeasures: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['specialMeasures']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['specialMeasures'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['specialMeasures'];
        },
        pediatricUseIndicator: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['pediatricUseIndicator']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['pediatricUseIndicator'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['pediatricUseIndicator'];
        },
        classification: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['classification']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['classification'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['classification'];
        },
        marketingStatus: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['marketingStatus']) {
                if (!MarketingStatusSerializer) {
                    MarketingStatusSerializer = require('../backboneElements/marketingStatus.js');
                }
                this.#configCache['marketingStatus'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MarketingStatusSerializer
                };
            }
            return this.#configCache['marketingStatus'];
        },
        packagedMedicinalProduct: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['packagedMedicinalProduct']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['packagedMedicinalProduct'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['packagedMedicinalProduct'];
        },
        ingredient: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['ingredient']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['ingredient'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['ingredient'];
        },
        impurity: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['impurity']) {
                if (!CodeableReferenceSerializer) {
                    CodeableReferenceSerializer = require('../complexTypes/codeableReference.js');
                }
                this.#configCache['impurity'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableReferenceSerializer
                };
            }
            return this.#configCache['impurity'];
        },
        attachedDocument: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['attachedDocument']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['attachedDocument'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['attachedDocument'];
        },
        masterFile: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['masterFile']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['masterFile'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['masterFile'];
        },
        contact: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contact']) {
                if (!MedicinalProductDefinitionContactSerializer) {
                    MedicinalProductDefinitionContactSerializer = require('../backboneElements/medicinalProductDefinitionContact.js');
                }
                this.#configCache['contact'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicinalProductDefinitionContactSerializer
                };
            }
            return this.#configCache['contact'];
        },
        clinicalTrial: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['clinicalTrial']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['clinicalTrial'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['clinicalTrial'];
        },
        code: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['code']) {
                if (!CodingSerializer) {
                    CodingSerializer = require('../complexTypes/coding.js');
                }
                this.#configCache['code'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodingSerializer
                };
            }
            return this.#configCache['code'];
        },
        name: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['name']) {
                if (!MedicinalProductDefinitionNameSerializer) {
                    MedicinalProductDefinitionNameSerializer = require('../backboneElements/medicinalProductDefinitionName.js');
                }
                this.#configCache['name'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicinalProductDefinitionNameSerializer
                };
            }
            return this.#configCache['name'];
        },
        crossReference: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['crossReference']) {
                if (!MedicinalProductDefinitionCrossReferenceSerializer) {
                    MedicinalProductDefinitionCrossReferenceSerializer = require('../backboneElements/medicinalProductDefinitionCrossReference.js');
                }
                this.#configCache['crossReference'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicinalProductDefinitionCrossReferenceSerializer
                };
            }
            return this.#configCache['crossReference'];
        },
        operation: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['operation']) {
                if (!MedicinalProductDefinitionOperationSerializer) {
                    MedicinalProductDefinitionOperationSerializer = require('../backboneElements/medicinalProductDefinitionOperation.js');
                }
                this.#configCache['operation'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicinalProductDefinitionOperationSerializer
                };
            }
            return this.#configCache['operation'];
        },
        characteristic: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['characteristic']) {
                if (!MedicinalProductDefinitionCharacteristicSerializer) {
                    MedicinalProductDefinitionCharacteristicSerializer = require('../backboneElements/medicinalProductDefinitionCharacteristic.js');
                }
                this.#configCache['characteristic'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicinalProductDefinitionCharacteristicSerializer
                };
            }
            return this.#configCache['characteristic'];
        },
        resourceType: null
    };

    allPropertyToSerializerMap = {
        id: null,
        meta: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        type: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['type']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['type'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['type'];
        },
        domain: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['domain']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['domain'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['domain'];
        },
        version: null,
        status: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['status']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['status'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['status'];
        },
        statusDate: null,
        description: null,
        combinedPharmaceuticalDoseForm: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['combinedPharmaceuticalDoseForm']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['combinedPharmaceuticalDoseForm'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['combinedPharmaceuticalDoseForm'];
        },
        route: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['route']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['route'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['route'];
        },
        indication: null,
        legalStatusOfSupply: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['legalStatusOfSupply']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['legalStatusOfSupply'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['legalStatusOfSupply'];
        },
        additionalMonitoringIndicator: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['additionalMonitoringIndicator']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['additionalMonitoringIndicator'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['additionalMonitoringIndicator'];
        },
        specialMeasures: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['specialMeasures']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['specialMeasures'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['specialMeasures'];
        },
        pediatricUseIndicator: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['pediatricUseIndicator']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['pediatricUseIndicator'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['pediatricUseIndicator'];
        },
        classification: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['classification']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['classification'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['classification'];
        },
        marketingStatus: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['marketingStatus']) {
                if (!MarketingStatusSerializer) {
                    MarketingStatusSerializer = require('../backboneElements/marketingStatus.js');
                }
                this.#configCache['marketingStatus'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MarketingStatusSerializer
                };
            }
            return this.#configCache['marketingStatus'];
        },
        packagedMedicinalProduct: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['packagedMedicinalProduct']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['packagedMedicinalProduct'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['packagedMedicinalProduct'];
        },
        ingredient: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['ingredient']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['ingredient'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['ingredient'];
        },
        impurity: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['impurity']) {
                if (!CodeableReferenceSerializer) {
                    CodeableReferenceSerializer = require('../complexTypes/codeableReference.js');
                }
                this.#configCache['impurity'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableReferenceSerializer
                };
            }
            return this.#configCache['impurity'];
        },
        attachedDocument: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['attachedDocument']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['attachedDocument'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['attachedDocument'];
        },
        masterFile: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['masterFile']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['masterFile'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['masterFile'];
        },
        contact: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contact']) {
                if (!MedicinalProductDefinitionContactSerializer) {
                    MedicinalProductDefinitionContactSerializer = require('../backboneElements/medicinalProductDefinitionContact.js');
                }
                this.#configCache['contact'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicinalProductDefinitionContactSerializer
                };
            }
            return this.#configCache['contact'];
        },
        clinicalTrial: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['clinicalTrial']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['clinicalTrial'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['clinicalTrial'];
        },
        code: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['code']) {
                if (!CodingSerializer) {
                    CodingSerializer = require('../complexTypes/coding.js');
                }
                this.#configCache['code'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodingSerializer
                };
            }
            return this.#configCache['code'];
        },
        name: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['name']) {
                if (!MedicinalProductDefinitionNameSerializer) {
                    MedicinalProductDefinitionNameSerializer = require('../backboneElements/medicinalProductDefinitionName.js');
                }
                this.#configCache['name'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicinalProductDefinitionNameSerializer
                };
            }
            return this.#configCache['name'];
        },
        crossReference: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['crossReference']) {
                if (!MedicinalProductDefinitionCrossReferenceSerializer) {
                    MedicinalProductDefinitionCrossReferenceSerializer = require('../backboneElements/medicinalProductDefinitionCrossReference.js');
                }
                this.#configCache['crossReference'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicinalProductDefinitionCrossReferenceSerializer
                };
            }
            return this.#configCache['crossReference'];
        },
        operation: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['operation']) {
                if (!MedicinalProductDefinitionOperationSerializer) {
                    MedicinalProductDefinitionOperationSerializer = require('../backboneElements/medicinalProductDefinitionOperation.js');
                }
                this.#configCache['operation'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicinalProductDefinitionOperationSerializer
                };
            }
            return this.#configCache['operation'];
        },
        characteristic: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['characteristic']) {
                if (!MedicinalProductDefinitionCharacteristicSerializer) {
                    MedicinalProductDefinitionCharacteristicSerializer = require('../backboneElements/medicinalProductDefinitionCharacteristic.js');
                }
                this.#configCache['characteristic'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicinalProductDefinitionCharacteristicSerializer
                };
            }
            return this.#configCache['characteristic'];
        },
        resourceType: null,
        _access: null,
        _sourceAssigningAuthority: null,
        _uuid: null,
        _sourceId: null
    };
}

module.exports = new MedicinalProductDefinitionSerializer();
