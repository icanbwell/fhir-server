// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/meta.js')} */
let MetaSerializer;
/** @type {import('../complexTypes/narrative.js')} */
let NarrativeSerializer;
/** @type {import('../customSerializers/resourceContainer.js')} */
let ResourceContainerSerializer;
/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/contactDetail.js')} */
let ContactDetailSerializer;
/** @type {import('../complexTypes/usageContext.js')} */
let UsageContextSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../backboneElements/implementationGuideDependsOn.js')} */
let ImplementationGuideDependsOnSerializer;
/** @type {import('../backboneElements/implementationGuideGlobal.js')} */
let ImplementationGuideGlobalSerializer;
/** @type {import('../backboneElements/implementationGuideDefinition.js')} */
let ImplementationGuideDefinitionSerializer;
/** @type {import('../backboneElements/implementationGuideManifest.js')} */
let ImplementationGuideManifestSerializer;

class ImplementationGuideSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        meta: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        url: null,
        version: null,
        name: null,
        title: null,
        status: null,
        experimental: null,
        date: null,
        publisher: null,
        contact: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contact']) {
                if (!ContactDetailSerializer) {
                    ContactDetailSerializer = require('../complexTypes/contactDetail.js');
                }
                this.#configCache['contact'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ContactDetailSerializer
                };
            }
            return this.#configCache['contact'];
        },
        description: null,
        useContext: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['useContext']) {
                if (!UsageContextSerializer) {
                    UsageContextSerializer = require('../complexTypes/usageContext.js');
                }
                this.#configCache['useContext'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: UsageContextSerializer
                };
            }
            return this.#configCache['useContext'];
        },
        jurisdiction: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['jurisdiction']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['jurisdiction'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['jurisdiction'];
        },
        copyright: null,
        packageId: null,
        license: null,
        fhirVersion: () => {
            // No serializer class needed for primitive lists
            return {
                serializeFunction: 'serializePrimitiveArray'
            };
        },
        dependsOn: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['dependsOn']) {
                if (!ImplementationGuideDependsOnSerializer) {
                    ImplementationGuideDependsOnSerializer = require('../backboneElements/implementationGuideDependsOn.js');
                }
                this.#configCache['dependsOn'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ImplementationGuideDependsOnSerializer
                };
            }
            return this.#configCache['dependsOn'];
        },
        global: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['global']) {
                if (!ImplementationGuideGlobalSerializer) {
                    ImplementationGuideGlobalSerializer = require('../backboneElements/implementationGuideGlobal.js');
                }
                this.#configCache['global'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ImplementationGuideGlobalSerializer
                };
            }
            return this.#configCache['global'];
        },
        definition: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['definition']) {
                if (!ImplementationGuideDefinitionSerializer) {
                    ImplementationGuideDefinitionSerializer = require('../backboneElements/implementationGuideDefinition.js');
                }
                this.#configCache['definition'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ImplementationGuideDefinitionSerializer
                };
            }
            return this.#configCache['definition'];
        },
        manifest: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['manifest']) {
                if (!ImplementationGuideManifestSerializer) {
                    ImplementationGuideManifestSerializer = require('../backboneElements/implementationGuideManifest.js');
                }
                this.#configCache['manifest'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ImplementationGuideManifestSerializer
                };
            }
            return this.#configCache['manifest'];
        },
        resourceType: null
    };

    allPropertyToSerializerMap = {
        id: null,
        meta: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        url: null,
        version: null,
        name: null,
        title: null,
        status: null,
        experimental: null,
        date: null,
        publisher: null,
        contact: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contact']) {
                if (!ContactDetailSerializer) {
                    ContactDetailSerializer = require('../complexTypes/contactDetail.js');
                }
                this.#configCache['contact'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ContactDetailSerializer
                };
            }
            return this.#configCache['contact'];
        },
        description: null,
        useContext: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['useContext']) {
                if (!UsageContextSerializer) {
                    UsageContextSerializer = require('../complexTypes/usageContext.js');
                }
                this.#configCache['useContext'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: UsageContextSerializer
                };
            }
            return this.#configCache['useContext'];
        },
        jurisdiction: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['jurisdiction']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['jurisdiction'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['jurisdiction'];
        },
        copyright: null,
        packageId: null,
        license: null,
        fhirVersion: () => {
            // No serializer class needed for primitive lists
            return {
                serializeFunction: 'serializePrimitiveArray'
            };
        },
        dependsOn: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['dependsOn']) {
                if (!ImplementationGuideDependsOnSerializer) {
                    ImplementationGuideDependsOnSerializer = require('../backboneElements/implementationGuideDependsOn.js');
                }
                this.#configCache['dependsOn'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ImplementationGuideDependsOnSerializer
                };
            }
            return this.#configCache['dependsOn'];
        },
        global: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['global']) {
                if (!ImplementationGuideGlobalSerializer) {
                    ImplementationGuideGlobalSerializer = require('../backboneElements/implementationGuideGlobal.js');
                }
                this.#configCache['global'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ImplementationGuideGlobalSerializer
                };
            }
            return this.#configCache['global'];
        },
        definition: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['definition']) {
                if (!ImplementationGuideDefinitionSerializer) {
                    ImplementationGuideDefinitionSerializer = require('../backboneElements/implementationGuideDefinition.js');
                }
                this.#configCache['definition'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ImplementationGuideDefinitionSerializer
                };
            }
            return this.#configCache['definition'];
        },
        manifest: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['manifest']) {
                if (!ImplementationGuideManifestSerializer) {
                    ImplementationGuideManifestSerializer = require('../backboneElements/implementationGuideManifest.js');
                }
                this.#configCache['manifest'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ImplementationGuideManifestSerializer
                };
            }
            return this.#configCache['manifest'];
        },
        resourceType: null,
        _access: null,
        _sourceAssigningAuthority: null,
        _uuid: null,
        _sourceId: null
    };
}

module.exports = new ImplementationGuideSerializer();
