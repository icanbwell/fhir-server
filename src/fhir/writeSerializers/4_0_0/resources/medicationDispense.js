// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/meta.js')} */
let MetaSerializer;
/** @type {import('../complexTypes/narrative.js')} */
let NarrativeSerializer;
/** @type {import('../customSerializers/resourceContainer.js')} */
let ResourceContainerSerializer;
/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/identifier.js')} */
let IdentifierSerializer;
/** @type {import('../complexTypes/reference.js')} */
let ReferenceSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../backboneElements/medicationDispensePerformer.js')} */
let MedicationDispensePerformerSerializer;
/** @type {import('../complexTypes/quantity.js')} */
let QuantitySerializer;
/** @type {import('../complexTypes/annotation.js')} */
let AnnotationSerializer;
/** @type {import('../backboneElements/dosage.js')} */
let DosageSerializer;
/** @type {import('../backboneElements/medicationDispenseSubstitution.js')} */
let MedicationDispenseSubstitutionSerializer;

class MedicationDispenseSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        meta: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        partOf: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['partOf']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['partOf'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['partOf'];
        },
        status: null,
        statusReasonCodeableConcept: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['statusReasonCodeableConcept']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['statusReasonCodeableConcept'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['statusReasonCodeableConcept'];
        },
        statusReasonReference: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['statusReasonReference']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['statusReasonReference'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['statusReasonReference'];
        },
        category: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['category']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['category'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['category'];
        },
        medicationCodeableConcept: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['medicationCodeableConcept']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['medicationCodeableConcept'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['medicationCodeableConcept'];
        },
        medicationReference: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['medicationReference']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['medicationReference'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['medicationReference'];
        },
        subject: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['subject']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['subject'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['subject'];
        },
        context: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['context']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['context'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['context'];
        },
        supportingInformation: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['supportingInformation']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['supportingInformation'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['supportingInformation'];
        },
        performer: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['performer']) {
                if (!MedicationDispensePerformerSerializer) {
                    MedicationDispensePerformerSerializer = require('../backboneElements/medicationDispensePerformer.js');
                }
                this.#configCache['performer'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicationDispensePerformerSerializer
                };
            }
            return this.#configCache['performer'];
        },
        location: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['location']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['location'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['location'];
        },
        authorizingPrescription: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['authorizingPrescription']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['authorizingPrescription'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['authorizingPrescription'];
        },
        type: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['type']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['type'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['type'];
        },
        quantity: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['quantity']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['quantity'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['quantity'];
        },
        daysSupply: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['daysSupply']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['daysSupply'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['daysSupply'];
        },
        whenPrepared: null,
        whenHandedOver: null,
        destination: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['destination']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['destination'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['destination'];
        },
        receiver: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['receiver']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['receiver'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['receiver'];
        },
        note: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['note']) {
                if (!AnnotationSerializer) {
                    AnnotationSerializer = require('../complexTypes/annotation.js');
                }
                this.#configCache['note'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: AnnotationSerializer
                };
            }
            return this.#configCache['note'];
        },
        dosageInstruction: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['dosageInstruction']) {
                if (!DosageSerializer) {
                    DosageSerializer = require('../backboneElements/dosage.js');
                }
                this.#configCache['dosageInstruction'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: DosageSerializer
                };
            }
            return this.#configCache['dosageInstruction'];
        },
        substitution: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['substitution']) {
                if (!MedicationDispenseSubstitutionSerializer) {
                    MedicationDispenseSubstitutionSerializer = require('../backboneElements/medicationDispenseSubstitution.js');
                }
                this.#configCache['substitution'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MedicationDispenseSubstitutionSerializer
                };
            }
            return this.#configCache['substitution'];
        },
        detectedIssue: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['detectedIssue']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['detectedIssue'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['detectedIssue'];
        },
        eventHistory: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['eventHistory']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['eventHistory'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['eventHistory'];
        },
        resourceType: null
    };

    allPropertyToSerializerMap = {
        id: null,
        meta: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        partOf: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['partOf']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['partOf'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['partOf'];
        },
        status: null,
        statusReasonCodeableConcept: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['statusReasonCodeableConcept']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['statusReasonCodeableConcept'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['statusReasonCodeableConcept'];
        },
        statusReasonReference: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['statusReasonReference']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['statusReasonReference'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['statusReasonReference'];
        },
        category: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['category']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['category'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['category'];
        },
        medicationCodeableConcept: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['medicationCodeableConcept']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['medicationCodeableConcept'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['medicationCodeableConcept'];
        },
        medicationReference: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['medicationReference']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['medicationReference'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['medicationReference'];
        },
        subject: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['subject']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['subject'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['subject'];
        },
        context: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['context']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['context'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['context'];
        },
        supportingInformation: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['supportingInformation']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['supportingInformation'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['supportingInformation'];
        },
        performer: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['performer']) {
                if (!MedicationDispensePerformerSerializer) {
                    MedicationDispensePerformerSerializer = require('../backboneElements/medicationDispensePerformer.js');
                }
                this.#configCache['performer'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicationDispensePerformerSerializer
                };
            }
            return this.#configCache['performer'];
        },
        location: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['location']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['location'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['location'];
        },
        authorizingPrescription: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['authorizingPrescription']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['authorizingPrescription'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['authorizingPrescription'];
        },
        type: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['type']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['type'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['type'];
        },
        quantity: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['quantity']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['quantity'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['quantity'];
        },
        daysSupply: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['daysSupply']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['daysSupply'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['daysSupply'];
        },
        whenPrepared: null,
        whenHandedOver: null,
        destination: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['destination']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['destination'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['destination'];
        },
        receiver: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['receiver']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['receiver'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['receiver'];
        },
        note: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['note']) {
                if (!AnnotationSerializer) {
                    AnnotationSerializer = require('../complexTypes/annotation.js');
                }
                this.#configCache['note'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: AnnotationSerializer
                };
            }
            return this.#configCache['note'];
        },
        dosageInstruction: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['dosageInstruction']) {
                if (!DosageSerializer) {
                    DosageSerializer = require('../backboneElements/dosage.js');
                }
                this.#configCache['dosageInstruction'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: DosageSerializer
                };
            }
            return this.#configCache['dosageInstruction'];
        },
        substitution: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['substitution']) {
                if (!MedicationDispenseSubstitutionSerializer) {
                    MedicationDispenseSubstitutionSerializer = require('../backboneElements/medicationDispenseSubstitution.js');
                }
                this.#configCache['substitution'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MedicationDispenseSubstitutionSerializer
                };
            }
            return this.#configCache['substitution'];
        },
        detectedIssue: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['detectedIssue']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['detectedIssue'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['detectedIssue'];
        },
        eventHistory: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['eventHistory']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['eventHistory'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['eventHistory'];
        },
        resourceType: null,
        _access: null,
        _sourceAssigningAuthority: null,
        _uuid: null,
        _sourceId: null
    };
}

module.exports = new MedicationDispenseSerializer();
