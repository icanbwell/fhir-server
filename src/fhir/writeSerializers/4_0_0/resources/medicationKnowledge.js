// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/meta.js')} */
let MetaSerializer;
/** @type {import('../complexTypes/narrative.js')} */
let NarrativeSerializer;
/** @type {import('../customSerializers/resourceContainer.js')} */
let ResourceContainerSerializer;
/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../complexTypes/reference.js')} */
let ReferenceSerializer;
/** @type {import('../complexTypes/quantity.js')} */
let QuantitySerializer;
/** @type {import('../backboneElements/medicationKnowledgeRelatedMedicationKnowledge.js')} */
let MedicationKnowledgeRelatedMedicationKnowledgeSerializer;
/** @type {import('../backboneElements/medicationKnowledgeMonograph.js')} */
let MedicationKnowledgeMonographSerializer;
/** @type {import('../backboneElements/medicationKnowledgeIngredient.js')} */
let MedicationKnowledgeIngredientSerializer;
/** @type {import('../backboneElements/medicationKnowledgeCost.js')} */
let MedicationKnowledgeCostSerializer;
/** @type {import('../backboneElements/medicationKnowledgeMonitoringProgram.js')} */
let MedicationKnowledgeMonitoringProgramSerializer;
/** @type {import('../backboneElements/medicationKnowledgeAdministrationGuidelines.js')} */
let MedicationKnowledgeAdministrationGuidelinesSerializer;
/** @type {import('../backboneElements/medicationKnowledgeMedicineClassification.js')} */
let MedicationKnowledgeMedicineClassificationSerializer;
/** @type {import('../backboneElements/medicationKnowledgePackaging.js')} */
let MedicationKnowledgePackagingSerializer;
/** @type {import('../backboneElements/medicationKnowledgeDrugCharacteristic.js')} */
let MedicationKnowledgeDrugCharacteristicSerializer;
/** @type {import('../backboneElements/medicationKnowledgeRegulatory.js')} */
let MedicationKnowledgeRegulatorySerializer;
/** @type {import('../backboneElements/medicationKnowledgeKinetics.js')} */
let MedicationKnowledgeKineticsSerializer;

class MedicationKnowledgeSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        meta: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        code: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['code']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['code'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['code'];
        },
        status: null,
        manufacturer: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['manufacturer']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['manufacturer'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['manufacturer'];
        },
        doseForm: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['doseForm']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['doseForm'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['doseForm'];
        },
        amount: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['amount']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['amount'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['amount'];
        },
        synonym: null,
        relatedMedicationKnowledge: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['relatedMedicationKnowledge']) {
                if (!MedicationKnowledgeRelatedMedicationKnowledgeSerializer) {
                    MedicationKnowledgeRelatedMedicationKnowledgeSerializer = require('../backboneElements/medicationKnowledgeRelatedMedicationKnowledge.js');
                }
                this.#configCache['relatedMedicationKnowledge'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicationKnowledgeRelatedMedicationKnowledgeSerializer
                };
            }
            return this.#configCache['relatedMedicationKnowledge'];
        },
        associatedMedication: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['associatedMedication']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['associatedMedication'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['associatedMedication'];
        },
        productType: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['productType']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['productType'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['productType'];
        },
        monograph: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['monograph']) {
                if (!MedicationKnowledgeMonographSerializer) {
                    MedicationKnowledgeMonographSerializer = require('../backboneElements/medicationKnowledgeMonograph.js');
                }
                this.#configCache['monograph'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicationKnowledgeMonographSerializer
                };
            }
            return this.#configCache['monograph'];
        },
        ingredient: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['ingredient']) {
                if (!MedicationKnowledgeIngredientSerializer) {
                    MedicationKnowledgeIngredientSerializer = require('../backboneElements/medicationKnowledgeIngredient.js');
                }
                this.#configCache['ingredient'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicationKnowledgeIngredientSerializer
                };
            }
            return this.#configCache['ingredient'];
        },
        preparationInstruction: null,
        intendedRoute: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['intendedRoute']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['intendedRoute'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['intendedRoute'];
        },
        cost: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['cost']) {
                if (!MedicationKnowledgeCostSerializer) {
                    MedicationKnowledgeCostSerializer = require('../backboneElements/medicationKnowledgeCost.js');
                }
                this.#configCache['cost'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicationKnowledgeCostSerializer
                };
            }
            return this.#configCache['cost'];
        },
        monitoringProgram: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['monitoringProgram']) {
                if (!MedicationKnowledgeMonitoringProgramSerializer) {
                    MedicationKnowledgeMonitoringProgramSerializer = require('../backboneElements/medicationKnowledgeMonitoringProgram.js');
                }
                this.#configCache['monitoringProgram'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicationKnowledgeMonitoringProgramSerializer
                };
            }
            return this.#configCache['monitoringProgram'];
        },
        administrationGuidelines: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['administrationGuidelines']) {
                if (!MedicationKnowledgeAdministrationGuidelinesSerializer) {
                    MedicationKnowledgeAdministrationGuidelinesSerializer = require('../backboneElements/medicationKnowledgeAdministrationGuidelines.js');
                }
                this.#configCache['administrationGuidelines'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicationKnowledgeAdministrationGuidelinesSerializer
                };
            }
            return this.#configCache['administrationGuidelines'];
        },
        medicineClassification: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['medicineClassification']) {
                if (!MedicationKnowledgeMedicineClassificationSerializer) {
                    MedicationKnowledgeMedicineClassificationSerializer = require('../backboneElements/medicationKnowledgeMedicineClassification.js');
                }
                this.#configCache['medicineClassification'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicationKnowledgeMedicineClassificationSerializer
                };
            }
            return this.#configCache['medicineClassification'];
        },
        packaging: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['packaging']) {
                if (!MedicationKnowledgePackagingSerializer) {
                    MedicationKnowledgePackagingSerializer = require('../backboneElements/medicationKnowledgePackaging.js');
                }
                this.#configCache['packaging'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MedicationKnowledgePackagingSerializer
                };
            }
            return this.#configCache['packaging'];
        },
        drugCharacteristic: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['drugCharacteristic']) {
                if (!MedicationKnowledgeDrugCharacteristicSerializer) {
                    MedicationKnowledgeDrugCharacteristicSerializer = require('../backboneElements/medicationKnowledgeDrugCharacteristic.js');
                }
                this.#configCache['drugCharacteristic'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicationKnowledgeDrugCharacteristicSerializer
                };
            }
            return this.#configCache['drugCharacteristic'];
        },
        contraindication: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contraindication']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['contraindication'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['contraindication'];
        },
        regulatory: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['regulatory']) {
                if (!MedicationKnowledgeRegulatorySerializer) {
                    MedicationKnowledgeRegulatorySerializer = require('../backboneElements/medicationKnowledgeRegulatory.js');
                }
                this.#configCache['regulatory'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicationKnowledgeRegulatorySerializer
                };
            }
            return this.#configCache['regulatory'];
        },
        kinetics: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['kinetics']) {
                if (!MedicationKnowledgeKineticsSerializer) {
                    MedicationKnowledgeKineticsSerializer = require('../backboneElements/medicationKnowledgeKinetics.js');
                }
                this.#configCache['kinetics'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicationKnowledgeKineticsSerializer
                };
            }
            return this.#configCache['kinetics'];
        },
        resourceType: null
    };

    allPropertyToSerializerMap = {
        id: null,
        meta: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        code: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['code']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['code'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['code'];
        },
        status: null,
        manufacturer: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['manufacturer']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['manufacturer'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['manufacturer'];
        },
        doseForm: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['doseForm']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['doseForm'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['doseForm'];
        },
        amount: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['amount']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['amount'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['amount'];
        },
        synonym: null,
        relatedMedicationKnowledge: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['relatedMedicationKnowledge']) {
                if (!MedicationKnowledgeRelatedMedicationKnowledgeSerializer) {
                    MedicationKnowledgeRelatedMedicationKnowledgeSerializer = require('../backboneElements/medicationKnowledgeRelatedMedicationKnowledge.js');
                }
                this.#configCache['relatedMedicationKnowledge'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicationKnowledgeRelatedMedicationKnowledgeSerializer
                };
            }
            return this.#configCache['relatedMedicationKnowledge'];
        },
        associatedMedication: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['associatedMedication']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['associatedMedication'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['associatedMedication'];
        },
        productType: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['productType']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['productType'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['productType'];
        },
        monograph: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['monograph']) {
                if (!MedicationKnowledgeMonographSerializer) {
                    MedicationKnowledgeMonographSerializer = require('../backboneElements/medicationKnowledgeMonograph.js');
                }
                this.#configCache['monograph'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicationKnowledgeMonographSerializer
                };
            }
            return this.#configCache['monograph'];
        },
        ingredient: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['ingredient']) {
                if (!MedicationKnowledgeIngredientSerializer) {
                    MedicationKnowledgeIngredientSerializer = require('../backboneElements/medicationKnowledgeIngredient.js');
                }
                this.#configCache['ingredient'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicationKnowledgeIngredientSerializer
                };
            }
            return this.#configCache['ingredient'];
        },
        preparationInstruction: null,
        intendedRoute: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['intendedRoute']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['intendedRoute'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['intendedRoute'];
        },
        cost: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['cost']) {
                if (!MedicationKnowledgeCostSerializer) {
                    MedicationKnowledgeCostSerializer = require('../backboneElements/medicationKnowledgeCost.js');
                }
                this.#configCache['cost'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicationKnowledgeCostSerializer
                };
            }
            return this.#configCache['cost'];
        },
        monitoringProgram: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['monitoringProgram']) {
                if (!MedicationKnowledgeMonitoringProgramSerializer) {
                    MedicationKnowledgeMonitoringProgramSerializer = require('../backboneElements/medicationKnowledgeMonitoringProgram.js');
                }
                this.#configCache['monitoringProgram'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicationKnowledgeMonitoringProgramSerializer
                };
            }
            return this.#configCache['monitoringProgram'];
        },
        administrationGuidelines: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['administrationGuidelines']) {
                if (!MedicationKnowledgeAdministrationGuidelinesSerializer) {
                    MedicationKnowledgeAdministrationGuidelinesSerializer = require('../backboneElements/medicationKnowledgeAdministrationGuidelines.js');
                }
                this.#configCache['administrationGuidelines'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicationKnowledgeAdministrationGuidelinesSerializer
                };
            }
            return this.#configCache['administrationGuidelines'];
        },
        medicineClassification: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['medicineClassification']) {
                if (!MedicationKnowledgeMedicineClassificationSerializer) {
                    MedicationKnowledgeMedicineClassificationSerializer = require('../backboneElements/medicationKnowledgeMedicineClassification.js');
                }
                this.#configCache['medicineClassification'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicationKnowledgeMedicineClassificationSerializer
                };
            }
            return this.#configCache['medicineClassification'];
        },
        packaging: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['packaging']) {
                if (!MedicationKnowledgePackagingSerializer) {
                    MedicationKnowledgePackagingSerializer = require('../backboneElements/medicationKnowledgePackaging.js');
                }
                this.#configCache['packaging'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MedicationKnowledgePackagingSerializer
                };
            }
            return this.#configCache['packaging'];
        },
        drugCharacteristic: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['drugCharacteristic']) {
                if (!MedicationKnowledgeDrugCharacteristicSerializer) {
                    MedicationKnowledgeDrugCharacteristicSerializer = require('../backboneElements/medicationKnowledgeDrugCharacteristic.js');
                }
                this.#configCache['drugCharacteristic'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicationKnowledgeDrugCharacteristicSerializer
                };
            }
            return this.#configCache['drugCharacteristic'];
        },
        contraindication: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contraindication']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['contraindication'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['contraindication'];
        },
        regulatory: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['regulatory']) {
                if (!MedicationKnowledgeRegulatorySerializer) {
                    MedicationKnowledgeRegulatorySerializer = require('../backboneElements/medicationKnowledgeRegulatory.js');
                }
                this.#configCache['regulatory'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicationKnowledgeRegulatorySerializer
                };
            }
            return this.#configCache['regulatory'];
        },
        kinetics: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['kinetics']) {
                if (!MedicationKnowledgeKineticsSerializer) {
                    MedicationKnowledgeKineticsSerializer = require('../backboneElements/medicationKnowledgeKinetics.js');
                }
                this.#configCache['kinetics'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: MedicationKnowledgeKineticsSerializer
                };
            }
            return this.#configCache['kinetics'];
        },
        resourceType: null,
        _access: null,
        _sourceAssigningAuthority: null,
        _uuid: null,
        _sourceId: null
    };
}

module.exports = new MedicationKnowledgeSerializer();
