// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/meta.js')} */
let MetaSerializer;
/** @type {import('../complexTypes/narrative.js')} */
let NarrativeSerializer;
/** @type {import('../customSerializers/resourceContainer.js')} */
let ResourceContainerSerializer;
/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/identifier.js')} */
let IdentifierSerializer;
/** @type {import('../backboneElements/deviceDefinitionUdiDeviceIdentifier.js')} */
let DeviceDefinitionUdiDeviceIdentifierSerializer;
/** @type {import('../complexTypes/reference.js')} */
let ReferenceSerializer;
/** @type {import('../backboneElements/deviceDefinitionDeviceName.js')} */
let DeviceDefinitionDeviceNameSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../backboneElements/deviceDefinitionSpecialization.js')} */
let DeviceDefinitionSpecializationSerializer;
/** @type {import('../backboneElements/productShelfLife.js')} */
let ProductShelfLifeSerializer;
/** @type {import('../backboneElements/prodCharacteristic.js')} */
let ProdCharacteristicSerializer;
/** @type {import('../backboneElements/deviceDefinitionCapability.js')} */
let DeviceDefinitionCapabilitySerializer;
/** @type {import('../backboneElements/deviceDefinitionProperty.js')} */
let DeviceDefinitionPropertySerializer;
/** @type {import('../complexTypes/contactPoint.js')} */
let ContactPointSerializer;
/** @type {import('../complexTypes/annotation.js')} */
let AnnotationSerializer;
/** @type {import('../complexTypes/quantity.js')} */
let QuantitySerializer;
/** @type {import('../backboneElements/deviceDefinitionMaterial.js')} */
let DeviceDefinitionMaterialSerializer;

class DeviceDefinitionSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        meta: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        udiDeviceIdentifier: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['udiDeviceIdentifier']) {
                if (!DeviceDefinitionUdiDeviceIdentifierSerializer) {
                    DeviceDefinitionUdiDeviceIdentifierSerializer = require('../backboneElements/deviceDefinitionUdiDeviceIdentifier.js');
                }
                this.#configCache['udiDeviceIdentifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: DeviceDefinitionUdiDeviceIdentifierSerializer
                };
            }
            return this.#configCache['udiDeviceIdentifier'];
        },
        manufacturerString: null,
        manufacturerReference: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['manufacturerReference']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['manufacturerReference'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['manufacturerReference'];
        },
        deviceName: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['deviceName']) {
                if (!DeviceDefinitionDeviceNameSerializer) {
                    DeviceDefinitionDeviceNameSerializer = require('../backboneElements/deviceDefinitionDeviceName.js');
                }
                this.#configCache['deviceName'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: DeviceDefinitionDeviceNameSerializer
                };
            }
            return this.#configCache['deviceName'];
        },
        modelNumber: null,
        type: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['type']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['type'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['type'];
        },
        specialization: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['specialization']) {
                if (!DeviceDefinitionSpecializationSerializer) {
                    DeviceDefinitionSpecializationSerializer = require('../backboneElements/deviceDefinitionSpecialization.js');
                }
                this.#configCache['specialization'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: DeviceDefinitionSpecializationSerializer
                };
            }
            return this.#configCache['specialization'];
        },
        version: () => {
            // No serializer class needed for primitive lists
            return {
                serializeFunction: 'serializePrimitiveArray'
            };
        },
        safety: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['safety']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['safety'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['safety'];
        },
        shelfLifeStorage: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['shelfLifeStorage']) {
                if (!ProductShelfLifeSerializer) {
                    ProductShelfLifeSerializer = require('../backboneElements/productShelfLife.js');
                }
                this.#configCache['shelfLifeStorage'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ProductShelfLifeSerializer
                };
            }
            return this.#configCache['shelfLifeStorage'];
        },
        physicalCharacteristics: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['physicalCharacteristics']) {
                if (!ProdCharacteristicSerializer) {
                    ProdCharacteristicSerializer = require('../backboneElements/prodCharacteristic.js');
                }
                this.#configCache['physicalCharacteristics'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ProdCharacteristicSerializer
                };
            }
            return this.#configCache['physicalCharacteristics'];
        },
        languageCode: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['languageCode']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['languageCode'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['languageCode'];
        },
        capability: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['capability']) {
                if (!DeviceDefinitionCapabilitySerializer) {
                    DeviceDefinitionCapabilitySerializer = require('../backboneElements/deviceDefinitionCapability.js');
                }
                this.#configCache['capability'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: DeviceDefinitionCapabilitySerializer
                };
            }
            return this.#configCache['capability'];
        },
        property: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['property']) {
                if (!DeviceDefinitionPropertySerializer) {
                    DeviceDefinitionPropertySerializer = require('../backboneElements/deviceDefinitionProperty.js');
                }
                this.#configCache['property'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: DeviceDefinitionPropertySerializer
                };
            }
            return this.#configCache['property'];
        },
        owner: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['owner']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['owner'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['owner'];
        },
        contact: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contact']) {
                if (!ContactPointSerializer) {
                    ContactPointSerializer = require('../complexTypes/contactPoint.js');
                }
                this.#configCache['contact'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ContactPointSerializer
                };
            }
            return this.#configCache['contact'];
        },
        url: null,
        onlineInformation: null,
        note: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['note']) {
                if (!AnnotationSerializer) {
                    AnnotationSerializer = require('../complexTypes/annotation.js');
                }
                this.#configCache['note'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: AnnotationSerializer
                };
            }
            return this.#configCache['note'];
        },
        quantity: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['quantity']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['quantity'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['quantity'];
        },
        parentDevice: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['parentDevice']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['parentDevice'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['parentDevice'];
        },
        material: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['material']) {
                if (!DeviceDefinitionMaterialSerializer) {
                    DeviceDefinitionMaterialSerializer = require('../backboneElements/deviceDefinitionMaterial.js');
                }
                this.#configCache['material'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: DeviceDefinitionMaterialSerializer
                };
            }
            return this.#configCache['material'];
        },
        resourceType: null
    };

    allPropertyToSerializerMap = {
        id: null,
        meta: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        udiDeviceIdentifier: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['udiDeviceIdentifier']) {
                if (!DeviceDefinitionUdiDeviceIdentifierSerializer) {
                    DeviceDefinitionUdiDeviceIdentifierSerializer = require('../backboneElements/deviceDefinitionUdiDeviceIdentifier.js');
                }
                this.#configCache['udiDeviceIdentifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: DeviceDefinitionUdiDeviceIdentifierSerializer
                };
            }
            return this.#configCache['udiDeviceIdentifier'];
        },
        manufacturerString: null,
        manufacturerReference: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['manufacturerReference']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['manufacturerReference'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['manufacturerReference'];
        },
        deviceName: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['deviceName']) {
                if (!DeviceDefinitionDeviceNameSerializer) {
                    DeviceDefinitionDeviceNameSerializer = require('../backboneElements/deviceDefinitionDeviceName.js');
                }
                this.#configCache['deviceName'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: DeviceDefinitionDeviceNameSerializer
                };
            }
            return this.#configCache['deviceName'];
        },
        modelNumber: null,
        type: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['type']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['type'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['type'];
        },
        specialization: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['specialization']) {
                if (!DeviceDefinitionSpecializationSerializer) {
                    DeviceDefinitionSpecializationSerializer = require('../backboneElements/deviceDefinitionSpecialization.js');
                }
                this.#configCache['specialization'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: DeviceDefinitionSpecializationSerializer
                };
            }
            return this.#configCache['specialization'];
        },
        version: () => {
            // No serializer class needed for primitive lists
            return {
                serializeFunction: 'serializePrimitiveArray'
            };
        },
        safety: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['safety']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['safety'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['safety'];
        },
        shelfLifeStorage: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['shelfLifeStorage']) {
                if (!ProductShelfLifeSerializer) {
                    ProductShelfLifeSerializer = require('../backboneElements/productShelfLife.js');
                }
                this.#configCache['shelfLifeStorage'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ProductShelfLifeSerializer
                };
            }
            return this.#configCache['shelfLifeStorage'];
        },
        physicalCharacteristics: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['physicalCharacteristics']) {
                if (!ProdCharacteristicSerializer) {
                    ProdCharacteristicSerializer = require('../backboneElements/prodCharacteristic.js');
                }
                this.#configCache['physicalCharacteristics'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ProdCharacteristicSerializer
                };
            }
            return this.#configCache['physicalCharacteristics'];
        },
        languageCode: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['languageCode']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['languageCode'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['languageCode'];
        },
        capability: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['capability']) {
                if (!DeviceDefinitionCapabilitySerializer) {
                    DeviceDefinitionCapabilitySerializer = require('../backboneElements/deviceDefinitionCapability.js');
                }
                this.#configCache['capability'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: DeviceDefinitionCapabilitySerializer
                };
            }
            return this.#configCache['capability'];
        },
        property: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['property']) {
                if (!DeviceDefinitionPropertySerializer) {
                    DeviceDefinitionPropertySerializer = require('../backboneElements/deviceDefinitionProperty.js');
                }
                this.#configCache['property'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: DeviceDefinitionPropertySerializer
                };
            }
            return this.#configCache['property'];
        },
        owner: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['owner']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['owner'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['owner'];
        },
        contact: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contact']) {
                if (!ContactPointSerializer) {
                    ContactPointSerializer = require('../complexTypes/contactPoint.js');
                }
                this.#configCache['contact'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ContactPointSerializer
                };
            }
            return this.#configCache['contact'];
        },
        url: null,
        onlineInformation: null,
        note: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['note']) {
                if (!AnnotationSerializer) {
                    AnnotationSerializer = require('../complexTypes/annotation.js');
                }
                this.#configCache['note'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: AnnotationSerializer
                };
            }
            return this.#configCache['note'];
        },
        quantity: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['quantity']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['quantity'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['quantity'];
        },
        parentDevice: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['parentDevice']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['parentDevice'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['parentDevice'];
        },
        material: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['material']) {
                if (!DeviceDefinitionMaterialSerializer) {
                    DeviceDefinitionMaterialSerializer = require('../backboneElements/deviceDefinitionMaterial.js');
                }
                this.#configCache['material'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: DeviceDefinitionMaterialSerializer
                };
            }
            return this.#configCache['material'];
        },
        resourceType: null,
        _access: null,
        _sourceAssigningAuthority: null,
        _uuid: null,
        _sourceId: null
    };
}

module.exports = new DeviceDefinitionSerializer();
