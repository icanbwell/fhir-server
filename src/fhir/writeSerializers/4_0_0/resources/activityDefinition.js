// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/meta.js')} */
let MetaSerializer;
/** @type {import('../complexTypes/narrative.js')} */
let NarrativeSerializer;
/** @type {import('../customSerializers/resourceContainer.js')} */
let ResourceContainerSerializer;
/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/identifier.js')} */
let IdentifierSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../complexTypes/reference.js')} */
let ReferenceSerializer;
/** @type {import('../complexTypes/contactDetail.js')} */
let ContactDetailSerializer;
/** @type {import('../complexTypes/usageContext.js')} */
let UsageContextSerializer;
/** @type {import('../complexTypes/period.js')} */
let PeriodSerializer;
/** @type {import('../complexTypes/relatedArtifact.js')} */
let RelatedArtifactSerializer;
/** @type {import('../backboneElements/timing.js')} */
let TimingSerializer;
/** @type {import('../complexTypes/quantity.js')} */
let QuantitySerializer;
/** @type {import('../complexTypes/range.js')} */
let RangeSerializer;
/** @type {import('../backboneElements/activityDefinitionParticipant.js')} */
let ActivityDefinitionParticipantSerializer;
/** @type {import('../backboneElements/dosage.js')} */
let DosageSerializer;
/** @type {import('../backboneElements/activityDefinitionDynamicValue.js')} */
let ActivityDefinitionDynamicValueSerializer;

class ActivityDefinitionSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        meta: () => {
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        url: null,
        identifier: () => {
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        version: null,
        name: null,
        title: null,
        subtitle: null,
        status: null,
        experimental: null,
        subjectCodeableConcept: () => {
            if (!this.#configCache['subjectCodeableConcept']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['subjectCodeableConcept'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['subjectCodeableConcept'];
        },
        subjectReference: () => {
            if (!this.#configCache['subjectReference']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['subjectReference'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['subjectReference'];
        },
        subjectCanonical: null,
        date: null,
        publisher: null,
        contact: () => {
            if (!this.#configCache['contact']) {
                if (!ContactDetailSerializer) {
                    ContactDetailSerializer = require('../complexTypes/contactDetail.js');
                }
                this.#configCache['contact'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ContactDetailSerializer
                };
            }
            return this.#configCache['contact'];
        },
        description: null,
        useContext: () => {
            if (!this.#configCache['useContext']) {
                if (!UsageContextSerializer) {
                    UsageContextSerializer = require('../complexTypes/usageContext.js');
                }
                this.#configCache['useContext'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: UsageContextSerializer
                };
            }
            return this.#configCache['useContext'];
        },
        jurisdiction: () => {
            if (!this.#configCache['jurisdiction']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['jurisdiction'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['jurisdiction'];
        },
        purpose: null,
        usage: null,
        copyright: null,
        approvalDate: null,
        lastReviewDate: null,
        effectivePeriod: () => {
            if (!this.#configCache['effectivePeriod']) {
                if (!PeriodSerializer) {
                    PeriodSerializer = require('../complexTypes/period.js');
                }
                this.#configCache['effectivePeriod'] = {
                    serializeFunction: 'serialize',
                    serializerClass: PeriodSerializer
                };
            }
            return this.#configCache['effectivePeriod'];
        },
        topic: () => {
            if (!this.#configCache['topic']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['topic'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['topic'];
        },
        author: () => {
            if (!this.#configCache['author']) {
                if (!ContactDetailSerializer) {
                    ContactDetailSerializer = require('../complexTypes/contactDetail.js');
                }
                this.#configCache['author'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ContactDetailSerializer
                };
            }
            return this.#configCache['author'];
        },
        editor: () => {
            if (!this.#configCache['editor']) {
                if (!ContactDetailSerializer) {
                    ContactDetailSerializer = require('../complexTypes/contactDetail.js');
                }
                this.#configCache['editor'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ContactDetailSerializer
                };
            }
            return this.#configCache['editor'];
        },
        reviewer: () => {
            if (!this.#configCache['reviewer']) {
                if (!ContactDetailSerializer) {
                    ContactDetailSerializer = require('../complexTypes/contactDetail.js');
                }
                this.#configCache['reviewer'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ContactDetailSerializer
                };
            }
            return this.#configCache['reviewer'];
        },
        endorser: () => {
            if (!this.#configCache['endorser']) {
                if (!ContactDetailSerializer) {
                    ContactDetailSerializer = require('../complexTypes/contactDetail.js');
                }
                this.#configCache['endorser'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ContactDetailSerializer
                };
            }
            return this.#configCache['endorser'];
        },
        relatedArtifact: () => {
            if (!this.#configCache['relatedArtifact']) {
                if (!RelatedArtifactSerializer) {
                    RelatedArtifactSerializer = require('../complexTypes/relatedArtifact.js');
                }
                this.#configCache['relatedArtifact'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: RelatedArtifactSerializer
                };
            }
            return this.#configCache['relatedArtifact'];
        },
        library: () => {
            // No serializer class needed for primitive lists
            return {
                serializeFunction: 'serializePrimitiveArray'
            };
        },
        kind: null,
        profile: null,
        code: () => {
            if (!this.#configCache['code']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['code'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['code'];
        },
        intent: null,
        priority: null,
        doNotPerform: null,
        timingTiming: () => {
            if (!this.#configCache['timingTiming']) {
                if (!TimingSerializer) {
                    TimingSerializer = require('../backboneElements/timing.js');
                }
                this.#configCache['timingTiming'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TimingSerializer
                };
            }
            return this.#configCache['timingTiming'];
        },
        timingDateTime: null,
        timingAge: () => {
            if (!this.#configCache['timingAge']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['timingAge'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['timingAge'];
        },
        timingPeriod: () => {
            if (!this.#configCache['timingPeriod']) {
                if (!PeriodSerializer) {
                    PeriodSerializer = require('../complexTypes/period.js');
                }
                this.#configCache['timingPeriod'] = {
                    serializeFunction: 'serialize',
                    serializerClass: PeriodSerializer
                };
            }
            return this.#configCache['timingPeriod'];
        },
        timingRange: () => {
            if (!this.#configCache['timingRange']) {
                if (!RangeSerializer) {
                    RangeSerializer = require('../complexTypes/range.js');
                }
                this.#configCache['timingRange'] = {
                    serializeFunction: 'serialize',
                    serializerClass: RangeSerializer
                };
            }
            return this.#configCache['timingRange'];
        },
        timingDuration: () => {
            if (!this.#configCache['timingDuration']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['timingDuration'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['timingDuration'];
        },
        location: () => {
            if (!this.#configCache['location']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['location'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['location'];
        },
        participant: () => {
            if (!this.#configCache['participant']) {
                if (!ActivityDefinitionParticipantSerializer) {
                    ActivityDefinitionParticipantSerializer = require('../backboneElements/activityDefinitionParticipant.js');
                }
                this.#configCache['participant'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ActivityDefinitionParticipantSerializer
                };
            }
            return this.#configCache['participant'];
        },
        productReference: () => {
            if (!this.#configCache['productReference']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['productReference'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['productReference'];
        },
        productCodeableConcept: () => {
            if (!this.#configCache['productCodeableConcept']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['productCodeableConcept'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['productCodeableConcept'];
        },
        quantity: () => {
            if (!this.#configCache['quantity']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['quantity'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['quantity'];
        },
        dosage: () => {
            if (!this.#configCache['dosage']) {
                if (!DosageSerializer) {
                    DosageSerializer = require('../backboneElements/dosage.js');
                }
                this.#configCache['dosage'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: DosageSerializer
                };
            }
            return this.#configCache['dosage'];
        },
        bodySite: () => {
            if (!this.#configCache['bodySite']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['bodySite'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['bodySite'];
        },
        specimenRequirement: () => {
            if (!this.#configCache['specimenRequirement']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['specimenRequirement'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['specimenRequirement'];
        },
        observationRequirement: () => {
            if (!this.#configCache['observationRequirement']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['observationRequirement'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['observationRequirement'];
        },
        observationResultRequirement: () => {
            if (!this.#configCache['observationResultRequirement']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['observationResultRequirement'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['observationResultRequirement'];
        },
        transform: null,
        dynamicValue: () => {
            if (!this.#configCache['dynamicValue']) {
                if (!ActivityDefinitionDynamicValueSerializer) {
                    ActivityDefinitionDynamicValueSerializer = require('../backboneElements/activityDefinitionDynamicValue.js');
                }
                this.#configCache['dynamicValue'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ActivityDefinitionDynamicValueSerializer
                };
            }
            return this.#configCache['dynamicValue'];
        },
        resourceType: null
    };

    allPropertyToSerializerMap = {
        id: null,
        meta: () => {
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        url: null,
        identifier: () => {
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        version: null,
        name: null,
        title: null,
        subtitle: null,
        status: null,
        experimental: null,
        subjectCodeableConcept: () => {
            if (!this.#configCache['subjectCodeableConcept']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['subjectCodeableConcept'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['subjectCodeableConcept'];
        },
        subjectReference: () => {
            if (!this.#configCache['subjectReference']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['subjectReference'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['subjectReference'];
        },
        subjectCanonical: null,
        date: null,
        publisher: null,
        contact: () => {
            if (!this.#configCache['contact']) {
                if (!ContactDetailSerializer) {
                    ContactDetailSerializer = require('../complexTypes/contactDetail.js');
                }
                this.#configCache['contact'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ContactDetailSerializer
                };
            }
            return this.#configCache['contact'];
        },
        description: null,
        useContext: () => {
            if (!this.#configCache['useContext']) {
                if (!UsageContextSerializer) {
                    UsageContextSerializer = require('../complexTypes/usageContext.js');
                }
                this.#configCache['useContext'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: UsageContextSerializer
                };
            }
            return this.#configCache['useContext'];
        },
        jurisdiction: () => {
            if (!this.#configCache['jurisdiction']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['jurisdiction'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['jurisdiction'];
        },
        purpose: null,
        usage: null,
        copyright: null,
        approvalDate: null,
        lastReviewDate: null,
        effectivePeriod: () => {
            if (!this.#configCache['effectivePeriod']) {
                if (!PeriodSerializer) {
                    PeriodSerializer = require('../complexTypes/period.js');
                }
                this.#configCache['effectivePeriod'] = {
                    serializeFunction: 'serialize',
                    serializerClass: PeriodSerializer
                };
            }
            return this.#configCache['effectivePeriod'];
        },
        topic: () => {
            if (!this.#configCache['topic']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['topic'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['topic'];
        },
        author: () => {
            if (!this.#configCache['author']) {
                if (!ContactDetailSerializer) {
                    ContactDetailSerializer = require('../complexTypes/contactDetail.js');
                }
                this.#configCache['author'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ContactDetailSerializer
                };
            }
            return this.#configCache['author'];
        },
        editor: () => {
            if (!this.#configCache['editor']) {
                if (!ContactDetailSerializer) {
                    ContactDetailSerializer = require('../complexTypes/contactDetail.js');
                }
                this.#configCache['editor'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ContactDetailSerializer
                };
            }
            return this.#configCache['editor'];
        },
        reviewer: () => {
            if (!this.#configCache['reviewer']) {
                if (!ContactDetailSerializer) {
                    ContactDetailSerializer = require('../complexTypes/contactDetail.js');
                }
                this.#configCache['reviewer'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ContactDetailSerializer
                };
            }
            return this.#configCache['reviewer'];
        },
        endorser: () => {
            if (!this.#configCache['endorser']) {
                if (!ContactDetailSerializer) {
                    ContactDetailSerializer = require('../complexTypes/contactDetail.js');
                }
                this.#configCache['endorser'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ContactDetailSerializer
                };
            }
            return this.#configCache['endorser'];
        },
        relatedArtifact: () => {
            if (!this.#configCache['relatedArtifact']) {
                if (!RelatedArtifactSerializer) {
                    RelatedArtifactSerializer = require('../complexTypes/relatedArtifact.js');
                }
                this.#configCache['relatedArtifact'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: RelatedArtifactSerializer
                };
            }
            return this.#configCache['relatedArtifact'];
        },
        library: () => {
            // No serializer class needed for primitive lists
            return {
                serializeFunction: 'serializePrimitiveArray'
            };
        },
        kind: null,
        profile: null,
        code: () => {
            if (!this.#configCache['code']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['code'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['code'];
        },
        intent: null,
        priority: null,
        doNotPerform: null,
        timingTiming: () => {
            if (!this.#configCache['timingTiming']) {
                if (!TimingSerializer) {
                    TimingSerializer = require('../backboneElements/timing.js');
                }
                this.#configCache['timingTiming'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TimingSerializer
                };
            }
            return this.#configCache['timingTiming'];
        },
        timingDateTime: null,
        timingAge: () => {
            if (!this.#configCache['timingAge']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['timingAge'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['timingAge'];
        },
        timingPeriod: () => {
            if (!this.#configCache['timingPeriod']) {
                if (!PeriodSerializer) {
                    PeriodSerializer = require('../complexTypes/period.js');
                }
                this.#configCache['timingPeriod'] = {
                    serializeFunction: 'serialize',
                    serializerClass: PeriodSerializer
                };
            }
            return this.#configCache['timingPeriod'];
        },
        timingRange: () => {
            if (!this.#configCache['timingRange']) {
                if (!RangeSerializer) {
                    RangeSerializer = require('../complexTypes/range.js');
                }
                this.#configCache['timingRange'] = {
                    serializeFunction: 'serialize',
                    serializerClass: RangeSerializer
                };
            }
            return this.#configCache['timingRange'];
        },
        timingDuration: () => {
            if (!this.#configCache['timingDuration']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['timingDuration'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['timingDuration'];
        },
        location: () => {
            if (!this.#configCache['location']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['location'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['location'];
        },
        participant: () => {
            if (!this.#configCache['participant']) {
                if (!ActivityDefinitionParticipantSerializer) {
                    ActivityDefinitionParticipantSerializer = require('../backboneElements/activityDefinitionParticipant.js');
                }
                this.#configCache['participant'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ActivityDefinitionParticipantSerializer
                };
            }
            return this.#configCache['participant'];
        },
        productReference: () => {
            if (!this.#configCache['productReference']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['productReference'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['productReference'];
        },
        productCodeableConcept: () => {
            if (!this.#configCache['productCodeableConcept']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['productCodeableConcept'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['productCodeableConcept'];
        },
        quantity: () => {
            if (!this.#configCache['quantity']) {
                if (!QuantitySerializer) {
                    QuantitySerializer = require('../complexTypes/quantity.js');
                }
                this.#configCache['quantity'] = {
                    serializeFunction: 'serialize',
                    serializerClass: QuantitySerializer
                };
            }
            return this.#configCache['quantity'];
        },
        dosage: () => {
            if (!this.#configCache['dosage']) {
                if (!DosageSerializer) {
                    DosageSerializer = require('../backboneElements/dosage.js');
                }
                this.#configCache['dosage'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: DosageSerializer
                };
            }
            return this.#configCache['dosage'];
        },
        bodySite: () => {
            if (!this.#configCache['bodySite']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['bodySite'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['bodySite'];
        },
        specimenRequirement: () => {
            if (!this.#configCache['specimenRequirement']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['specimenRequirement'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['specimenRequirement'];
        },
        observationRequirement: () => {
            if (!this.#configCache['observationRequirement']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['observationRequirement'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['observationRequirement'];
        },
        observationResultRequirement: () => {
            if (!this.#configCache['observationResultRequirement']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['observationResultRequirement'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['observationResultRequirement'];
        },
        transform: null,
        dynamicValue: () => {
            if (!this.#configCache['dynamicValue']) {
                if (!ActivityDefinitionDynamicValueSerializer) {
                    ActivityDefinitionDynamicValueSerializer = require('../backboneElements/activityDefinitionDynamicValue.js');
                }
                this.#configCache['dynamicValue'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ActivityDefinitionDynamicValueSerializer
                };
            }
            return this.#configCache['dynamicValue'];
        },
        resourceType: null,
        _access: null,
        _sourceAssigningAuthority: null,
        _uuid: null,
        _sourceId: null
    };
}

module.exports = new ActivityDefinitionSerializer();
