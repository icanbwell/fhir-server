// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/meta.js')} */
let MetaSerializer;
/** @type {import('../complexTypes/narrative.js')} */
let NarrativeSerializer;
/** @type {import('../customSerializers/resourceContainer.js')} */
let ResourceContainerSerializer;
/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/identifier.js')} */
let IdentifierSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../complexTypes/reference.js')} */
let ReferenceSerializer;
/** @type {import('../backboneElements/clinicalUseDefinitionContraindication.js')} */
let ClinicalUseDefinitionContraindicationSerializer;
/** @type {import('../backboneElements/clinicalUseDefinitionIndication.js')} */
let ClinicalUseDefinitionIndicationSerializer;
/** @type {import('../backboneElements/clinicalUseDefinitionInteraction.js')} */
let ClinicalUseDefinitionInteractionSerializer;
/** @type {import('../backboneElements/clinicalUseDefinitionUndesirableEffect.js')} */
let ClinicalUseDefinitionUndesirableEffectSerializer;
/** @type {import('../backboneElements/clinicalUseDefinitionWarning.js')} */
let ClinicalUseDefinitionWarningSerializer;

class ClinicalUseDefinitionSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        meta: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        type: null,
        category: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['category']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['category'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['category'];
        },
        subject: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['subject']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['subject'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['subject'];
        },
        status: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['status']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['status'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['status'];
        },
        contraindication: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contraindication']) {
                if (!ClinicalUseDefinitionContraindicationSerializer) {
                    ClinicalUseDefinitionContraindicationSerializer = require('../backboneElements/clinicalUseDefinitionContraindication.js');
                }
                this.#configCache['contraindication'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ClinicalUseDefinitionContraindicationSerializer
                };
            }
            return this.#configCache['contraindication'];
        },
        indication: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['indication']) {
                if (!ClinicalUseDefinitionIndicationSerializer) {
                    ClinicalUseDefinitionIndicationSerializer = require('../backboneElements/clinicalUseDefinitionIndication.js');
                }
                this.#configCache['indication'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ClinicalUseDefinitionIndicationSerializer
                };
            }
            return this.#configCache['indication'];
        },
        interaction: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['interaction']) {
                if (!ClinicalUseDefinitionInteractionSerializer) {
                    ClinicalUseDefinitionInteractionSerializer = require('../backboneElements/clinicalUseDefinitionInteraction.js');
                }
                this.#configCache['interaction'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ClinicalUseDefinitionInteractionSerializer
                };
            }
            return this.#configCache['interaction'];
        },
        population: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['population']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['population'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['population'];
        },
        undesirableEffect: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['undesirableEffect']) {
                if (!ClinicalUseDefinitionUndesirableEffectSerializer) {
                    ClinicalUseDefinitionUndesirableEffectSerializer = require('../backboneElements/clinicalUseDefinitionUndesirableEffect.js');
                }
                this.#configCache['undesirableEffect'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ClinicalUseDefinitionUndesirableEffectSerializer
                };
            }
            return this.#configCache['undesirableEffect'];
        },
        warning: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['warning']) {
                if (!ClinicalUseDefinitionWarningSerializer) {
                    ClinicalUseDefinitionWarningSerializer = require('../backboneElements/clinicalUseDefinitionWarning.js');
                }
                this.#configCache['warning'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ClinicalUseDefinitionWarningSerializer
                };
            }
            return this.#configCache['warning'];
        },
        resourceType: null
    };

    allPropertyToSerializerMap = {
        id: null,
        meta: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        type: null,
        category: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['category']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['category'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['category'];
        },
        subject: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['subject']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['subject'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['subject'];
        },
        status: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['status']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['status'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['status'];
        },
        contraindication: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contraindication']) {
                if (!ClinicalUseDefinitionContraindicationSerializer) {
                    ClinicalUseDefinitionContraindicationSerializer = require('../backboneElements/clinicalUseDefinitionContraindication.js');
                }
                this.#configCache['contraindication'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ClinicalUseDefinitionContraindicationSerializer
                };
            }
            return this.#configCache['contraindication'];
        },
        indication: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['indication']) {
                if (!ClinicalUseDefinitionIndicationSerializer) {
                    ClinicalUseDefinitionIndicationSerializer = require('../backboneElements/clinicalUseDefinitionIndication.js');
                }
                this.#configCache['indication'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ClinicalUseDefinitionIndicationSerializer
                };
            }
            return this.#configCache['indication'];
        },
        interaction: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['interaction']) {
                if (!ClinicalUseDefinitionInteractionSerializer) {
                    ClinicalUseDefinitionInteractionSerializer = require('../backboneElements/clinicalUseDefinitionInteraction.js');
                }
                this.#configCache['interaction'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ClinicalUseDefinitionInteractionSerializer
                };
            }
            return this.#configCache['interaction'];
        },
        population: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['population']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['population'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['population'];
        },
        undesirableEffect: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['undesirableEffect']) {
                if (!ClinicalUseDefinitionUndesirableEffectSerializer) {
                    ClinicalUseDefinitionUndesirableEffectSerializer = require('../backboneElements/clinicalUseDefinitionUndesirableEffect.js');
                }
                this.#configCache['undesirableEffect'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ClinicalUseDefinitionUndesirableEffectSerializer
                };
            }
            return this.#configCache['undesirableEffect'];
        },
        warning: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['warning']) {
                if (!ClinicalUseDefinitionWarningSerializer) {
                    ClinicalUseDefinitionWarningSerializer = require('../backboneElements/clinicalUseDefinitionWarning.js');
                }
                this.#configCache['warning'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ClinicalUseDefinitionWarningSerializer
                };
            }
            return this.#configCache['warning'];
        },
        resourceType: null,
        _access: null,
        _sourceAssigningAuthority: null,
        _uuid: null,
        _sourceId: null
    };
}

module.exports = new ClinicalUseDefinitionSerializer();
