// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/meta.js')} */
let MetaSerializer;
/** @type {import('../complexTypes/narrative.js')} */
let NarrativeSerializer;
/** @type {import('../customSerializers/resourceContainer.js')} */
let ResourceContainerSerializer;
/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/identifier.js')} */
let IdentifierSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../complexTypes/reference.js')} */
let ReferenceSerializer;
/** @type {import('../complexTypes/period.js')} */
let PeriodSerializer;
/** @type {import('../backboneElements/explanationOfBenefitRelated.js')} */
let ExplanationOfBenefitRelatedSerializer;
/** @type {import('../backboneElements/explanationOfBenefitPayee.js')} */
let ExplanationOfBenefitPayeeSerializer;
/** @type {import('../backboneElements/explanationOfBenefitCareTeam.js')} */
let ExplanationOfBenefitCareTeamSerializer;
/** @type {import('../backboneElements/explanationOfBenefitSupportingInfo.js')} */
let ExplanationOfBenefitSupportingInfoSerializer;
/** @type {import('../backboneElements/explanationOfBenefitDiagnosis.js')} */
let ExplanationOfBenefitDiagnosisSerializer;
/** @type {import('../backboneElements/explanationOfBenefitProcedure.js')} */
let ExplanationOfBenefitProcedureSerializer;
/** @type {import('../backboneElements/explanationOfBenefitInsurance.js')} */
let ExplanationOfBenefitInsuranceSerializer;
/** @type {import('../backboneElements/explanationOfBenefitAccident.js')} */
let ExplanationOfBenefitAccidentSerializer;
/** @type {import('../backboneElements/explanationOfBenefitItem.js')} */
let ExplanationOfBenefitItemSerializer;
/** @type {import('../backboneElements/explanationOfBenefitAddItem.js')} */
let ExplanationOfBenefitAddItemSerializer;
/** @type {import('../backboneElements/explanationOfBenefitAdjudication.js')} */
let ExplanationOfBenefitAdjudicationSerializer;
/** @type {import('../backboneElements/explanationOfBenefitTotal.js')} */
let ExplanationOfBenefitTotalSerializer;
/** @type {import('../backboneElements/explanationOfBenefitPayment.js')} */
let ExplanationOfBenefitPaymentSerializer;
/** @type {import('../complexTypes/attachment.js')} */
let AttachmentSerializer;
/** @type {import('../backboneElements/explanationOfBenefitProcessNote.js')} */
let ExplanationOfBenefitProcessNoteSerializer;
/** @type {import('../backboneElements/explanationOfBenefitBenefitBalance.js')} */
let ExplanationOfBenefitBenefitBalanceSerializer;

class ExplanationOfBenefitSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        meta: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        status: null,
        type: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['type']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['type'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['type'];
        },
        subType: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['subType']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['subType'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['subType'];
        },
        use: null,
        patient: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['patient']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['patient'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['patient'];
        },
        billablePeriod: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['billablePeriod']) {
                if (!PeriodSerializer) {
                    PeriodSerializer = require('../complexTypes/period.js');
                }
                this.#configCache['billablePeriod'] = {
                    serializeFunction: 'serialize',
                    serializerClass: PeriodSerializer
                };
            }
            return this.#configCache['billablePeriod'];
        },
        created: null,
        enterer: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['enterer']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['enterer'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['enterer'];
        },
        insurer: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['insurer']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['insurer'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['insurer'];
        },
        provider: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['provider']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['provider'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['provider'];
        },
        priority: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['priority']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['priority'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['priority'];
        },
        fundsReserveRequested: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['fundsReserveRequested']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['fundsReserveRequested'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['fundsReserveRequested'];
        },
        fundsReserve: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['fundsReserve']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['fundsReserve'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['fundsReserve'];
        },
        related: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['related']) {
                if (!ExplanationOfBenefitRelatedSerializer) {
                    ExplanationOfBenefitRelatedSerializer = require('../backboneElements/explanationOfBenefitRelated.js');
                }
                this.#configCache['related'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExplanationOfBenefitRelatedSerializer
                };
            }
            return this.#configCache['related'];
        },
        prescription: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['prescription']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['prescription'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['prescription'];
        },
        originalPrescription: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['originalPrescription']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['originalPrescription'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['originalPrescription'];
        },
        payee: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['payee']) {
                if (!ExplanationOfBenefitPayeeSerializer) {
                    ExplanationOfBenefitPayeeSerializer = require('../backboneElements/explanationOfBenefitPayee.js');
                }
                this.#configCache['payee'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ExplanationOfBenefitPayeeSerializer
                };
            }
            return this.#configCache['payee'];
        },
        referral: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['referral']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['referral'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['referral'];
        },
        facility: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['facility']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['facility'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['facility'];
        },
        claim: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['claim']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['claim'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['claim'];
        },
        claimResponse: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['claimResponse']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['claimResponse'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['claimResponse'];
        },
        outcome: null,
        disposition: null,
        preAuthRef: null,
        preAuthRefPeriod: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['preAuthRefPeriod']) {
                if (!PeriodSerializer) {
                    PeriodSerializer = require('../complexTypes/period.js');
                }
                this.#configCache['preAuthRefPeriod'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: PeriodSerializer
                };
            }
            return this.#configCache['preAuthRefPeriod'];
        },
        careTeam: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['careTeam']) {
                if (!ExplanationOfBenefitCareTeamSerializer) {
                    ExplanationOfBenefitCareTeamSerializer = require('../backboneElements/explanationOfBenefitCareTeam.js');
                }
                this.#configCache['careTeam'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExplanationOfBenefitCareTeamSerializer
                };
            }
            return this.#configCache['careTeam'];
        },
        supportingInfo: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['supportingInfo']) {
                if (!ExplanationOfBenefitSupportingInfoSerializer) {
                    ExplanationOfBenefitSupportingInfoSerializer = require('../backboneElements/explanationOfBenefitSupportingInfo.js');
                }
                this.#configCache['supportingInfo'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExplanationOfBenefitSupportingInfoSerializer
                };
            }
            return this.#configCache['supportingInfo'];
        },
        diagnosis: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['diagnosis']) {
                if (!ExplanationOfBenefitDiagnosisSerializer) {
                    ExplanationOfBenefitDiagnosisSerializer = require('../backboneElements/explanationOfBenefitDiagnosis.js');
                }
                this.#configCache['diagnosis'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExplanationOfBenefitDiagnosisSerializer
                };
            }
            return this.#configCache['diagnosis'];
        },
        procedure: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['procedure']) {
                if (!ExplanationOfBenefitProcedureSerializer) {
                    ExplanationOfBenefitProcedureSerializer = require('../backboneElements/explanationOfBenefitProcedure.js');
                }
                this.#configCache['procedure'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExplanationOfBenefitProcedureSerializer
                };
            }
            return this.#configCache['procedure'];
        },
        precedence: null,
        insurance: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['insurance']) {
                if (!ExplanationOfBenefitInsuranceSerializer) {
                    ExplanationOfBenefitInsuranceSerializer = require('../backboneElements/explanationOfBenefitInsurance.js');
                }
                this.#configCache['insurance'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExplanationOfBenefitInsuranceSerializer
                };
            }
            return this.#configCache['insurance'];
        },
        accident: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['accident']) {
                if (!ExplanationOfBenefitAccidentSerializer) {
                    ExplanationOfBenefitAccidentSerializer = require('../backboneElements/explanationOfBenefitAccident.js');
                }
                this.#configCache['accident'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ExplanationOfBenefitAccidentSerializer
                };
            }
            return this.#configCache['accident'];
        },
        item: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['item']) {
                if (!ExplanationOfBenefitItemSerializer) {
                    ExplanationOfBenefitItemSerializer = require('../backboneElements/explanationOfBenefitItem.js');
                }
                this.#configCache['item'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExplanationOfBenefitItemSerializer
                };
            }
            return this.#configCache['item'];
        },
        addItem: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['addItem']) {
                if (!ExplanationOfBenefitAddItemSerializer) {
                    ExplanationOfBenefitAddItemSerializer = require('../backboneElements/explanationOfBenefitAddItem.js');
                }
                this.#configCache['addItem'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExplanationOfBenefitAddItemSerializer
                };
            }
            return this.#configCache['addItem'];
        },
        adjudication: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['adjudication']) {
                if (!ExplanationOfBenefitAdjudicationSerializer) {
                    ExplanationOfBenefitAdjudicationSerializer = require('../backboneElements/explanationOfBenefitAdjudication.js');
                }
                this.#configCache['adjudication'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExplanationOfBenefitAdjudicationSerializer
                };
            }
            return this.#configCache['adjudication'];
        },
        total: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['total']) {
                if (!ExplanationOfBenefitTotalSerializer) {
                    ExplanationOfBenefitTotalSerializer = require('../backboneElements/explanationOfBenefitTotal.js');
                }
                this.#configCache['total'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExplanationOfBenefitTotalSerializer
                };
            }
            return this.#configCache['total'];
        },
        payment: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['payment']) {
                if (!ExplanationOfBenefitPaymentSerializer) {
                    ExplanationOfBenefitPaymentSerializer = require('../backboneElements/explanationOfBenefitPayment.js');
                }
                this.#configCache['payment'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ExplanationOfBenefitPaymentSerializer
                };
            }
            return this.#configCache['payment'];
        },
        formCode: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['formCode']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['formCode'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['formCode'];
        },
        form: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['form']) {
                if (!AttachmentSerializer) {
                    AttachmentSerializer = require('../complexTypes/attachment.js');
                }
                this.#configCache['form'] = {
                    serializeFunction: 'serialize',
                    serializerClass: AttachmentSerializer
                };
            }
            return this.#configCache['form'];
        },
        processNote: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['processNote']) {
                if (!ExplanationOfBenefitProcessNoteSerializer) {
                    ExplanationOfBenefitProcessNoteSerializer = require('../backboneElements/explanationOfBenefitProcessNote.js');
                }
                this.#configCache['processNote'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExplanationOfBenefitProcessNoteSerializer
                };
            }
            return this.#configCache['processNote'];
        },
        benefitPeriod: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['benefitPeriod']) {
                if (!PeriodSerializer) {
                    PeriodSerializer = require('../complexTypes/period.js');
                }
                this.#configCache['benefitPeriod'] = {
                    serializeFunction: 'serialize',
                    serializerClass: PeriodSerializer
                };
            }
            return this.#configCache['benefitPeriod'];
        },
        benefitBalance: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['benefitBalance']) {
                if (!ExplanationOfBenefitBenefitBalanceSerializer) {
                    ExplanationOfBenefitBenefitBalanceSerializer = require('../backboneElements/explanationOfBenefitBenefitBalance.js');
                }
                this.#configCache['benefitBalance'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExplanationOfBenefitBenefitBalanceSerializer
                };
            }
            return this.#configCache['benefitBalance'];
        },
        resourceType: null
    };

    allPropertyToSerializerMap = {
        id: null,
        meta: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        identifier: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        status: null,
        type: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['type']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['type'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['type'];
        },
        subType: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['subType']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['subType'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['subType'];
        },
        use: null,
        patient: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['patient']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['patient'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['patient'];
        },
        billablePeriod: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['billablePeriod']) {
                if (!PeriodSerializer) {
                    PeriodSerializer = require('../complexTypes/period.js');
                }
                this.#configCache['billablePeriod'] = {
                    serializeFunction: 'serialize',
                    serializerClass: PeriodSerializer
                };
            }
            return this.#configCache['billablePeriod'];
        },
        created: null,
        enterer: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['enterer']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['enterer'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['enterer'];
        },
        insurer: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['insurer']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['insurer'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['insurer'];
        },
        provider: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['provider']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['provider'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['provider'];
        },
        priority: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['priority']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['priority'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['priority'];
        },
        fundsReserveRequested: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['fundsReserveRequested']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['fundsReserveRequested'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['fundsReserveRequested'];
        },
        fundsReserve: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['fundsReserve']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['fundsReserve'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['fundsReserve'];
        },
        related: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['related']) {
                if (!ExplanationOfBenefitRelatedSerializer) {
                    ExplanationOfBenefitRelatedSerializer = require('../backboneElements/explanationOfBenefitRelated.js');
                }
                this.#configCache['related'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExplanationOfBenefitRelatedSerializer
                };
            }
            return this.#configCache['related'];
        },
        prescription: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['prescription']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['prescription'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['prescription'];
        },
        originalPrescription: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['originalPrescription']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['originalPrescription'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['originalPrescription'];
        },
        payee: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['payee']) {
                if (!ExplanationOfBenefitPayeeSerializer) {
                    ExplanationOfBenefitPayeeSerializer = require('../backboneElements/explanationOfBenefitPayee.js');
                }
                this.#configCache['payee'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ExplanationOfBenefitPayeeSerializer
                };
            }
            return this.#configCache['payee'];
        },
        referral: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['referral']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['referral'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['referral'];
        },
        facility: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['facility']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['facility'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['facility'];
        },
        claim: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['claim']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['claim'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['claim'];
        },
        claimResponse: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['claimResponse']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['claimResponse'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['claimResponse'];
        },
        outcome: null,
        disposition: null,
        preAuthRef: null,
        preAuthRefPeriod: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['preAuthRefPeriod']) {
                if (!PeriodSerializer) {
                    PeriodSerializer = require('../complexTypes/period.js');
                }
                this.#configCache['preAuthRefPeriod'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: PeriodSerializer
                };
            }
            return this.#configCache['preAuthRefPeriod'];
        },
        careTeam: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['careTeam']) {
                if (!ExplanationOfBenefitCareTeamSerializer) {
                    ExplanationOfBenefitCareTeamSerializer = require('../backboneElements/explanationOfBenefitCareTeam.js');
                }
                this.#configCache['careTeam'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExplanationOfBenefitCareTeamSerializer
                };
            }
            return this.#configCache['careTeam'];
        },
        supportingInfo: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['supportingInfo']) {
                if (!ExplanationOfBenefitSupportingInfoSerializer) {
                    ExplanationOfBenefitSupportingInfoSerializer = require('../backboneElements/explanationOfBenefitSupportingInfo.js');
                }
                this.#configCache['supportingInfo'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExplanationOfBenefitSupportingInfoSerializer
                };
            }
            return this.#configCache['supportingInfo'];
        },
        diagnosis: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['diagnosis']) {
                if (!ExplanationOfBenefitDiagnosisSerializer) {
                    ExplanationOfBenefitDiagnosisSerializer = require('../backboneElements/explanationOfBenefitDiagnosis.js');
                }
                this.#configCache['diagnosis'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExplanationOfBenefitDiagnosisSerializer
                };
            }
            return this.#configCache['diagnosis'];
        },
        procedure: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['procedure']) {
                if (!ExplanationOfBenefitProcedureSerializer) {
                    ExplanationOfBenefitProcedureSerializer = require('../backboneElements/explanationOfBenefitProcedure.js');
                }
                this.#configCache['procedure'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExplanationOfBenefitProcedureSerializer
                };
            }
            return this.#configCache['procedure'];
        },
        precedence: null,
        insurance: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['insurance']) {
                if (!ExplanationOfBenefitInsuranceSerializer) {
                    ExplanationOfBenefitInsuranceSerializer = require('../backboneElements/explanationOfBenefitInsurance.js');
                }
                this.#configCache['insurance'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExplanationOfBenefitInsuranceSerializer
                };
            }
            return this.#configCache['insurance'];
        },
        accident: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['accident']) {
                if (!ExplanationOfBenefitAccidentSerializer) {
                    ExplanationOfBenefitAccidentSerializer = require('../backboneElements/explanationOfBenefitAccident.js');
                }
                this.#configCache['accident'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ExplanationOfBenefitAccidentSerializer
                };
            }
            return this.#configCache['accident'];
        },
        item: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['item']) {
                if (!ExplanationOfBenefitItemSerializer) {
                    ExplanationOfBenefitItemSerializer = require('../backboneElements/explanationOfBenefitItem.js');
                }
                this.#configCache['item'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExplanationOfBenefitItemSerializer
                };
            }
            return this.#configCache['item'];
        },
        addItem: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['addItem']) {
                if (!ExplanationOfBenefitAddItemSerializer) {
                    ExplanationOfBenefitAddItemSerializer = require('../backboneElements/explanationOfBenefitAddItem.js');
                }
                this.#configCache['addItem'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExplanationOfBenefitAddItemSerializer
                };
            }
            return this.#configCache['addItem'];
        },
        adjudication: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['adjudication']) {
                if (!ExplanationOfBenefitAdjudicationSerializer) {
                    ExplanationOfBenefitAdjudicationSerializer = require('../backboneElements/explanationOfBenefitAdjudication.js');
                }
                this.#configCache['adjudication'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExplanationOfBenefitAdjudicationSerializer
                };
            }
            return this.#configCache['adjudication'];
        },
        total: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['total']) {
                if (!ExplanationOfBenefitTotalSerializer) {
                    ExplanationOfBenefitTotalSerializer = require('../backboneElements/explanationOfBenefitTotal.js');
                }
                this.#configCache['total'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExplanationOfBenefitTotalSerializer
                };
            }
            return this.#configCache['total'];
        },
        payment: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['payment']) {
                if (!ExplanationOfBenefitPaymentSerializer) {
                    ExplanationOfBenefitPaymentSerializer = require('../backboneElements/explanationOfBenefitPayment.js');
                }
                this.#configCache['payment'] = {
                    serializeFunction: 'serialize',
                    serializerClass: ExplanationOfBenefitPaymentSerializer
                };
            }
            return this.#configCache['payment'];
        },
        formCode: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['formCode']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['formCode'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['formCode'];
        },
        form: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['form']) {
                if (!AttachmentSerializer) {
                    AttachmentSerializer = require('../complexTypes/attachment.js');
                }
                this.#configCache['form'] = {
                    serializeFunction: 'serialize',
                    serializerClass: AttachmentSerializer
                };
            }
            return this.#configCache['form'];
        },
        processNote: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['processNote']) {
                if (!ExplanationOfBenefitProcessNoteSerializer) {
                    ExplanationOfBenefitProcessNoteSerializer = require('../backboneElements/explanationOfBenefitProcessNote.js');
                }
                this.#configCache['processNote'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExplanationOfBenefitProcessNoteSerializer
                };
            }
            return this.#configCache['processNote'];
        },
        benefitPeriod: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['benefitPeriod']) {
                if (!PeriodSerializer) {
                    PeriodSerializer = require('../complexTypes/period.js');
                }
                this.#configCache['benefitPeriod'] = {
                    serializeFunction: 'serialize',
                    serializerClass: PeriodSerializer
                };
            }
            return this.#configCache['benefitPeriod'];
        },
        benefitBalance: () => {
            // Lazy load serializer only when first accessed (with caching)
            if (!this.#configCache['benefitBalance']) {
                if (!ExplanationOfBenefitBenefitBalanceSerializer) {
                    ExplanationOfBenefitBenefitBalanceSerializer = require('../backboneElements/explanationOfBenefitBenefitBalance.js');
                }
                this.#configCache['benefitBalance'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExplanationOfBenefitBenefitBalanceSerializer
                };
            }
            return this.#configCache['benefitBalance'];
        },
        resourceType: null,
        _access: null,
        _sourceAssigningAuthority: null,
        _uuid: null,
        _sourceId: null
    };
}

module.exports = new ExplanationOfBenefitSerializer();
