// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/meta.js')} */
let MetaSerializer;
/** @type {import('../complexTypes/narrative.js')} */
let NarrativeSerializer;
/** @type {import('../customSerializers/resourceContainer.js')} */
let ResourceContainerSerializer;
/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/identifier.js')} */
let IdentifierSerializer;
/** @type {import('../complexTypes/contactDetail.js')} */
let ContactDetailSerializer;
/** @type {import('../complexTypes/usageContext.js')} */
let UsageContextSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../backboneElements/testScriptOrigin.js')} */
let TestScriptOriginSerializer;
/** @type {import('../backboneElements/testScriptDestination.js')} */
let TestScriptDestinationSerializer;
/** @type {import('../backboneElements/testScriptMetadata.js')} */
let TestScriptMetadataSerializer;
/** @type {import('../backboneElements/testScriptFixture.js')} */
let TestScriptFixtureSerializer;
/** @type {import('../complexTypes/reference.js')} */
let ReferenceSerializer;
/** @type {import('../backboneElements/testScriptVariable.js')} */
let TestScriptVariableSerializer;
/** @type {import('../backboneElements/testScriptSetup.js')} */
let TestScriptSetupSerializer;
/** @type {import('../backboneElements/testScriptTest.js')} */
let TestScriptTestSerializer;
/** @type {import('../backboneElements/testScriptTeardown.js')} */
let TestScriptTeardownSerializer;

class TestScriptSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        meta: () => {
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        url: null,
        identifier: () => {
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serialize',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        version: null,
        name: null,
        title: null,
        status: null,
        experimental: null,
        date: null,
        publisher: null,
        contact: () => {
            if (!this.#configCache['contact']) {
                if (!ContactDetailSerializer) {
                    ContactDetailSerializer = require('../complexTypes/contactDetail.js');
                }
                this.#configCache['contact'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ContactDetailSerializer
                };
            }
            return this.#configCache['contact'];
        },
        description: null,
        useContext: () => {
            if (!this.#configCache['useContext']) {
                if (!UsageContextSerializer) {
                    UsageContextSerializer = require('../complexTypes/usageContext.js');
                }
                this.#configCache['useContext'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: UsageContextSerializer
                };
            }
            return this.#configCache['useContext'];
        },
        jurisdiction: () => {
            if (!this.#configCache['jurisdiction']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['jurisdiction'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['jurisdiction'];
        },
        purpose: null,
        copyright: null,
        origin: () => {
            if (!this.#configCache['origin']) {
                if (!TestScriptOriginSerializer) {
                    TestScriptOriginSerializer = require('../backboneElements/testScriptOrigin.js');
                }
                this.#configCache['origin'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: TestScriptOriginSerializer
                };
            }
            return this.#configCache['origin'];
        },
        destination: () => {
            if (!this.#configCache['destination']) {
                if (!TestScriptDestinationSerializer) {
                    TestScriptDestinationSerializer = require('../backboneElements/testScriptDestination.js');
                }
                this.#configCache['destination'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: TestScriptDestinationSerializer
                };
            }
            return this.#configCache['destination'];
        },
        metadata: () => {
            if (!this.#configCache['metadata']) {
                if (!TestScriptMetadataSerializer) {
                    TestScriptMetadataSerializer = require('../backboneElements/testScriptMetadata.js');
                }
                this.#configCache['metadata'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TestScriptMetadataSerializer
                };
            }
            return this.#configCache['metadata'];
        },
        fixture: () => {
            if (!this.#configCache['fixture']) {
                if (!TestScriptFixtureSerializer) {
                    TestScriptFixtureSerializer = require('../backboneElements/testScriptFixture.js');
                }
                this.#configCache['fixture'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: TestScriptFixtureSerializer
                };
            }
            return this.#configCache['fixture'];
        },
        profile: () => {
            if (!this.#configCache['profile']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['profile'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['profile'];
        },
        variable: () => {
            if (!this.#configCache['variable']) {
                if (!TestScriptVariableSerializer) {
                    TestScriptVariableSerializer = require('../backboneElements/testScriptVariable.js');
                }
                this.#configCache['variable'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: TestScriptVariableSerializer
                };
            }
            return this.#configCache['variable'];
        },
        setup: () => {
            if (!this.#configCache['setup']) {
                if (!TestScriptSetupSerializer) {
                    TestScriptSetupSerializer = require('../backboneElements/testScriptSetup.js');
                }
                this.#configCache['setup'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TestScriptSetupSerializer
                };
            }
            return this.#configCache['setup'];
        },
        test: () => {
            if (!this.#configCache['test']) {
                if (!TestScriptTestSerializer) {
                    TestScriptTestSerializer = require('../backboneElements/testScriptTest.js');
                }
                this.#configCache['test'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: TestScriptTestSerializer
                };
            }
            return this.#configCache['test'];
        },
        teardown: () => {
            if (!this.#configCache['teardown']) {
                if (!TestScriptTeardownSerializer) {
                    TestScriptTeardownSerializer = require('../backboneElements/testScriptTeardown.js');
                }
                this.#configCache['teardown'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TestScriptTeardownSerializer
                };
            }
            return this.#configCache['teardown'];
        },
        resourceType: null
    };

    allPropertyToSerializerMap = {
        id: null,
        meta: () => {
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        url: null,
        identifier: () => {
            if (!this.#configCache['identifier']) {
                if (!IdentifierSerializer) {
                    IdentifierSerializer = require('../complexTypes/identifier.js');
                }
                this.#configCache['identifier'] = {
                    serializeFunction: 'serialize',
                    serializerClass: IdentifierSerializer
                };
            }
            return this.#configCache['identifier'];
        },
        version: null,
        name: null,
        title: null,
        status: null,
        experimental: null,
        date: null,
        publisher: null,
        contact: () => {
            if (!this.#configCache['contact']) {
                if (!ContactDetailSerializer) {
                    ContactDetailSerializer = require('../complexTypes/contactDetail.js');
                }
                this.#configCache['contact'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ContactDetailSerializer
                };
            }
            return this.#configCache['contact'];
        },
        description: null,
        useContext: () => {
            if (!this.#configCache['useContext']) {
                if (!UsageContextSerializer) {
                    UsageContextSerializer = require('../complexTypes/usageContext.js');
                }
                this.#configCache['useContext'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: UsageContextSerializer
                };
            }
            return this.#configCache['useContext'];
        },
        jurisdiction: () => {
            if (!this.#configCache['jurisdiction']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['jurisdiction'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['jurisdiction'];
        },
        purpose: null,
        copyright: null,
        origin: () => {
            if (!this.#configCache['origin']) {
                if (!TestScriptOriginSerializer) {
                    TestScriptOriginSerializer = require('../backboneElements/testScriptOrigin.js');
                }
                this.#configCache['origin'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: TestScriptOriginSerializer
                };
            }
            return this.#configCache['origin'];
        },
        destination: () => {
            if (!this.#configCache['destination']) {
                if (!TestScriptDestinationSerializer) {
                    TestScriptDestinationSerializer = require('../backboneElements/testScriptDestination.js');
                }
                this.#configCache['destination'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: TestScriptDestinationSerializer
                };
            }
            return this.#configCache['destination'];
        },
        metadata: () => {
            if (!this.#configCache['metadata']) {
                if (!TestScriptMetadataSerializer) {
                    TestScriptMetadataSerializer = require('../backboneElements/testScriptMetadata.js');
                }
                this.#configCache['metadata'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TestScriptMetadataSerializer
                };
            }
            return this.#configCache['metadata'];
        },
        fixture: () => {
            if (!this.#configCache['fixture']) {
                if (!TestScriptFixtureSerializer) {
                    TestScriptFixtureSerializer = require('../backboneElements/testScriptFixture.js');
                }
                this.#configCache['fixture'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: TestScriptFixtureSerializer
                };
            }
            return this.#configCache['fixture'];
        },
        profile: () => {
            if (!this.#configCache['profile']) {
                if (!ReferenceSerializer) {
                    ReferenceSerializer = require('../complexTypes/reference.js');
                }
                this.#configCache['profile'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ReferenceSerializer
                };
            }
            return this.#configCache['profile'];
        },
        variable: () => {
            if (!this.#configCache['variable']) {
                if (!TestScriptVariableSerializer) {
                    TestScriptVariableSerializer = require('../backboneElements/testScriptVariable.js');
                }
                this.#configCache['variable'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: TestScriptVariableSerializer
                };
            }
            return this.#configCache['variable'];
        },
        setup: () => {
            if (!this.#configCache['setup']) {
                if (!TestScriptSetupSerializer) {
                    TestScriptSetupSerializer = require('../backboneElements/testScriptSetup.js');
                }
                this.#configCache['setup'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TestScriptSetupSerializer
                };
            }
            return this.#configCache['setup'];
        },
        test: () => {
            if (!this.#configCache['test']) {
                if (!TestScriptTestSerializer) {
                    TestScriptTestSerializer = require('../backboneElements/testScriptTest.js');
                }
                this.#configCache['test'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: TestScriptTestSerializer
                };
            }
            return this.#configCache['test'];
        },
        teardown: () => {
            if (!this.#configCache['teardown']) {
                if (!TestScriptTeardownSerializer) {
                    TestScriptTeardownSerializer = require('../backboneElements/testScriptTeardown.js');
                }
                this.#configCache['teardown'] = {
                    serializeFunction: 'serialize',
                    serializerClass: TestScriptTeardownSerializer
                };
            }
            return this.#configCache['teardown'];
        },
        resourceType: null,
        _access: null,
        _sourceAssigningAuthority: null,
        _uuid: null,
        _sourceId: null
    };
}

module.exports = new TestScriptSerializer();
