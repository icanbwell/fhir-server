// This file is auto-generated by generate_serializers so do not edit manually
const BaseSerializer = require('../customSerializers/baseSerializer.js');

/** @type {import('../complexTypes/meta.js')} */
let MetaSerializer;
/** @type {import('../complexTypes/narrative.js')} */
let NarrativeSerializer;
/** @type {import('../customSerializers/resourceContainer.js')} */
let ResourceContainerSerializer;
/** @type {import('../complexTypes/extension.js')} */
let ExtensionSerializer;
/** @type {import('../complexTypes/coding.js')} */
let CodingSerializer;
/** @type {import('../complexTypes/period.js')} */
let PeriodSerializer;
/** @type {import('../complexTypes/codeableConcept.js')} */
let CodeableConceptSerializer;
/** @type {import('../backboneElements/auditEventAgent.js')} */
let AuditEventAgentSerializer;
/** @type {import('../backboneElements/auditEventSource.js')} */
let AuditEventSourceSerializer;
/** @type {import('../backboneElements/auditEventEntity.js')} */
let AuditEventEntitySerializer;

class AuditEventSerializer extends BaseSerializer {
    // Private cache for lazy-loaded property configs
    #configCache = {};

    fhirPropertyToSerializerMap = {
        id: null,
        meta: () => {
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        type: () => {
            if (!this.#configCache['type']) {
                if (!CodingSerializer) {
                    CodingSerializer = require('../complexTypes/coding.js');
                }
                this.#configCache['type'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodingSerializer
                };
            }
            return this.#configCache['type'];
        },
        subtype: () => {
            if (!this.#configCache['subtype']) {
                if (!CodingSerializer) {
                    CodingSerializer = require('../complexTypes/coding.js');
                }
                this.#configCache['subtype'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodingSerializer
                };
            }
            return this.#configCache['subtype'];
        },
        action: null,
        period: () => {
            if (!this.#configCache['period']) {
                if (!PeriodSerializer) {
                    PeriodSerializer = require('../complexTypes/period.js');
                }
                this.#configCache['period'] = {
                    serializeFunction: 'serialize',
                    serializerClass: PeriodSerializer
                };
            }
            return this.#configCache['period'];
        },
        recorded: null,
        outcome: null,
        outcomeDesc: null,
        purposeOfEvent: () => {
            if (!this.#configCache['purposeOfEvent']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['purposeOfEvent'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['purposeOfEvent'];
        },
        agent: () => {
            if (!this.#configCache['agent']) {
                if (!AuditEventAgentSerializer) {
                    AuditEventAgentSerializer = require('../backboneElements/auditEventAgent.js');
                }
                this.#configCache['agent'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: AuditEventAgentSerializer
                };
            }
            return this.#configCache['agent'];
        },
        source: () => {
            if (!this.#configCache['source']) {
                if (!AuditEventSourceSerializer) {
                    AuditEventSourceSerializer = require('../backboneElements/auditEventSource.js');
                }
                this.#configCache['source'] = {
                    serializeFunction: 'serialize',
                    serializerClass: AuditEventSourceSerializer
                };
            }
            return this.#configCache['source'];
        },
        entity: () => {
            if (!this.#configCache['entity']) {
                if (!AuditEventEntitySerializer) {
                    AuditEventEntitySerializer = require('../backboneElements/auditEventEntity.js');
                }
                this.#configCache['entity'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: AuditEventEntitySerializer
                };
            }
            return this.#configCache['entity'];
        },
        resourceType: null
    };

    allPropertyToSerializerMap = {
        id: null,
        meta: () => {
            if (!this.#configCache['meta']) {
                if (!MetaSerializer) {
                    MetaSerializer = require('../complexTypes/meta.js');
                }
                this.#configCache['meta'] = {
                    serializeFunction: 'serialize',
                    serializerClass: MetaSerializer
                };
            }
            return this.#configCache['meta'];
        },
        implicitRules: null,
        language: null,
        text: () => {
            if (!this.#configCache['text']) {
                if (!NarrativeSerializer) {
                    NarrativeSerializer = require('../complexTypes/narrative.js');
                }
                this.#configCache['text'] = {
                    serializeFunction: 'serialize',
                    serializerClass: NarrativeSerializer
                };
            }
            return this.#configCache['text'];
        },
        contained: () => {
            if (!this.#configCache['contained']) {
                if (!ResourceContainerSerializer) {
                    ResourceContainerSerializer = require('../customSerializers/resourceContainer.js');
                }
                this.#configCache['contained'] = {
                    serializeFunction: 'serializeArray'
                };
            }
            return this.#configCache['contained'];
        },
        extension: () => {
            if (!this.#configCache['extension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['extension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['extension'];
        },
        modifierExtension: () => {
            if (!this.#configCache['modifierExtension']) {
                if (!ExtensionSerializer) {
                    ExtensionSerializer = require('../complexTypes/extension.js');
                }
                this.#configCache['modifierExtension'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: ExtensionSerializer
                };
            }
            return this.#configCache['modifierExtension'];
        },
        type: () => {
            if (!this.#configCache['type']) {
                if (!CodingSerializer) {
                    CodingSerializer = require('../complexTypes/coding.js');
                }
                this.#configCache['type'] = {
                    serializeFunction: 'serialize',
                    serializerClass: CodingSerializer
                };
            }
            return this.#configCache['type'];
        },
        subtype: () => {
            if (!this.#configCache['subtype']) {
                if (!CodingSerializer) {
                    CodingSerializer = require('../complexTypes/coding.js');
                }
                this.#configCache['subtype'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodingSerializer
                };
            }
            return this.#configCache['subtype'];
        },
        action: null,
        period: () => {
            if (!this.#configCache['period']) {
                if (!PeriodSerializer) {
                    PeriodSerializer = require('../complexTypes/period.js');
                }
                this.#configCache['period'] = {
                    serializeFunction: 'serialize',
                    serializerClass: PeriodSerializer
                };
            }
            return this.#configCache['period'];
        },
        recorded: null,
        outcome: null,
        outcomeDesc: null,
        purposeOfEvent: () => {
            if (!this.#configCache['purposeOfEvent']) {
                if (!CodeableConceptSerializer) {
                    CodeableConceptSerializer = require('../complexTypes/codeableConcept.js');
                }
                this.#configCache['purposeOfEvent'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: CodeableConceptSerializer
                };
            }
            return this.#configCache['purposeOfEvent'];
        },
        agent: () => {
            if (!this.#configCache['agent']) {
                if (!AuditEventAgentSerializer) {
                    AuditEventAgentSerializer = require('../backboneElements/auditEventAgent.js');
                }
                this.#configCache['agent'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: AuditEventAgentSerializer
                };
            }
            return this.#configCache['agent'];
        },
        source: () => {
            if (!this.#configCache['source']) {
                if (!AuditEventSourceSerializer) {
                    AuditEventSourceSerializer = require('../backboneElements/auditEventSource.js');
                }
                this.#configCache['source'] = {
                    serializeFunction: 'serialize',
                    serializerClass: AuditEventSourceSerializer
                };
            }
            return this.#configCache['source'];
        },
        entity: () => {
            if (!this.#configCache['entity']) {
                if (!AuditEventEntitySerializer) {
                    AuditEventEntitySerializer = require('../backboneElements/auditEventEntity.js');
                }
                this.#configCache['entity'] = {
                    serializeFunction: 'serializeArray',
                    serializerClass: AuditEventEntitySerializer
                };
            }
            return this.#configCache['entity'];
        },
        resourceType: null,
        _access: null,
        _sourceAssigningAuthority: null,
        _uuid: null,
        _sourceId: null
    };
}

module.exports = new AuditEventSerializer();
