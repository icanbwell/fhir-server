<% if (!Array.isArray(references)) {
    references = [references];
} %>
<% if (references && references.length > 0 && references[0]) { %>
    <div>
        <h4><%= name %></h4>
        <% references.forEach(function(reference){ %>
            <% if (reference){ %>
                <a href="/4_0_0/<%= reference.reference %>?_keepOldUI=1">
                    <% if (reference.display) { %><%= reference.display %>
                    <% } else { %><%= reference.reference %>
                    <% } %>
                </a>
            <% } %>
        <% }) %>
    </div>
<% } %>
