import React from 'react';
import {Box} from '@mui/material';

const MedicationAdministration = ({resource, admin, index}) => {

    return (
        <Box>
        </Box>
    );
};

export default MedicationAdministration;
