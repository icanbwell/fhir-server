import React from 'react';
import {Box} from '@mui/material';

const MedicationDispense = ({resource, admin, index}) => {

    return (
        <Box>
        </Box>
    );
};

export default MedicationDispense;
