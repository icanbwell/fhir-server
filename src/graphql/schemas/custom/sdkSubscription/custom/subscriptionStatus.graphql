# This schema is temporary until we switch our Cosmo Graph to use federation.
# See https://icanbwell.atlassian.net/browse/EFS-985
# import SubscriptionTopic from 'SubscriptionTopic.graphql'

extend type Subscription_SubscriptionStatus {
    subscriptionTopic: Subscription_SubscriptionTopic
}
