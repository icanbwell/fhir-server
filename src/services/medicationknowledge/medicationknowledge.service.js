// Autogenerated by generate_services.py. Do not edit.
const { COLLECTION } = require('../../constants');
const base_service = require('../base/base.service');

const resource_name = 'MedicationKnowledge';
const collection_name = COLLECTION.MEDICATIONKNOWLEDGE;

module.exports.search = (args, { req, res }) =>
  base_service.search(args, { req, res }, resource_name, collection_name);

module.exports.searchById = (args, { req, res }) =>
  base_service.searchById(args, { req, res }, resource_name, collection_name);

module.exports.create = (args, { req, res }) =>
  base_service.create(args, { req, res }, resource_name, collection_name);

module.exports.update = (args, { req, res }) =>
  base_service.update(args, { req, res }, resource_name, collection_name);

module.exports.merge = (args, { req, res }) =>
  base_service.merge(args, { req, res }, resource_name, collection_name);

module.exports.remove = (args, { req, res }) =>
  base_service.remove(args, { req, res }, resource_name, collection_name);

module.exports.searchByVersionId = (args, { req, res }) =>
  base_service.search(args, { req, res }, resource_name, collection_name);

module.exports.everything = (args, { req, res }) =>
  base_service.everything(args, { req, res }, resource_name, collection_name);

module.exports.history = (args, { req, res }) =>
  base_service.history(args, { req, res }, resource_name, collection_name);

module.exports.historyById = (args, { req, res }) =>
  base_service.historyById(args, { req, res }, resource_name, collection_name);

module.exports.patch = (args, { req, res }) =>
  base_service.patch(args, { req, res }, resource_name, collection_name);

module.exports.validate = (args, { req, res }) =>
  base_service.validate(args, { req, res }, resource_name);

module.exports.graph = (args, { req, res }) =>
  base_service.graph(args, { req, res }, resource_name, collection_name);

module.exports.expand = (args, { req, res }) =>
  base_service.expand(args, { req, res }, resource_name, collection_name);


